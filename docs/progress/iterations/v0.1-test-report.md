# v0.1 Developer 自测报告 — news-l1 真实化

> 测试阶段已并入实现：Developer 实现完成即做自测（接口 / 自动化），证据必留。
> 手动点击验收由 Owner 在迭代关闭前完成（见迭代记录「Owner 验收」）。

## 文档状态
- 整体状态：已定稿（Architect + DevOps 两方通过，2026-07-04）
- Review 轮次：R1
- 关联迭代：v0.1（实现阶段 R1）
- 关联实现：base_commit `2605c07` → head_commit `344ad49`（S1~S5 分片，S5=CN-002 KB 主动检索纳入 v0.1，见 `v0.1.md` 实现进度）

## Review 计划
- Review 模式：动态 Review
- 影响领域：验收标准 / 边界 / 回归复核（实现是否满足 AC-1~9）、部署配置与发布风险
- 本轮指定 Review 方：
  - **Architect** — 验收标准 / 边界 / 回归的独立复核，实现是否忠实设计与 ADR、契约不变
  - **DevOps** — provider / Tavily 配置、密钥注入、超时与部署冒烟检查项落实
- 未指定角色及原因：
  - PM — 验收标准产出方，不自审（Developer 手册质量门禁分工）
  - Developer — 本报告产出方，提供证据非 Review
- 少于 2 个 Review 方时的用户确认：不适用（已 2 方）
- 定稿条件：两方通过且无未处理高严重度缺陷

## Review 状态
| Review 方 | 指定原因 | 状态 | 轮次 |
|-----------|----------|------|------|
| Architect | 验收 / 边界 / 回归 + 设计忠实度 | 通过（附 5 条非阻塞观察项） | R1 |
| DevOps | provider / Tavily 配置 + 部署冒烟检查项 | 通过（附 4 条发布检查项跟踪到部署阶段） | R1 |

## 结论
- 自测判定：**通过**（设计 §8 全部 10 项 + 补充用例自动化覆盖，40 passed，0 失败 0 阻塞）
- 说明：真实外部调用（LLM `_http_call_provider`、link 自抓、Tavily 搜索）的**连通性**未在单测覆盖（注入 fake 隔离网络），已由 DevOps 部署冒烟 + 跨项目端到端联调验证（见 D-1 闭环）。

## 自测范围（四维）

### 正常路径
- `POST /v1/runs/news-l1` 合法证据 → 200 + `succeeded` + 完整 `L1Output`（四维评分 / 五类标签 / 摘要 / 翻译）。
- 内部 registry dispatch `news-l1` 正确。
- 证据不足且有 URL → 触发 link read 并计数；Tavily 已配置且证据不足 → 触发 web search 并计数。
- 多 provider：首选 provider 成功即返回。

### 边界值
- 越界 `score`（9 / -2）→ clamp 到 0-5。
- provider 配置：多 provider JSON / 单 provider 兼容 / 空配置（→ `UnconfiguredClient`）。
- LLM 空响应、JSON 前后夹文本、markdown fence 包裹 → 修复解析。
- 上下文充分性阈值：`raw_text` 长文（>300 字）跳过工具；短文触发。
- 预取上下文存在 → 不触发对应主动工具。

### 异常路径
- 全部 provider 失败 → `AllProvidersFailedError` → `status=failed` + `output=null` + `error`。
- link / web 抓取失败 → recoverable 降级，`succeeded` + `tags.processing` 含 `degraded:*`。
- 不可恢复 JSON → `JSONParseError` → fallback 下一 provider。
- 非法 `LLM_PROVIDERS_JSON` / 缺必填字段 → `ProviderConfigError`。
- 未知 task_type → `UnknownTaskError`（内部错误，不暴露为新 HTTP 路由，通用路径 404）。

### 状态转换
- `succeeded` / `failed` 语义对齐 AC-6：部分可用（工具失败 / backup 命中 / 证据不足）均 `succeeded`；完全不可用才 `failed`。
- `needs_context`：工具后证据仍不足 → true（主动 KB 未落地缺口标记）。
- provider quirk：`response_format` / `temperature` 不支持 → 同 provider 调整参数重试一次。

## 结果摘要

| 类型 | 数量 |
|------|------|
| 通过 | 40 |
| 失败 | 0 |
| 阻塞 | 0 |

> S5（CN-002 KB 主动检索）追加 4 用例：KB 配置且证据不足时触发并优先于 link/web（`test_kb_search_triggered_first_when_configured`）、预取 `kb_results` 存在不主动调（`test_kb_not_triggered_when_prefetched`）、未配置 `KB_SEARCH_URL` 不触发（`test_kb_not_triggered_when_unconfigured`）、KB 调用失败降级但 succeeded（`test_kb_fail_degraded_but_succeeded`）。真实 xiaobao `/v1/kb-search` 连通性并入 D-1 部署冒烟。

### 设计 §8 测试清单覆盖

| # | 设计要求 | 覆盖用例 |
|---|----------|----------|
| 1 | endpoint 兼容原契约 | `test_endpoint_contract_compatible` |
| 2 | registry 注册、未知 task 不暴露新路由 | `test_registry_registers_news_l1` / `test_unknown_task_raises_internal_error` / `test_no_generic_task_route_exposed` |
| 3 | 预取上下文消费但不计入 `tool_summary` | `test_prefetch_context_not_counted_in_tool_summary` |
| 4 | 有 URL 且上下文不足触发 link read 计数 | `test_link_read_triggered_when_context_insufficient` |
| 5 | Tavily 缺失不触发 web search，可降级 | `test_web_search_not_triggered_when_tavily_unconfigured` |
| 6 | 主 provider 超时/429/5xx/空响应 fallback | `test_fallback_to_next_provider[*]` / `test_parse_error_falls_back` / quirk 用例 |
| 7 | 全 provider 失败返回 failed + output=null | `test_all_providers_fail_returns_failed` / `test_all_providers_fail_raises` |
| 8 | link/web 失败但 LLM 成功 → succeeded + 降级标记 | `test_link_fail_llm_ok_returns_succeeded_with_degradation` |
| 9 | 越界 score normalize 到 0-5 | `test_out_of_range_scores_clamped` |
| 10 | context URL 必须来自工具结果，否则过滤 | `test_context_url_filtered_to_evidence` |

## 缺陷列表

| 编号 | 严重程度 | 描述 | 状态 | 责任角色 |
|------|----------|------|------|----------|
| D-1 | 中 | 真实外部调用（LLM / link 自抓 / Tavily）连通性未在单测覆盖，仅注入 fake 覆盖编排逻辑 | ✅ 已闭环（2026-07-01 ai 冒烟 + 2026-07-04 跨项目端到端联调 4 条用例通过） | DevOps |
| D-2 | 低 | 上下文充分性为首版启发式阈值（raw<300 且 ctx<500），质量待真实数据调优 | 记录，不阻塞 | Developer / 后续迭代 |
| D-3 | 低 | KB 空结果语义：xiaobao `/v1/kb-search` 返回 200 + `results: []` 时，ai 仍标 `degraded:kb_search_failed`，应优化为 `kb_search_empty` 或不降级 | 记录，不阻塞（coordination 2026-07-04 已登记，可入 v0.2 或独立任务） | Developer / 后续迭代 |

## 回归结论
- 全量 `pytest -q`：**40 passed, 0 failed**（含 S1 的 health/契约、S2 的 provider fallback/JSON、S3 的工具路由、S5 的 KB 主动检索 4 用例）。
- 无回归失败；旧 stub 口径（预取计入 `tool_summary`）已按新契约修正并有用例守护。
- D-1 真实端到端连通性已闭环：2026-07-01 ai 侧真实冒烟 succeeded（run `run_bcf24393b947`）+ 2026-07-04 跨项目端到端联调 4 条用例通过（见 coordination `communications/REQ-001-news-l1.md`）。

## Review 记录

### 实现 R1 — Architect Review（2026-07-04）

- 结论：**通过**（实现忠实设计 ADR-0001/0002 与 PRD AC-1~9，对外契约不变；附 5 条非阻塞观察项，建议进入 DevOps Review）
- Review 范围（Architect 边界）：实现是否符合设计与 ADR、是否破坏对外契约、是否引入不可接受技术债、测试覆盖是否对齐设计 §8；不审部署密钥注入与运行环境（DevOps 边界）

核对确认：

1. **对外契约不变。** `schemas.py` 的 `L1Input`/`L1Output`/`RunResponse`/`ToolSummary` 字段与 news-l1 v1 一致；`main.py` 仅暴露 `/health` 与 `POST /v1/runs/news-l1`，未新增通用路由或 `caller`/`task_type` 传输字段（`test_no_generic_task_route_exposed` 守护）；failed 响应 `output=null` 对齐 v1。
2. **内部 registry（AC-9）落地。** `tasks.py` 实现 `TaskSpec`/`register_task`/`get_task`/`run_task`，只注册 news-l1；`UnknownTaskError` 不暴露为新 HTTP 路由。符合设计 §3。
3. **条件图编排（ADR-0001 / 设计 §4.3）忠实。** `ingest_context → route_after_ingest`（kb>link>web 优先级）`→ llm_process → normalize_output`；CN-002 主动 KB 节点优先级最高，符合迭代记录 S5。预算检查、上下文不足启发式、预取不计数、主动工具发起即计数、失败降级均符合设计 §4。
4. **ChainedAIClient（ADR-0002 / 设计 §3）fallback 矩阵完整。** rate_limited/timeout/5xx/auth/empty 均 fallback；400 quirk（`response_format`/`temperature`）同 provider 调整重试一次；JSON 解析失败先本地修复再 fallback；全失败抛 `AllProvidersFailedError`。显式维护总 timeout budget（`min(budget_ms, timeout_ms)` + remaining 扣减），承接 Developer R1 提示。`LLMResult` 最小字段（`provider_name`/`parsed`/`raw`/`degradations`）到位。
5. **降级与失败语义（AC-6 / 设计 §4.8）对齐。** 部分可用=`succeeded`+降级标记；完全不可用=`failed`+`output=null`+`error`。`normalize_output` 在 LLM 失败时保持 `output=None`。
6. **输出清洗（设计 §4.7）到位。** score clamp 0-5；context URL 过滤只保留证据 URL（`test_context_url_filtered_to_evidence`）；`processing` 标签含 `engine:agent_hub`+`llm:<provider>`+`degraded:*`；`needs_context` 标记。
7. **prompt（设计 §4.6 + PM R1 建议）完整。** system prompt 明确五类标签（processing 系统追加）、四维 0-5 整数+非空中文 reason、中文摘要、`translation.zh`、不编造 URL、只输出 JSON。
8. **测试覆盖（设计 §8）齐备。** 自测报告 40 passed 覆盖设计 §8 全 10 项 + KB 主动检索 4 用例 + provider fallback 矩阵 + JSON 修复；旧 stub 口径已修正有用例守护。真实外部连通性 D-1 待 DevOps 部署冒烟，不阻塞 Architect Review。
9. **`.env.example`（DevOps R1 检查项 1）已补全。** `LLM_PROVIDERS_JSON`、`BACKUP_OPENAI_API_KEY`、`TAVILY_API_KEY`、`KB_SEARCH_URL`/`KB_ADMIN_TOKEN` 均含示例且无真实密钥。

非阻塞观察项（不阻塞 R1 通过，建议后续处理）：

1. **[低] 设计文档未同步 CN-002 KB 主动路由。** 设计 §4.3/§4.6/风险表仍写「KB realtime 接口占位默认禁用」，实现已含主动 KB 节点且优先级最高。CN-002 已批准纳入 v0.1，实现合理。建议后续以 Change Note 或设计 R2 同步 KB 路由到设计文档，避免文档与实现长期不一致。
2. **[低] `normalize_output` 兜底未完全实现设计 §4.7。** 设计要求「缺失 reason 填降级说明」「summary 为空但 LLM 成功时填降级摘要并标 `degraded:summary_empty`」；代码均填空字符串。prompt 已要求非空，正常路径无问题；但 LLM 偶发不遵守 prompt 时可能产出空 reason 违反 AC-1「非空理由」。建议补兜底或确认 prompt 兜底足够。
3. **[低] `kb_search_node` 未传 `exclude_raw_item_id`。** 可能导致 KB 检索返回当前正在处理的新闻自身。当前 `L1Input` 无 `raw_item_id` 字段，无法传。需确认 coordination `kb-search` v1 契约是否要求此参数；若需要，属后续迭代补全（涉及契约/字段）。
4. **[低] `config.py` 的 `Config` 类与 `config` 实例为死代码。** `load_providers` 直接读 `os.getenv`，未用 `Config`。建议清理或确认用途。
5. **[低] `_http_call_provider` 400 非 quirk 错误的 kind 标记不准确。** 400 非 `response_format`/`temperature` 时抛 `kind="server_error"`，tag 映射为 `provider_5xx`，语义不准确（400 是客户端错误）。影响日志精度，不影响 fallback 行为。

下一步：
- DevOps Review 实现 R1（provider/Tavily/KB 配置 + 部署冒烟检查项 + D-1 真实连通性验证）。
- 两方均通过后实现 R1 定稿，进入 Owner 验收与迭代关闭检查。
- 非阻塞观察项 1/2 建议在实现 R2 或下一迭代处理；3/4/5 可视优先级安排。

### 实现 R1 — DevOps Review（2026-07-04）

- 结论：**通过**（实现 R1 从 DevOps 视角可定稿；附 4 条发布检查项跟踪到部署阶段，不阻塞）
- Review 范围（DevOps 边界）：部署配置、环境变量、密钥注入、外部依赖、健康检查、超时预算、发布风险与回滚条件；不审产品范围、架构选型或代码实现细节（Architect/Developer 边界）
- 关联证据：本次复核实读 `.env.example`、`src/agent_hub/{config,main}.py`、`src/agent_hub/llm/client.py`、`src/agent_hub/tools/{web_search,kb,link_reader}.py`、`requirements.txt`；运行状态实查 `curl 8100/health`、`lsof 8100/8001`、`ps uvicorn`

#### 设计 DevOps R1 三条发布检查项落实情况

1. **`.env.example` 补全** ✅ — 已含 `LLM_PROVIDERS_JSON`（双 provider 示例）、`OPENAI_API_KEY` / `BACKUP_OPENAI_API_KEY`、`TAVILY_API_KEY` / `WEB_SEARCH_MAX_RESULTS`、单 provider 兼容路径 `OPENAI_BASE_URL` / `L1_LLM_MODEL` / `LLM_TIMEOUT_MS`，并已补 CN-002 增量 `KB_SEARCH_URL` / `KB_ADMIN_TOKEN`。示例值无真实密钥。
2. **生产 ≥2 provider 验证** ⏳ — 测试环境仅火山单 provider（`doubao-seed-2.0-pro`），生产发布就绪判定需 ≥2 provider，跟踪到部署就绪检查阶段（不阻塞实现 R1）。
3. **健康检查 / 发布冒烟覆盖** 🟡 — `/health` 实现简单返回 200；2026-07-01 真实 `news-l1` 冒烟 `succeeded`（run `run_bcf24393b947`，四维评分 / 五类标签 / 中文摘要 / 分析真实产出），`web_search` 真实调 Tavily 通；provider fallback、Tavily 未配置降级、KB 未配置降级、越界 score clamp、context URL 过滤均有单测覆盖（设计 §8 用例 5/6/8/9/10）。真实 provider fallback（多 provider 矩阵）未在测试环境冒烟（单 provider），跟踪到部署阶段。

#### 确认项（与设计 / DevOps R1 一致）

1. **密钥注入边界正确** — `ProviderConfig` 仅存 `api_key_env`，运行时 `os.getenv(provider.api_key_env)` 读取；`TAVILY_API_KEY` / `KB_ADMIN_TOKEN` 均走环境变量，未写入 JSON 配置或代码字面量；`StepError.message` 设计要求脱敏（不写 key / 完整 prompt）。
2. **总 timeout budget 显式维护** — `ChainedAIClient.complete_json` 按 `min(self._budget_ms, timeout_ms)` 计算总预算，按 provider 消耗 `remaining`，`remaining <= 0` 即 `budget_exhausted` 终止（落实 Developer R1 实现提示 2）。
3. **provider fallback 矩阵落实** — `_http_call_provider` 对 408/timeout/网络错误 → `timeout`；401/403 → `auth`；429 → `rate_limited`；5xx 与其他 4xx → `server_error`；400 含 `response_format` / `temperature` → `ProviderQuirkError` 调整参数同 provider 重试一次；空响应 → `empty`；JSON 解析失败 → fallback 下一 provider。与设计 §3 矩阵一致。
4. **兼容路径** — `LLM_PROVIDERS_JSON` 缺失时回退单组 `OPENAI_*`；空配置 → `UnconfiguredClient` → `ProvidersNotConfiguredError` → 入口兜底转 `status=failed` + `error=ProvidersNotConfiguredError`（对齐 AC-6 完全失败）。
5. **Tavily / KB 未配置可降级** — `search_web` 缺 `TAVILY_API_KEY` 返回 `not_configured`、不计数、不阻塞；`search_kb` 缺 `KB_SEARCH_URL` 返回 `not_configured`、不阻塞，由 graph 路由标记 `needs_context=true`。与设计 §4.3/§4.5 一致。

#### 发布检查项（不阻塞实现 R1 定稿，跟踪到部署就绪检查阶段）

1. **[中] 当前服务实际未运行** — 本机 `8100` / `8001` 均无监听（`curl` 503、`lsof` 空、`ps` 无 uvicorn），与 `INDEX.md` / `v0.1.md` 记录的「2026-07-02 均监听」不符，印证 2026-07-02 收尾遗留风险「`nohup` 起、非托管、重启不自动拉起」。建议部署阶段补 systemd / supervisor / launchd 托管方案；当前部署就绪检查记录需更新为实况。
2. **[中] 日志可观测性缺失** — `main.py` / `llm/client.py` / `tools/*` 均无 `logging` 输出，错误只通过 `RunResponse.error` 返回（仅 `type(exc).__name__`，已脱敏）。设计 DevOps R1 检查项 3「日志不输出 API key / 完整 prompt」严格讲需可观测的脱敏日志以供发布排障；建议部署阶段补结构化 `logging`（run_id / provider / step / 耗时 / 错误 kind），明确禁止 `logging` 输出 `api_key` / `Authorization` / 完整 `messages`。
3. **[低] 生产 ≥2 provider 未验证** — 测试环境仅火山单 provider，发布就绪判定需 ≥2 provider 真实 fallback 冒烟（设计 DevOps R1 检查项 2）。
4. **[低] 单条 ~104s 偏长** — reasoning 模型 + 工具串行，生产 SLA 风险；建议评估换非 reasoning 模型 / 工具并发（KB / link / web 可并发）。

#### 阻塞实现 R1 的高严重度缺陷

无。D-1（真实外部调用连通性）2026-07-01 已有真实冒烟证据（`succeeded` + Tavily 通），单测用 fake 隔离网络的设计取舍合理；当前服务未运行属运维状态问题，不构成代码缺陷。

#### 元信息一致性提醒（不阻塞）

- 本报告 `Review 状态` 表 Architect 行仍标「待Review」，但下方 Review 记录区已有 Architect 2026-07-04 通过结论；`v0.1.md` 实现阶段 R1 表格已同步 Architect 通过。建议 Architect 同步本表 Architect 行状态。
- 本报告 `结论` 段仍写「36 passed」，与「结果摘要」段「40 passed」不一致（S5 后未更新）。建议 Developer 同步。

#### 下一步入口

- DevOps 与 Architect 两方均已通过 → 实现 R1 定稿，进入 Owner 验收 / 部署就绪检查
- 部署就绪检查阶段处理上述 4 条发布检查项（托管化 / 日志 / 多 provider / 耗时）
- Architect 5 条非阻塞观察项按优先级在实现 R2 或下一迭代处理
