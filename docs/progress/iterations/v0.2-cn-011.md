# v0.2 Change Note · CN-011 — 补 `degraded:scores_missing`，判据下沉到维度级

## 基本信息
- 日期：2026-08-02
- 提出角色：**Architect**（承接 `INDEX.md` 跨任务待办「是否补 `degraded:scores_missing`」，归属 PM / Architect，由 Developer 2026-08-02 联调实测登记）
- 关联文档：`v0.2-design.md`（§4.4 出向映射、§8）、coordination `communications/REQ-003`（ai 2026-08-02 帖二、xiaobao `662238a`）
- 关联阶段：实现阶段（R1 已提交，Review 中）
- 变更级别：**轻量变更**
- 当前状态：**两方已确认（PM 无保留 / DevOps 附条件），待 Developer**；DevOps 中① 的 E1 判据补强归其执行，其中「N ≥ 冒烟条数一半」的取值交 PM 定

## 背景

Developer 在联调中实测出一处边界：**LLM 未给出 `scores` 字段时，ai 产出的是「结构完整但四维全 0、reason 全空」，不是结构残缺**——而 xiaobao 补算 tick 原本的判据是「结构残缺则跳过并告警」，**抓不到它**，会把该条当有效的「四维全 0」加权落值，这条新闻以「真的 0 分」的身份排到最后。

成因在 `normalize_output_node`：`dim()` 对每个维度取 `d.get("score", 0)` 兜底，故**结构永远完整**。

对方已按 ai 给的当下可用判别手段落地（`662238a`：跳过「四维全 0 **且** reason 全空」并告警），并明确「你方若后续加 `degraded:scores_missing` 标记，两层判别互为冗余，不冲突」。ai 侧是否加，属我方范围决策，登记待判断。

## 问题描述

### A. 该不该加：加

四条理由，按 Owner 定的「兼容性 / 健全性 / 稳定性」权重排：

1. **兼容性——不构成契约变更**。契约 `news-l1` 对 `tags_v2.processing` 的定义是「处理标签（**如** `engine:agent`）」，是开放集不是闭集枚举；且 `processing` 是数组，追加一个元素不改变结构。对方刚修好「`tags_v2` 只认数组形态」的前端缺陷（`aa7a05a`），数组路径已被正确消费。
2. **健全性——数据层直接可判是唯一可行的检出点**（原写作「优于靠推断」，据 DevOps 确认意见升级为唯一性判断，见下 §Architect 处置）。当前判别要靠「`score=0` 且 `reason` 为空串」这个约定，它成立的前提是「真实打分时 LLM 必给 reason」——**这是一条经验规律，不是被任何机制保证的量**。本迭代已经因为「校验通过但保证不成立」翻过六次车，不该再新增一条同形状的依赖。
3. **稳定性——成本为一行，且与既有机制同源同形**。落点就在 `normalize_output_node` 已有的两行 `processing.extend(f"degraded:{d}" ...)` 旁边，不新增任何机制。
4. **两层冗余是好事不是浪费**：对方按 reason 判、ai 按数据判，任一层失效另一层仍在。对方已明确不冲突。

### B. **但 Developer 报的形态只是特例，真正的盲区在部分缺失**

`dim()` 是**逐维**取默认值的：

```python
scores = parsed.get("scores") or {}
def dim(name): d = scores.get(name) or {}; return ScoreDimension(score=_clamp_score(d.get("score", 0)), reason=str(d.get("reason", "")))
```

即 **LLM 给了 3 个维度、漏了 1 个时，那一维同样是伪 0 + 空 reason**。而：

- 对方已落地的判据是「四维**全** 0 且 reason **全**空」——**部分缺失完全抓不到**；
- ai 建议的 reason 判别手段同样是按「全 0 且全空」表述的，**同样抓不到**。

**所以整体缺失反而是最容易被发现的那一种（四维全 0 极其显眼），部分缺失才是真正的静默降级**：三维有正常分数和 reason、一维是伪 0，加权后只是「某条新闻分数偏低一点」，没有任何一侧会察觉。

**这与 `needs_context` 的 `false` 双来源、`domain_tags` 的 `{}` 形态是同一类**——默认值与有效值混在同一取值里且落在错误一侧——但比它们更隐蔽，因为**它连「异常形态」的外观都没有**。

## 变更判断

- 是否改变产品范围：**否**（不新增能力，是给已产出的信息加一个标记）
- 是否改变主流程：**否**
- 是否改变架构方案：**否**（不动分层、协议、事务边界）
- 是否改变接口/数据契约：**否** —— `processing` 是开放集，追加标记不改结构；**但须知会对方**（见变更 3）
- 是否改变验收/回归：**是** —— 新增一项测试，**收紧**
- 是否改变部署方式：**否**
- **结论：轻量变更**

## 影响范围

- 产品：**正面** —— 该情形在数据层直接可判，不依赖「真实打分必给 reason」这条经验规律
- 架构：§4.4 出向映射补一条降级标记规则
- 实现：`normalize_output_node` 加一处维度级判定 + 一次 `processing.append`
- 自测/验收：新增测试 31（含**部分缺失**用例，这是本 CN 的重点）
- 跨项目：知会一条，不需对方改码

## 处理方式

### 变更 1：判据下沉到维度级

```python
_SCORE_DIMS = ("timeliness", "impact", "confidence", "clarity")
missing = [n for n in _SCORE_DIMS if not isinstance(scores.get(n), dict)]
if missing:
    processing.append("degraded:scores_missing")
    # 缺哪几维进日志，不进标记（见变更 2）
```

**判据用 `not isinstance(..., dict)` 而不是 `not scores.get(n)`**：与 `map_domain_tags` 对 `{}` 的处置同一条纪律——`scores.get(n)` 为 `{}` 时是 falsy 会被蒙对，为 `{"score": 0}` 时是真值却同样缺 reason。**显式类型判定，不依赖真值性**，这正是双方三次踩坑后共同定下的做法（对方 2026-08-02 帖：「建议双方今后对 jsonb 列一律做显式类型判定」）。

### 变更 2：标记保持单段形态，缺失明细进日志

**不写成 `degraded:scores_missing:timeliness`**：既有 `degraded:*` 标记全部是 `degraded:{name}` 单段，对方的消费代码可能按全等或前缀匹配，加第三段有兼容风险，而本 CN 的第一条理由正是兼容性。缺哪几维通过 `obs/logging.py` 已有的 `degradations` 承载，不占契约面。

### 变更 3：知会对方（归 PM，不需其改码）

告知两点：① ai 已加该标记，与其 `662238a` 的 reason 判别互为冗余；② **其判据「四维全 0 且 reason 全空」抓不到部分缺失**，建议其补算 tick 改为**逐维**判别（任一维 `score=0` 且 `reason` 为空即视为该维无效）。这条对其是新信息——ai 上一帖给的判别手段也是按「全 0」表述的，**盲区是 ai 这边先说窄了**。

### 范围纪律（明确不做）

**不改 `_clamp_score` 的默认值行为、不把该情形改判为失败**。产出仍然有效（title / summary / tags / translation 都在），只是评分维度缺失——这正是「部分可用 = `succeeded`」的定义，是 v0.1 已验证的降级语义。加标记是**如实标注**，改判失败是**推翻既有语义**，后者属重大变更且会误伤。

## 需确认角色和理由

| 角色 | 指定理由 | 是否确认 | 确认意见 |
|------|----------|----------|----------|
| PM | 本待办归属写的是 PM / Architect，范围决策须其共同拍板；变更 3 的跨项目知会由其执行；须确认「不改判失败」的范围纪律 | **已确认 · 通过（无保留）**（2026-08-02） | **范围决策：加，PM 侧同意。** 三点确认 + 一条我认领的责任： ① **该加，理由 2 是决定性的那条**——当前判别依赖「真实打分时 LLM 必给 reason」这条**经验规律，不是被任何机制保证的量**。本迭代已因「校验通过但保证不成立」翻过六次车，**不该再新增一条同形状的依赖**。按 §0 准则这属「地基」不属「收割」：成本一行，换掉的是一条会在某次模型换代后静默失效的假设。 ② **B 项（部分缺失才是真正盲区）是本 CN 最有价值的一条，且它扩展了原待办的范围——我同意扩**。原登记只是「是否补 `degraded:scores_missing`」，Architect 追到 `dim()` 是**逐维**兜底，发现漏 1 维与漏 4 维产生完全相同的伪 0。**整体缺失反而最容易发现（四维全 0 极其显眼），部分缺失才真正静默**——三维正常、一维伪 0，加权后只是「分数偏低一点」，两侧都不会察觉。**这比 `needs_context` 的 `false` 双来源更隐蔽，因为它连异常形态的外观都没有。** ③ **「不改判失败」的范围纪律我确认，且这是本 CN 必须守住的边界**：产出仍然有效（title / summary / tags / translation 俱在），只是评分维度缺失——**这正是「部分可用 = `succeeded`」的定义，是 v0.1 已验证的降级语义**。加标记是**如实标注**，改判失败是**推翻既有语义**：会把一条本可正常展示的新闻变成失败、进重试、烧 `attempt`，属重大变更且误伤面大。**不做。** ④ **变更 3 的跨项目知会归我，我认领其中的责任**：需告知对方「其判据抓不到部分缺失」——而**这个盲区是 ai 先说窄了**，我方上一帖给的判别手段同样是按「四维全 0 且 reason 全空」表述的，对方是照我们给的口径落的码（`662238a`）。**去信时要把这一点写在前面，不能让它读起来像是在指出对方的疏漏。** 已执行（见 coordination 回帖）。 ⑤ **变更 2 确认**：标记保持 `degraded:{name}` 单段、缺失明细进日志——既有标记全是单段，对方消费代码可能按全等或前缀匹配，加第三段有兼容风险，而本 CN 第一条理由正是兼容性。**自洽。** |
| Developer | 变更 1/2 由其落地；其为该边界的实测发现方，**维度级判据是对其报告的扩展**，须其确认该扩展与实测一致；测试 31 由其编写 | 待确认 | — |
| DevOps | 该标记会进 `tags_v2.processing` 并出现在其冒烟核对面；须确认不与其已定的部署就绪检查判据冲突 | **已确认 · 通过（附条件 1 中 2 低）**（2026-08-02） | **点名问题的答复：不冲突——但正因为不冲突，暴露出我的判据有个洞，须连带补强。** ① **不冲突**：我已定的 A~F 判据没有任何一条断言 `tags_v2.processing` 的内容，新增标记不会撞任何现有判据；D4/D5/D6 与本 CN 无交集。 **中①（附条件）｜E1 冒烟判据须补两条，否则本 CN 加的标记在冒烟环节会被判据本身吃掉。** 现 E1 只要求「走完 claim → 处理 → 写回 `completed`，留 `run_id`」。而按本 CN 已确认的范围纪律，`scores_missing` **仍是 `succeeded`**（部分可用语义，我同意不改判失败）——于是：**(a) 降级会被判成通过**。8 条全部写回 `completed`、E1 打绿灯，而其中若干条的四维评分是伪 0，判据不会有任何反应。故 E1 须补「**逐条记录并核对 `processing` 中的 `degraded:*` 标记**」，与 D4/D5 已确立的「逐条核对而非只看总数」同一纪律。 **(b) 更要紧的一条：须断言至少 N 条不带 `degraded:scores_missing`。** 若冒烟条目**普遍**带该标记，意味着**四维评分——v0.2 的核心产出——实际上一次都没被真正验证过**，而 E1 照样全绿。**这是「流程跑通」被当成「功能验通」**：claim / 处理 / 写回三段式确实走通了，但被验证的只是管道，不是管道里流的东西。这与本迭代反复出现的那类同型——判据成立，保证不成立。**建议取 N ≥ 冒烟条数的一半**，具体值请 PM 从产品侧定（涉及「多少比例的评分缺失算可接受」，是产品判断不是运维判断）。 **低②｜可观测性已足够，无需额外要求。** 我核实了 `obs/logging.py:88` 的 `degradations` —— 是**结构化字段**（`list[str]` 进 `extra.fields`），不是拼进 message，**灰度期可直接聚合降级率**，本 CN 无需为我再加什么。另注意到该函数的注释写明「`tags_v2.processing` 是唯一承载这些标记的结构化字段，日志再记一份作第二条线索——**写回失败时前者就没了**」：这条对我尤其有用——**写回失败的条目在 xiaobao 库里根本不存在，其降级信息只在 ai 日志里**，故灰度看护必须以日志为准、不能只看对方库。 **低③｜赞成变更 2 保持 `degraded:{name}` 单段形态。** 消费方的匹配方式（全等 or 前缀）不在我方控制内时，**不改变既有形态是唯一稳的做法**——我在 D6 判据上刚吃过同构的亏（同一条判据用不同查法得出相反结论），凡是「对端怎么解析我不知道」的地方，都不该赌。 **另补一条运维视角，印证 B 项（部分缺失才是真盲区）——从监控侧看，它比本 CN 说的还要更不可见。** 整体缺失（四维全 0）**还有救**：它是显著异常值，任何基于阈值或分布的监控都可能抓到；而**部分缺失连异常值都不是**——三维正常、一维伪 0，加权后只是「这条新闻分数偏低一点」，落在正常分布内。**即：无论消费侧部署多少监控，都发现不了它。** 这正是「必须在产生侧标记」的决定性理由——不是产生侧标记更方便，而是**消费侧根本没有任何手段可以发现**。变更 1 的维度级判据（`not isinstance(..., dict)`）因此不是锦上添花，是唯一可行的检出点。 |

## Architect 对 DevOps 附条件的处置（2026-08-02）

| 附条件 | 处置 |
|---|---|
| 中① E1 冒烟判据补两条 | **认可，归 DevOps 执行**（其判据表，我不代改）。(a)「逐条核对 `degraded:*`」与 D4/D5 已有纪律同源，无异议。**(b) 是本轮更要紧的一条，且它指出的是我这个 CN 的射程边界**——我只解决了「让该情形可判」，没解决「冒烟时怎么知道评分真的被验过」。其原话准确：**8 条全绿而四维评分一次都没真正验过，是「流程跑通」被当成「功能验通」**。N 的具体取值涉及「多少比例评分缺失算可接受」，确属产品判断，同意交 PM。 |
| 低② 可观测性已足够 | 确认，无需追加。其顺带核出的一点值得记：**写回失败的条目在 xiaobao 库里根本不存在，其降级信息只在 ai 日志里**——故灰度看护不能只看对方库。 |
| 低③ 保持单段形态 | 与变更 2 一致，无分歧。 |

### DevOps 补的那条运维视角，把本 CN 理由 2 的等级提高了一档

其原话：**部分缺失「连异常值都不是」——三维正常、一维伪 0，加权后落在正常分布内，故无论消费侧部署多少监控都发现不了它。**

我在 §A 理由 2 写的是「数据层直接可判**优于**靠推断」——这是**比较级**，隐含「不加也有别的办法」。按其视角这句话要改成**唯一性**判断：

> **不是产生侧标记更方便，而是消费侧根本不存在任何可行的检出手段。** 整体缺失（四维全 0）还能被阈值或分布监控抓到，部分缺失则连统计异常都不构成。

**故变更 1 的维度级判据不是「锦上添花的加强」，是这个信息的唯一产生点——错过这里，它此后永远不可恢复。** 这条已并入本 CN 的理由，且是我认为最该被记住的一条。

## 未指定角色和理由

无。三方均为受影响方或执行方。

## 执行状态

- [ ] 三方确认
- [ ] 设计文档落地（§4.4 / §8）
- [ ] 实现落地（Developer）
- [ ] 跨项目知会（PM）
- [ ] `v0.2.md` Change Notes 表登记 + `INDEX.md` 待办状态更新
