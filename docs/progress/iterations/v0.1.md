# v0.1 迭代记录

## 概览
- 目标：news-l1 从 stub 做成真实可用的 L1 处理（交付 REQ-001 真实化），架构遵循 REQ-002 调研结论
- 开始时间：2026-06-29
- 关闭时间：—
- 最终状态：进行中
- 类型：标准迭代
- 当前阶段：实现（待 Developer 启动）
- 收尾状态：未收尾

## 阶段门禁

> Bootstrap 初始化、迭代关闭确认和收尾归档不是迭代内的阶段，而是独立的非角色机制。触发时机和执行规则见 `docs/baseline/mechanisms.md`。

### PRD 阶段
| 轮次 | 产出方动作 | 指定 Review 方 | Review 结果 | 阶段状态 |
|------|------------|------------------|-------------|----------|
| R1 | PM 初版（`v0.1-prd.md`） | Architect、Developer | Architect 未通过（需修改：契约冲突与验收语义歧义）；Developer 未通过（需修改：实现边界、降级语义、工具统计与 provider fallback 验收不清） | 已修改→R2 |
| R2 | PM 据 R1 反馈修改（AC-9 内部 registry / AC-6 降级语义 / AC-5 URL+tool_summary / AC-7 配置留设计 / web 搜索 Tavily） | Architect、Developer | Architect 通过；Developer 通过 | 已定稿 |

### 设计阶段
| 轮次 | 产出方动作 | 指定 Review 方 | Review 结果 | 阶段状态 |
|------|------------|------------------|-------------|----------|
| R1 | Architect 初版（`v0.1-design.md` + ADR-0001/0002） | PM、Developer、DevOps | PM 通过（附 2 非阻塞建议：AC-2 标签 prompt 明确、AC-3/4 语言约定）；Developer 通过（附 3 实现提示）；DevOps 通过（附 3 发布检查项） | 已定稿 |

### 实现阶段
| 轮次 | 产出方动作 | base_commit | head_commit | 指定 Review 方 | Review 结果 | 验证 | 阶段状态 |
|------|------------|-------------|-------------|------------------|-------------|------|----------|
| R1 | Developer 分片实现（S1~S5，见下「实现进度」；S5=CN-002 KB 主动检索纳入 v0.1）+ 自测报告 `v0.1-test-report.md` | 2605c07 | 344ad49 | Architect、DevOps（验收/边界/回归复核，§9） | 待Review | pytest 40 passed（设计 §8 全 10 项 + KB 主动检索 + 四维，0 失败 0 阻塞；真实外调连通性待部署冒烟 D-1） | Review中 |

> 说明：S1~S4 是实现阶段 R1 的**内部开发切片**（承接 O-6，由 Developer 定），代码逐片累积 commit；四片已完成、自测报告齐备，实现阶段 R1 提交 Architect/DevOps 复核（验收/边界/回归 + 部署配置），现置「Review中」。

#### 实现进度（R1 内部切片）
| 切片 | 内容 | commit | 自测 | 状态 |
|------|------|--------|------|------|
| S1 骨架真实化 | 内部 registry/dispatch + 条件图骨架（ingest_context→llm_process→normalize_output）+ LLM client 依赖注入 + tool_summary 旧口径修正 + AC-6 降级/失败语义 | `f8de726` | pytest 9 passed（设计 §8 测试 1/2/3/7/8 部分） | 已完成 |
| S2 LLM client | `ChainedAIClient` 多 provider fallback 矩阵 + JSON 修复 + normalize（score clamp / context URL 引用校验）+ provider 配置解析 + prompt 详化 | `7535949` | pytest 30 passed（设计 §8 测试 6/9/10 + fallback 矩阵） | 已完成 |
| S3 工具真实化 | `tools/{link_reader,web_search,kb}.py`（Tavily 直连）+ 线性图扩为条件图 + 上下文充分性/预算路由启发式 + 工具注入 | `afad1aa` | pytest 36 passed（设计 §8 测试 4/5/8） | 已完成 |
| S4 收尾 | `.env.example` 补多 provider/Tavily 配置 + 自测报告 `v0.1-test-report.md` + 全量回归 | `0863c6a` | pytest 36 passed（全量回归 + 导入冒烟） | 已完成 |
| S5 KB 接入（CN-002） | `tools/kb.py` 真实调 xiaobao `POST /v1/kb-search` + graph 主动 KB 路由（优先级 kb>link>web）+ `.env.example` KB 配置；主动 KB 检索纳入 v0.1 | `344ad49` | pytest 40 passed（KB 触发/未配置/预取不触发/失败降级） | 已完成 |

### 部署就绪检查
| 状态 | 目标环境 | 证据 | 阻塞项 |
|------|----------|------|--------|
| news-l1 主链路就绪（部分通过） | 测试环境 `127.0.0.1:8100`（当前机器 uvicorn 常驻；LLM=openclaw 火山 `doubao-seed-2.0-pro`） | `/health` 200；真实 news-l1 火山处理 `succeeded`（run `run_bcf24393b947`，四维评分/五类标签/中文摘要/分析真实产出）；`web_search` 真实调 Tavily 通；KB 真实调 xiaobao 8001 降级（未起→`degraded:kb_search_failed`，整体仍 succeeded） | ① KB 端到端待 xiaobao 起 8001 + 定 `KB_ADMIN_TOKEN` ② 单条约 104s 偏长（reasoning 模型+工具串行）待观察/调模型 ③ 未启用鉴权（测试环境约定） |

## Review 记录

### PRD R1 — Architect Review（2026-06-30）

- 结论：未通过（需 PM 修改后进入下一轮 Review）
- 核心问题：
  1. AC-9 / 范围边界涉及通用入口 `/v1/runs/{task_type}`、caller 标识，与 AC-8「本迭代不改契约」冲突。
  2. AC-6 “失败时返回部分可用结果”与 coordination `news-l1` v1 中 `failed => output=null` 的响应语义冲突。
  3. AC-5 链接自抓缺少 URL 来源约定；`tool_summary.kb_search` 需区分预取消费与 ai 主动工具调用。
- 建议：PM 修改 PRD，明确 AC-9 是内部薄接缝还是正式契约变更；将降级语义收敛到现有 v1 契约；补充 URL 来源和工具统计口径。

### PRD R1 — Developer Review（2026-06-30）

- 结论：未通过（需 PM 修改后进入下一轮 Review）
- 核心问题：
  1. AC-9 若新增通用路由或 caller 字段，会触发 API / schema / xiaobao 联调；若不改契约，应收敛为内部 registry。
  2. AC-6 未明确“部分可用结果”在 `RunResponse.status` 中算 `succeeded` 还是 `failed`，导致实现和测试无法稳定断言。
  3. AC-5 链接自抓缺少 URL 输入 key；`tool_summary` 需区分预取上下文消费与 ai 主动工具调用。
  4. AC-7 多 provider fallback 缺少最小配置形状和失败判定矩阵。
- 建议：PM 修改 PRD，给出可测试的状态语义、输入来源、统计口径和 AC-9 契约边界；provider 配置细节可要求 Architect 在设计阶段落定。

### PRD R2 — Developer Review（2026-06-30）

- 结论：通过
- 确认：
  1. AC-9 已收敛为内部 registry / dispatch，对外仍保持 `POST /v1/runs/news-l1`，不触发契约变更。
  2. AC-6 已明确部分可用与完全失败的 `RunResponse.status` 语义，可写稳定测试。
  3. AC-5 已明确 URL 来源 key 与 `tool_summary` 只统计 ai 主动工具调用。
  4. AC-7 已把 provider 配置形状、重试顺序、失败判定矩阵和 timeout 继承交给设计阶段落定，足够进入 PRD 定稿判断。
- 实现提示：现有骨架测试中预取 `kb_results` 计入 `tool_summary.kb_search` 的断言，需在实现阶段按 R2 PRD 新口径调整。

### PRD R2 — Architect Review（2026-06-30）

- 结论：通过，PRD 阶段定稿，可进入设计阶段。
- 确认：
  1. AC-9 已收敛为内部 registry / dispatch，对外契约仍为 `POST /v1/runs/news-l1`，不新增 caller/task_type 字段，避免跨项目契约变更。
  2. AC-6 已对齐 `RunResponse` v1：部分可用为 `succeeded`，完全不可用才 `failed + output=null + error`。
  3. AC-5 已明确 URL 来源 key 与 `tool_summary` 只统计 ai 主动工具调用，预取上下文不计入调用次数。
  4. AC-7 的 provider 配置结构、失败矩阵和 timeout 继承留给 Architect 设计阶段落定，作为 PRD 门禁已足够。
- 设计阶段注意：需落条件图编排与 LLM client 移植 ADR，并在设计文档中明确 provider fallback、`needs_context`、`tool_summary` 和内部 task registry 边界。

### 设计 R1 — Developer Review（2026-07-01）

- 结论：通过（设计可落地，附实现提示，不阻塞定稿）
- 确认：
  1. 模块边界清楚：`main.py` 保持 HTTP 契约，`tasks.py` 承接内部 registry，`llm/*` / `tools/*` / `graphs/news_l1.py` 分工可实施。
  2. 状态模型和接口足以支撑 TDD：`TaskSpec`、`L1State`、`ContextItem`、`ProviderConfig`、`StepError` 覆盖 registry、上下文、预算、降级和 fallback。
  3. PRD R1/R2 中 Developer 关注点已承接：AC-9 契约不变、AC-6 降级状态、AC-5 主动工具计数、AC-7 fallback 矩阵均有设计落点。
  4. 测试清单覆盖实现阶段主要风险。
- 实现提示：实现阶段需补清 `LLMResult` 最小字段、显式维护总 timeout budget，并先修正现有 stub 测试里 `kb_results` 计入 `tool_summary.kb_search` 的旧口径。

### 设计 R1 — DevOps Review（2026-07-01）

- 结论：通过（provider 配置、Tavily key、密钥注入、超时和部署环境影响均有设计落点；附 3 条发布检查项，不阻塞定稿）
- 确认：
  1. `LLM_PROVIDERS_JSON` 多 provider 配置与旧 `OPENAI_BASE_URL` / `OPENAI_API_KEY` / `L1_LLM_MODEL` 单 provider 兼容路径清楚。
  2. provider 配置使用 `api_key_env`，Tavily key 使用 `TAVILY_API_KEY` 注入，密钥不入库。
  3. 生产至少 2 个 provider、fallback 判定矩阵和总 timeout budget 已有设计约束，可支撑上线前验证。
  4. Tavily 未配置时可降级，不应阻断服务启动或主流程。
- 发布检查项：实现阶段补 `.env.example` 的 `LLM_PROVIDERS_JSON` / backup key 示例 / `TAVILY_API_KEY`；部署阶段验证生产至少 2 个 provider；冒烟覆盖 `/health`、脱敏 `news-l1` 请求、provider fallback、Tavily 未配置降级和日志脱敏。

## Change Notes

| Change Note | 状态 | 变更级别 | 受影响角色 | 执行状态 | 归档状态 |
|-------------|------|----------|------------|----------|----------|
| CN-001 · news-l1 工具后端分工细化（web 搜索 ai 直连 Tavily、key 取自 openclaw 配 `.env`；KB 选方案 b 实时接口） | 待确认 | 轻量（不改 AC / 对外契约 / v0.1 范围；KB 主动检索仍后补） | Architect、Developer | PRD 前置依赖 + O-3 已补（带 [CN-001] 引用） | 未归档 |
| CN-002 · KB 主动检索**纳入 v0.1**（原 AC-5「待 xiaobao 接口落地后补」→ 落地）：xiaobao 已提供 `POST /v1/kb-search`（契约 coordination `contracts/kb-search.md` v1），ai 实现主动库内检索（提炼 query→调 xiaobao→计入 `tool_summary.kb_search`） | Owner 2026-07-01 批准纳入 | 轻量（新增能力，**不改对外 news-l1 v1 契约**；依赖已定稿 kb-search v1；实现为 S5） | Architect、Developer、DevOps | 实现 S5（`tools/kb.py` 真实化 + graph 主动 KB 路由 + config KB 项） | 未归档 |

## Git 关键节点

| 节点 | commit | 说明 |
|------|--------|------|
| PRD R1 起草 | （本次收尾提交） | v0.1 PRD 初版 + 迭代记录建立 |

## 迭代关闭归档
- Owner 验收（真源记录，未验收 / 打回则不得关闭）：未验收
- Developer 自测结论（证据齐备）：—
- 关闭结论：—
- 未关闭原因或延期项：—
- 已生成 summary：—
- 已更新 `docs/progress/INDEX.md`：—
- 已更新当前角色日志：—
- 其他角色待补充/确认：—
- 已更新知识库：—
- 遗留问题下一步：—
