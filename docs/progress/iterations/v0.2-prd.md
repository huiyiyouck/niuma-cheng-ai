# v0.2 PRD — 承接 REQ-003：数据库边界异步解耦（双模式）

## 文档状态
- 整体状态：待Review
- Review 轮次：**R3**（2026-07-27，按 xiaobao 三方答复收敛：契约升至 v1.3 + 3 列 GRANT 已执行，原 4 条阻塞定稿项全部闭合、所有 `[待 C-xx]` 标记解除；R2 未经 Review 即被外部事实推进，故直接出 R3）
- 关联迭代：v0.2

## Review 计划
- Review 模式：动态 Review
- 影响领域：产品范围/验收标准（PM 产出，不自审）、架构/接口/适配层分层（Architect）、工程成本/可实现性（Developer）、部署形态与凭据注入（DevOps）
- 本轮（R3）指定 Review 方：**Architect、Developer、DevOps 三方复审**
  - 三方在 R1 均判未通过，且本轮的核心变更（async 地基改造进范围）**直接推翻了 Developer R1 问题 2 的推荐解法**，必须三方复审，不可减方。
- 定稿条件：三方全部通过，且无未处理的阻塞级问题。**原 4 条阻塞定稿的契约缺项（C-2/C-3/C-5/C-10）已于 2026-07-27 全部闭合**，不再构成定稿障碍；剩余 C-6（ai 侧实证）与 Q-1（xiaobao PM 表态）均不阻塞定稿。
- R1→R2 的 Review 计划变化：无减方。R1 的定稿条件「O-1 必须先有结论」已解除（O-1 于 2026-07-25 定案方案 A，见事实刷新第 1 项）。

## Review 状态
| Review 方 | 指定原因 | 状态 | 轮次 |
|-----------|----------|------|------|
| Architect | 适配层协议设计 / worker 编排接缝 / async 地基改造的架构影响 / 事务边界；**并复核 C-3 结论反转（UPDATE 占位行）对设计的影响** | 待 Review | R3 |
| Developer | 工程成本（async 改造后重估）/ claim 与写回实现 / 验收可验证性；**并复核 3 处被推翻判断的收敛是否到位** | 未通过（2026-07-27，3 高 2 中 2 低；R1 六条已全部收敛，新问题均来自 async 新范围） | R3 |
| DevOps | 运行形态 / 探活与优雅停机 / 凭据注入 / async 后的部署形态；**并接手 C-6 行锁实证的执行安排** | 未通过（2026-07-27，3 高 2 中；R1 七条已全部收敛，新问题集中在部署环境缺失与跨层/双侧协同） | R3 |

---

## 0. 本迭代的决策准则（Owner 2026-07-26 定，优先于成本考量）

> 本节是 R2 新增，且**优先级高于本文档其余各节的取舍**。R1 三方 Review 中凡以「成本最低」为由给出的推荐解法，均按本准则重新裁定。

**以基础夯实、可扩展性强的方式开发，而不是图便捷。宁可现在麻烦一点、活多一点，也要让系统更健全、后期接入更友好。**

依据：ai 是**持续迭代**的产品。基础越牢，后续能扩展的功能越多；每次选「当下最方便」，都是给未来迭代加税，且债会在形态变化时集中爆发——本迭代的 async 问题正是此类（见 §2 US-8）。

本准则在 R2 中产生的三处具体裁定：

| # | R1 的成本最优解 | R2 按准则裁定 | 依据 |
|---|----------------|--------------|------|
| 1 | worker 跑独立线程绕开同步阻塞，处理核心零改造（Developer 问题 2 推荐①） | **改为 async 地基改造**（US-8 / AC-9） | 线程方案是「绕过问题」而非「解决问题」；同步阻塞是既有架构债，v0.3 的工具并发与批内并发必然要还，届时 worker 要推倒重来；线程与后续 async 混用会更难收拾 |
| 2 | 适配层 = 两个映射函数（入向/出向），成本几乎为零（Architect O-2 倾向、Developer 确认） | **改为定义数据源协议**（AC-2） | 映射函数方案下，第二个调用方接入要回来改我们的代码；协议方案下只需实现协议。`decisions/0002` 的多调用方定位要求后者 |
| 3 | `tags_v2` 第五类二选一（Developer 问题 1） | **主张 `sentiment` 与 `processing` 两个语义都保留**（C-10） | 二者不是同一种东西被塞进同一字段位：`sentiment` 是业务标签，`processing` 是降级/provider 的唯一结构化线索。丢任一都会削弱系统可诊断性 |

---

## 1. 迭代目标

承接 coordination `REQ-003`：把 ai 的 news-l1 集成模式从「HTTP 同步调用」**扩展**为「数据库契约边界异步解耦」——xiaobao 入库标记状态，ai 轮询 claim 取数、处理、写回共享库。

四条硬约束：

1. **并行而非替换**：HTTP 模式（`news-l1` v1 契约）继续有效，供灰度与回滚使用；新增 DB 模式（`news-l1-db` v1.1 契约）。
2. **数据源协议隔离**：取数与写回封装为**独立的数据源协议**（而非内联映射代码），AI 处理核心不感知数据来源。这是 `decisions/0002`（多调用方预留）的硬要求——ai 直连 xiaobao 库读 schema 会把 ai 焊死在 xiaobao 上。**工程成本提示**：v0.1 的 `run_task(task_type, run_id, inp, client, tools)`（`tasks.py:55`）已是与传输层无关的签名、`main.py:34` 只调用它一行，因此「处理核心不感知数据来源」几乎是现成的（Developer R1 确认）——本约束的实现风险低，重点在**协议要定义得足够正**，而非改造量大。
3. **schema 权属不越界**：共享库 schema 归 xiaobao，ai 只读指定表列、只写指定结果字段，不建表不改表。此约束已在数据库层被强制（xiaobao DevOps 2026-07-25 越权实测：`SELECT alerts` / `INSERT raw_items` / `UPDATE process_type` 均 `permission denied`）。
4. **async 地基先行**：本迭代把 IO 路径从同步改为 async（§0 准则裁定 1），作为 worker 模式与后续并发能力的地基。

**范围重排说明**：v0.2 原范围为稳定性六项，起草于 2026-07-04，比 REQ-003 提报（2026-07-05）早一天，故未包含 REQ-003。Owner 2026-07-25 拍板重排：REQ-003 为 v0.2 主线，与 worker 形态耦合的三项（托管化 / 工具并发 / RunRecord）顺延 v0.3；logging 与 KB 空结果语义并入本迭代。R2 在此基础上按 §0 准则追加 async 地基改造。

## 2. 用户故事

- **US-1**（调用方解耦）作为 xiaobao，我想入库并标记状态后立即返回、不必挂起等待单条 74~79 秒的 AI 处理，以便抓取与展示流程不被 AI 处理阻塞。
- **US-2**（多调用方定位）作为 ai Owner，我想取数/写回通过一个明确定义的数据源协议接入，以便将来新增调用方时只需实现该协议、无需改动处理核心，也不让 ai 被 xiaobao 的库 schema 绑死。
- **US-3**（worker 闭环）作为 xiaobao，我想 ai 能定期自动领取待处理的 AI 类新闻、处理完把结果写回共享库，以便无需我主动调用即完成 L1 处理。
- **US-4**（灰度与回滚）作为 Owner，我想用配置开关在 HTTP 模式与 DB 模式之间切换，以便灰度验证期间出问题可立即切回 HTTP 模式。
- **US-5**（失败与卡死可控）作为 xiaobao，我想 ai 处理失败时按契约正确标记状态与尝试次数、按契约退避后才重试、卡死时我能按契约回收，以便队列不被永久占用、下游故障有恢复窗口。
- **US-6**（可观测性）作为 ai Owner，我想看到脱敏的结构化日志，以便在 worker 模式下定位问题——worker 模式没有 HTTP 响应可看，日志是主要观测面。
- **US-7**（KB 语义修正）作为 xiaobao，当工具检索返回空结果时，ai 应标记「无匹配」而非「检索故障」，以便区分两种情况（v0.1 遗留 D-3）。
- **US-8**（异步地基）作为 ai Owner，我想服务的 IO 路径全面改为 async，以便 worker 在处理长任务时仍能响应探活、且后续的工具并发与批内并发不需要重做架构。
  > 背景：v0.1 代码全同步（`grep "async def\|await" src/` 0 命中），LLM 与三个工具均为同步 `httpx`。FastAPI 本身是 async 框架，同步端点靠线程池兜底，HTTP 模式下侥幸无碍；worker 模式自行控制循环后该保护消失。这是**既有架构债在新形态下暴露**，且 v0.3 的工具并发化必然要还。

## 3. 验收标准

> 每条覆盖正常 + 至少一个异常路径，可验证。PM 不自审本节，由 Architect / Developer / DevOps 审。
> **R3 说明**：契约已由 xiaobao 连升 **v1.2 → v1.3**（DevOps 已执行 3 列 GRANT，待升 v1.4）。原 R2 中所有 `[待 C-xx]` 标记**已全部解除**——10 条契约缺项闭合 9 条、1 条（Q-1）待 xiaobao PM 表态且不阻塞。本节据实收敛为可实现、可验证的确定条款。

- **AC-1 双模式开关**：当配置为 HTTP 模式时，`POST /v1/runs/news-l1` 行为与 v0.1 等价、不启动轮询；当配置为 DB 模式时，worker 按配置间隔轮询共享库；当配置缺失或非法时，应以 HTTP 模式启动并记录警告，不得静默进入 DB 模式。切换模式只需改配置并重启，不需改代码。开关粒度为**进程级**。

- **AC-2 数据源协议隔离**（判据：**依赖方向**，不比对输出内容）：
  1. 应存在一个显式的数据源协议（取批 / 写回成功 / 标记失败 / 释放锁四类操作），DB 数据源与 HTTP 数据源均为该协议的实现；处理核心只依赖协议，不出现共享库表名、列名或 `raw_items`/`tasks` 等概念。
  2. **在 async 地基改造完成后的代码基线上**，切换数据源实现时，处理核心（`tasks.py` / `graphs/` / `llm/`）代码 diff 为空。
     > async 改造本身会修改处理核心，那是**地基变更**，不计入本条；本条约束的是「不因数据来源变化而改核心」。
  3. 两种数据源均应能产出结构合法的 `L1Output`（结构合法性判定，不比对字段内容——内容等价性归 AC-8）。
  4. **入向映射必须回填 `raw_content` 的 URL 键**：`_extract_url`（`news_l1.py:407`）只认 `raw_content["url"]` / `["canonical_url"]`；若把 `raw_items.content` 原样塞入 `L1Input.raw_content`，`_should_link_read` 恒为 False，**link_read 不报错、不降级、静默失效**（最难归因的一类问题）。
     **URL 取值来源（R3 订正）**：直接读 **`raw_items.source_item_url` 列**（契约 v1.3 已 GRANT SELECT，`x_twitter` 与 `rss` 均有值），**三类 source type 统一**。
     > 原 R2 写的「从 `tweet_id` + `author_username` 构造 `x.com/...`」**已作废**——xiaobao Architect 2026-07-27 指出该列一直存在，只是此前未 GRANT；R-5 字段表只描述了 `content` jsonb 的键，故未体现。构造方案会与 xiaobao 的实际写入值产生潜在不一致，弃用。
     验证方式：DB 模式下构造一条 `source_item_url` 非空的输入，断言 link_read 被触发；再构造一条该列为空的输入，断言不触发且不报错、不降级。
  5. 映射失败的错误归类：入向映射失败（必填缺失/类型不符）归 `client_error`、**不重试**直接置最终失败；出向映射失败归 `server_error`、可重试。

- **AC-3 claim 正确性**：
  1. **claim 以 `tasks` 表为准，不扫 `raw_items`**（契约 v1.3 claim 规则）。条件须包含 `t.type='l1_ai_process' AND t.status='queued' AND t.run_after <= now()`。
     > **`run_after` 条件不可省**——缺它则退避完全失效（见 AC-5.2）。
     > **只 claim `tasks` 是安全的**（xiaobao Architect 确认）：`raw_items` 入库时 `l1_status` 默认为 `not_started`、此时尚无 task，这类条目本就不该被领；ai 侧**不需要**增加孤儿条目探测逻辑。
  2. claim 时同事务写入：`tasks.status='running'` + `attempt = attempt + 1` + `locked_by` + `locked_at`，`raw_items.l1_status='processing'`（后者是 xiaobao 卡死回收的判定依据，必须写）。
  3. 无待处理条目时 worker 应空转等待而非报错退出。
  4. **并发安全**：同一条目已被其他 worker 领取时不得重复领取。
  5. **批量上限不变式（正确性约束，非调优建议）**：单批 claim 数 N 必须满足「批内最长完成时间 < xiaobao 卡死阈值 1800s」。按 v0.1 实测单条 74~79s 且批内串行计，N ≤ 22 为绝对上限，**v0.2 取 N ≤ 8**。违反会直接产生「xiaobao 误回收 + ai 仍在处理」的双写竞态。验证方式：配置校验中拒绝超限取值。
  6. **claim 原子性的可实现性须实证，不得假定**（C-6）：PG 对 `SELECT ... FOR UPDATE` 的权限检查按表级 UPDATE 判定，而契约给的是列级 UPDATE。实现前须以 `ai_worker` 身份在 `news_test` 实际执行一次 claim SQL，断言可执行且并发不重复。
     > xiaobao Architect 预判「列级授权很可能不满足」；若实测失败，xiaobao 将对 `tasks` 改授表级 UPDATE（其表内无敏感列，已同意）。**实证结论须回帖 coordination。**

- **AC-4 写回正确性**：
  1. **写入方式：UPDATE 占位行**（R3 订正，推翻 R2 的「ai INSERT」推断）。占位 `processed_news` 由 xiaobao 在 L0 通过时创建，是其产品硬约束——L0 通过后新闻须立即在前台可见（原文标题 + 摘要 + 「待解析」角标），其列表查询为 `FROM processed_news JOIN news_positions`，无占位行则新闻凭空消失。
     **实现方式**：用 `INSERT ... ON CONFLICT (raw_item_id) DO UPDATE`（`raw_item_id` 已有唯一约束，`schema.ts:202-205`），兼容占位行因故缺失的极端情况；ai 的 INSERT 权限保留不撤。
     > R2 曾担心「触发器在占位时按空分值排一次且无更新路径」——**该担心不成立**：`news_positions` 只存 `(news_id, position_id)` 关联、不存排序键，列表排序是查询时实时计算。
  2. **`id` 列不得显式写入**：`processed_news.id` 为 `uuid PRIMARY KEY DEFAULT gen_random_uuid()`，由数据库生成。
  3. 写回应为**单事务**（`processed_news` + `raw_items.l1_*` + `tasks` 三者同一事务），中途失败不得留下「结果已写但状态未推进」的不一致。**LLM 调用必须在事务之外**——74~79s 绝不能持有数据库事务。即：claim 短事务 → 处理无事务 → 写回短事务，共三段。
  4. 成功时同事务写：`tasks.status='succeeded'`、`raw_items.l1_status='completed'` + `l1_processed_at=now()`，并释放锁。
  5. ai 不得对 `raw_items` 写契约未授权的列、不得建表改表（数据库层已强制，可引用 xiaobao 2026-07-25 越权实测）。
  6. **写入字段逐条（R3 全部确定，无待定项）**：

     | 字段 | 取值 | 状态 |
     |---|---|---|
     | `raw_item_id` / `title` / `summary` / `translation` / `analysis` / `score_dimensions` / `context` | `L1Output` 对应字段 | ✅ |
     | `tags_v2` | `{domain, entity, event, content_type, **processing**}`——与 `news-l1` HTTP v1 完全一致 | ✅ C-10 定案（`sentiment` 系起草笔误，PM 裁定本阶段不引入，契约 v1.2 已订正） |
     | `language` | **固定 `'zh'`**（产出内容语种；news-l1 输出契约即中文输出，无需语种检测） | ✅ C-7 定案 |
     | **`published_at`** | **取 `raw_items.published_at` 一并写入**（R3 新增） | ✅ xiaobao 占位行当前写死 NULL 导致待解析新闻沉底；ai 对 `processed_news` 有全列写权限，写入作双保险 |
     | `analysis` 为 None 时 | 写 **NULL**（不写空串） | ✅ Q-3 双方确认（xiaobao 前端为真值判断，NULL 语义更准） |
     | `score_total` | **不写**（归 xiaobao 加权） | ✅ O-1 定案；但见 AC-8.2 的已知现象 |
     | `id` | 不写，DB 生成 | ✅ |

- **AC-5 失败、退避与重试语义**：
  1. **重试上限读 `tasks.max_attempts` 列，禁止硬编码 3**（C-8 定案；该列建任务时按 xiaobao 的 `AI_MAX_RETRIES` 写入，硬编码会与 env 配置形成双真源）。判定用 `tasks.attempt` 对比 `tasks.max_attempts`；`raw_items.l1_attempt` 为镜像值、同事务一并推进。
  2. **退避真源是 `tasks.run_after`，禁止自行用 `updated_at + backoff(attempt)` 计算**（R3 订正，推翻 R2 写法）。
     > 根因订正：R2 认为「契约 claim SQL 缺时间条件导致退避失效」，**实际根因是 xiaobao 的 GRANT 少给了 `run_after` 列的 UPDATE 权限**（xiaobao Architect 2026-07-27 认领，DevOps 已补 GRANT，test + prod 双库）。xiaobao 的退避机制本就存在（`dispatcher.ts:122-127`）。若 ai 自行计算，会与其 `requeueTask` 形成两套退避真源，卡死回收介入后必然漂移。
     **实现**：可重试失败时写 `tasks.status='queued'` + `run_after = now() + backoff(attempt)`（backoff 取 `[60s, 300s, 900s]`），claim 时按 `run_after <= now()` 过滤。
  3. 可重试失败（未达上限）：同事务写 `tasks.status='queued'` + `run_after`、`raw_items.l1_status='retryable_failed'` + 脱敏 `l1_error`、`tasks.last_error_kind`，递增计数并释放锁。
  4. 最终失败（达上限）：写 `tasks.status='failed'`、`raw_items.l1_status='final_failed'` + `l1_error` + `l1_processed_at`。
  5. 验证方式：构造一条失败条目，断言在退避窗口内不被重领、窗口后被重领；断言达 `max_attempts` 后进 `final_failed` 且不再被领。
  6. **残留锁自愈口径**：ai 仅可在启动时回收 `locked_by` 等于**本 worker 标识**的残留 `processing` 条目（属自愈，不越权）；其余一律交 xiaobao 的 1800s 回收，ai 不回收他人锁。`locked_by` 标识规则在设计阶段定（O-10）。
  7. **优雅停机**：收到 SIGTERM 时停止新 claim、处理完当前条目后释放锁再退出；超出宽限期才强杀并退回卡死回收路径。**宽限期上限须小于 1800s**。验证方式：处理进行中发送 SIGTERM，断言锁被释放且状态未停留在 `processing`。

- **AC-6 结构化 logging**：
  1. 服务/worker 启动时初始化统一结构化日志格式；各处理步骤记录 run 标识、`raw_item_id`、`step`、`duration_ms`、`status`；错误时记录 `error_kind`（`client_error` / `server_error` / `timeout` / `rate_limited` / `auth_error` / `db_error` / `mapping_error`）与脱敏 `error_message`。
  2. **数据库口令脱敏须可验证**：口令最常见的泄漏路径是**连接失败时驱动异常带出 DSN**。数据库连接/查询异常必须在记录前重写为不含连接串与口令的消息。验证方式：以错误口令发起连接，断言日志中不出现口令子串。
  3. 日志统一输出到 **stdout**（由托管层收集）；v0.2 灰度期由 DevOps 指定落盘路径并人工轮转，不在应用内实现轮转。
  4. **logging 须以注入方式接入**（随 state 或依赖注入传入），不得在 `graphs/` 内直接 import 全局 logger——否则与 AC-2 产生耦合，且不可测试、不可替换。
  5. 日志不得包含 API key、数据库口令、完整 `raw_content`。
  6. 降级与 provider 事实须在日志中冗余一份（不只依赖 `tags.processing` 承载）。

- **AC-7 工具「空结果」与「调用故障」语义统一**（v0.1 遗留 D-3，扩为三工具）：
  1. KB 检索、Web 搜索、链接读取任一工具**调用成功但无结果**时，标记为「空结果」——**不进 `degradations`**（不产生 `degraded:` 标签），只在日志与 `tool_summary` 中体现；`tool_summary` 维持「发起即计数」口径不变。
  2. 工具返回非 200、超时或连接错误时，才标记为调用故障并进 `degradations`。
  3. **缺陷位置**：不在 `tools/kb.py`（`kb.py:72` 返回 `ToolResult(ok=True, items=[])` 已是正确语义），而在 graph 层 `news_l1.py:150`（link_read）/ `:179`（web_search）/ `:213`（kb_search）三处同构判断 `if result.ok and result.items:`——空 items 落进 else 被记为故障。三处须一并修。
  4. 原「`tags.processing` 不应包含 `stub`」条款已删除（`stub` 在 `src/` 中 0 命中，`tests/test_news_l1.py:186` 已有断言），改为「保持既有断言不回归」。

- **AC-8 输出语义一致性 + 已知差异清单**（判据：**在输入等价的前提下**字段语义一致）：
  1. 在输入等价的前提下，DB 模式写回的业务字段语义应与 `news-l1` HTTP v1 契约一致。
  2. **DB 模式的已知差异（显式列出，联调时不按 bug 处理）**：
     - **`domain_tags` 改为单值来源，仍不完全等价**（R3 更新）：契约 v1.3 已 GRANT `raw_items.l0_label`（SELECT），故不再恒空；但 `l0_label` 是**单值 varchar**，而 HTTP 模式 `domain_tags` 是**数组**。适配层按单值处理（`[l0_label]`），语义近似但非等价。
     - **`score_total` 在 DB 模式将保持 NULL**（R3 新增，xiaobao 主动上报）：其 `calcScoreTotal` 只挂在 HTTP/内建 L1 路径（`l1-processor.ts:171`），**database 模式没有任何触发点**。后果是 xiaobao 列表排序回退到 `COALESCE(score_total, importance_score)`、占位行 `importance_score=0` → 这些新闻按分排序沉底、前端评分徽章显示 0。**这不是 ai 的缺陷**，联调时观察到该现象不应归因于 ai；xiaobao 已上报其 PM 决策补触发点。
     - 预取上下文（`kb_results` / `link_content` / `search_summary`）在 DB 模式无提供方 → KB 由「调用方预取」转为「ai 主动检索」，`tool_summary.kb_search` 计数必然从 0 变为常态 ≥1。
     - **DB 模式并未消除 ai → xiaobao 的 HTTP 依赖，只是反转了方向**：KB 检索的唯一通道仍是回调 xiaobao `POST /v1/kb-search`。xiaobao HTTP 不可用时，DB 模式退化为无 KB 上下文。
     - `context` 大概率恒为 `[]`（证据更少 + `normalize_output_node` 的防编造过滤）。**xiaobao PM 已明确知悉并接受**（其前端为条件渲染，空数组零 UI 副作用），承诺不作为质量问题回流，且不要求 ai 改过滤策略。
     - `needs_context` 丢失（`processed_news` 无列）——**待 xiaobao PM 表态**（Q-1，其 Architect 已倾向补列且成本极低）。若补列则 ai 一并写入；若不补则维持丢弃并写入已知限制。
  3. 本迭代不改 HTTP 对外契约。

- **AC-9 async 地基与探活**（§0 准则裁定 1）：
  1. IO 路径全面改为 async：四个出网调用点（`llm/client.py:176`、`tools/kb.py:50`、`tools/web_search.py:24`、`tools/link_reader.py:38`）由 `httpx` 同步接口改为 `AsyncClient`，沿调用链改造至 `tasks.run_task` 与 `main.py` 端点；LangGraph 节点使用 async 形态。
  2. **DB 驱动须与 async 地基一致**（选型归 Architect）——不得出现「同步驱动 + async 核心」的混合形态。
  3. **探活可用性（可验证）**：DB 模式下**单条处理进行中（≥60s）时，`/health` 仍须在 2s 内返回**，响应体含 `mode` 与 `last_poll_at`。DB 模式的 `/health` 只做存活探针，不注册 `POST /v1/runs/news-l1` 路由。
     > 这条一次性验死或验活「进程级 + DB 模式仍监听 `/health`」的组合：若 async 改造不彻底导致 event loop 被阻塞，探针会按超时判死 → 重启 → 制造残留锁 → 每条延迟 30 分钟等回收，比没有探活更糟。
  4. **回归要求**：v0.1 已验证的处理逻辑（四维评分/标签/摘要/翻译）在 async 改造后行为不变，现有 40 例单测全部通过（框架层允许改造为 `pytest-asyncio`，断言语义不得放宽）。

- **AC-10 三类 source type 的实现与验收分层**：
  1. `x_twitter` / `rss` / `jin10_flash` **三类映射均实现**（按 R-5 结构说明 + 缺失兜底），避免将来接入时再动适配层。URL 三类统一读 `source_item_url` 列（见 AC-2.4）。
  2. **验收分层**（系统当前只有 `x_twitter` 真实数据；xiaobao PM 已确认 **rss / jin10_flash 近期不接入 AI 处理链路**）：

     | source type | 验收要求 |
     |---|---|
     | `x_twitter` | **真实数据端到端通过**（claim → 处理 → 写回 `completed`），用 `news_test` 已预置的 5 条 `queued` 冒烟 |
     | `rss` | 仅单测覆盖（构造符合 R-5 结构的 fixture），标注「待接入 AI 链路后补真实验收」 |
     | `jin10_flash` | 同 `rss`（生产虽有 `direct` 类历史数据，但不走 AI 链路） |
  3. **不得声称三类均已真实验收通过**——无真实数据即为未真实验收。
  4. 两类将来接入 AI 链路时，由 xiaobao 提报新联调诉求（双方已确认，不留隐性预期）。

## 4. 范围边界

**本迭代做：**

- **async 地基改造**（IO 路径全面异步化，US-8 / AC-9）。
- 数据源协议 + DB 数据源实现（入向/出向映射，三类 source type）。
- 轮询 worker：claim → 处理 → 写回 → 释放锁完整闭环，含批量上限、轮询间隔、退避判定、优雅停机、启动自愈残留锁。
- `news-l1-db` v1.1 契约落地。
- 双模式配置开关（进程级）+ DB 模式 `/health` 探活。
- 结构化 logging（注入式）。
- 三工具「空结果 vs 调用故障」语义统一（v0.1 遗留 D-3 扩展）。

**本迭代不做（顺延 v0.3 或独立任务）：**

- **服务托管化**（systemd / launchd）——顺延 v0.3。**顺延代价须显式承担**：v0.2 的 worker 是**无托管、崩溃不自动拉起**的进程，且 worker 挂了没有任何一方会立即察觉（不同于 HTTP 服务挂了调用方立刻收到连接错误），队列会静默积压。因此 **v0.2 只能在人工看护下灰度，不得进入无人值守运行**。
- **工具调用并发化**——顺延 v0.3。但 async 地基进本迭代后，v0.2 的设计**必须保证并发化时不需再动架构**（判据：三工具并行只需在编排层加 `gather`，不需改工具实现或处理核心）。
- **批内多条并发处理**——顺延 v0.3（v0.2 灰度期单 worker 实例、批内串行）。
- **RunRecord 持久化**——顺延 v0.3（与 `processed_news`/`tasks` 写回职责重叠，待 DB 模式落地后重估）。
- **多 provider 生产验证**——顺延部署阶段/v0.3。
- 换非 reasoning 模型（技术选型，需 Architect 评估）。
- **主链路算法逻辑变更**——保持 v0.1 已验证的评分/标签/摘要/翻译逻辑。理由（Developer R1 强调）：v0.2 要验的是「两种模式结果是否等价」，任何算法改动都会让比对失去基准、联调无法归因。
- HTTP 对外契约变更；建表改表、角色与 GRANT 管理、`news_positions` 关联（xiaobao 权属）。
- 评分加权 `score_total`（O-1 已定案方案 A，归 xiaobao）。
- 强可重入 checkpoint（REQ-002 结论）；第二调用方的实际实现（只定义协议、不写实现）。

## 5. 前置依赖

**已就绪（xiaobao 交付并实测验证）：**

- ✅ `ai_worker` 角色 + 列级 GRANT（2026-07-25 逐列对照契约一致、越权拦截实测通过、`SECURITY DEFINER` 触发器已建）。
- ✅ **权限矩阵新增 3 列已 GRANT 并双库对称**（2026-07-27，`news_test` + 生产 `news`）：`raw_items.source_item_url`（SELECT）、`raw_items.l0_label`（SELECT）、`tasks.run_after`（UPDATE）。**这三列分别解锁了 URL 读取、`domain_tags` 来源、退避写入三件事**，缺一不可。
- ✅ schema：ai 可写四列 + 只读列在 `news_test` 全部存在。
- ✅ 造数脚本 `seed_ai_queue_test.sql` 已交付并已执行，`news_test` 现有 **5 条** `process_type='ai'` + `l1_status='queued'`。
- ✅ R-5 结构说明（三类 source type 的 `content` 字段表 + 缺失兜底 + `sources.config`）。**注意其表述盲区**：R-5 只描述 `content` jsonb 的键，未涵盖 `raw_items` 的一级列（原文链接实际在 `source_item_url` 列）。
- ✅ 连接四要素：`host=127.0.0.1` `port=5432` `dbname=news_test` `user=ai_worker`（与 xiaobao **同机**）。

**待注入（唯一剩余外部依赖，归 DevOps 在部署阶段执行）：**

- ⏳ **`ai_worker` 口令注入** —— 强口令由 xiaobao DevOps 生成，存服务器 `/root/.secrets/ai_worker_news_test.pw`（`chmod 600`、root only、不入仓）。
  **交付方式（Owner 2026-07-27 定，已简化）**：ai worker 与 xiaobao **同机部署**，故**不需要 Owner 人肉转交、不经对话传递**——由 **ai 侧 DevOps 在该服务器上直接读取该文件，写入部署目录的 `.env`**（`chmod 600`、仓外、`.gitignore` 已覆盖），按 O-7 拆字段注入 `AI_DB_PASSWORD`。
  **纪律**：口令全程不进 git、不进 coordination 仓、不进任何 `docs/`、不在会话中明文出现。Owner 只需授权 DevOps 访问该文件。
  到位前无法连库，**阻塞联调冒烟与 C-6 行锁实证**，但不阻塞设计与实现。

**契约与设计输入：**

- `news-l1-db` **v1.3**（数据库边界单一真源；DevOps 已执行 3 列 GRANT，**待 xiaobao 升 v1.4** 把权限矩阵补齐——不阻塞 ai，GRANT 事实已生效）、`news-l1` v1（输出语义真源）、`kb-search` v1。
- `decisions/0002`（多调用方定位，数据源协议的决策来源）；`ADR-0001` 条件图编排、`ADR-0002` ChainedAIClient。
- v0.1 实现代码（commit `344ad49` 起）为处理核心基线。

**新增技术依赖：**

- DB 驱动（async，选型归 Architect）+ `pytest-asyncio`；`requirements.txt` 与 `.env.example` 需更新（属受保护路径，改动走既有约定）。
- 环境变量按字段拆分：`AI_DB_HOST` / `AI_DB_PORT` / `AI_DB_NAME` / `AI_DB_USER` / `AI_DB_PASSWORD`（**不用整串 DSN**，防被日志/`ps`/驱动异常整串带出）；运行模式 `RUN_MODE=http|db`。

**xiaobao 侧已上报的 3 项缺口（登记，均不阻塞 ai，但影响联调判读）：**

1. **`score_total` 在 database 模式无触发点** —— ai 写回后该列保持 NULL，导致新闻按分排序沉底、前端评分徽章显示 0。**联调时观察到该现象不得归因于 ai**（见 AC-8.2）。xiaobao 已上报其 PM 决策补触发点，结论后回帖。
2. **占位行 `published_at` 写死 NULL** —— xiaobao 将修正占位 INSERT；ai 侧已按其建议在写回时一并带上 `published_at` 作双保险（见 AC-4.6）。
3. **手动重试接口不支持 `l1_ai_process`** —— 联调期若需重试某条失败条目，告知 xiaobao `raw_item_id`，由其处理。

**xiaobao 侧已承诺的 1 项修复（不阻塞 ai）：**

- **C-5 的毫秒级黑洞窗口**：`l1_status='queued'` 与 `INSERT INTO tasks` 两条语句间无显式事务，进程崩溃会产生「`queued` 但无 task」的孤儿条目。xiaobao 已登记待办包进事务。**ai 侧按其要求不增加孤儿探测逻辑**（徒增耦合）；联调期若遇到，告知 `raw_item_id` 由其补建。

**届时前置（登记，避免遗漏）：**

1. 生产库 `news` 的业务 GRANT 已随 3 列变更双库对称执行；ai 上生产前仍需 xiaobao 复核完整权限矩阵，v0.2 部署就绪检查**不假定生产已就绪**。
2. **造数队列会耗尽**（预置 5 条）→ 自测阶段即开始消耗（并发 claim 与事务回滚必须对真实 PG 测），耗尽前需 xiaobao 补跑脚本。ai 侧提前打招呼，不做「静默等待队列」误判。

## 6. 契约缺项清单（C-1~C-10 + Q-1~Q-6：闭合状态）

> 由 ai 侧 Architect / Developer 在 PRD R1 提出，2026-07-26 转达 xiaobao；xiaobao PM / Architect / DevOps 于 2026-07-27 全部答复。
> **结论：10 条契约缺项闭合 9 条**（C-1~C-10 中仅 C-6 待 ai 侧实证）；**6 条 Q 项闭合 5 条**（Q-1 待 xiaobao PM 表态，不阻塞）。**原 4 条阻塞定稿项全部闭合。**

| # | 闭合结论 | 契约版本 |
|---|---------|---------|
| **C-2** `tasks.status` 枚举 | ✅ 闭合。取值仅 4 个（`queued`/`running`/`succeeded`/`failed`）+ 与 `l1_status` 的四时点对应表（**非一一对应**：tasks 是执行态、l1_status 是业务态） | v1.3 |
| **C-3** 写入方式 | ✅ 闭合，**推翻 ai 推断**：是 **ai UPDATE 占位行**（占位行是 xiaobao 产品硬约束——L0 通过后新闻须立即可见）。ai 的「触发器时序」担心不成立（`news_positions` 不存排序键）。实现用 `INSERT ... ON CONFLICT (raw_item_id) DO UPDATE`；`raw_item_id` 唯一约束已有；`id` 由 DB 生成不写 | v1.3 |
| **C-5** 必建 task | ✅ 闭合，**诚实答案「几乎必然但非原子」**：唯一写入点紧邻建 task，但无显式事务，存在毫秒级窗口。xiaobao 已承诺包进事务。**ai 只 claim tasks 不扫 raw_items 是安全的**，不需孤儿探测 | v1.3 |
| **C-10** `tags_v2` 第五类 | ✅ 闭合。**`sentiment` 系起草笔误，PM 裁定本阶段不引入**；第五类回归 `processing`，与 HTTP 契约完全一致。依据：xiaobao 前后端 `sentiment` 零消费；引入须先改 HTTP 契约。情感标签登记为其后续迭代候选 | v1.2 |
| C-1 / Q-5 `domain_tags` | ✅ 闭合（部分满足）。已 GRANT `raw_items.l0_label`（SELECT）→ **不再恒空**；但 `l0_label` 是**单值 varchar** 而非数组，仍不与 HTTP 模式等价，差异说明保留在 AC-8.2 | v1.3 + GRANT |
| C-4 退避 | ✅ 闭合，**根因订正**：不是 claim SQL 缺条件，而是 **xiaobao 的 GRANT 少给了 `run_after` 列**（已认领并补 GRANT）。其退避机制本就存在。**ai 禁止自行用 `updated_at + backoff` 计算**（会形成两套真源） | v1.3 + GRANT |
| C-6 行锁可行性 | ⏳ **待 ai 侧实证**（不阻塞定稿）。xiaobao 预判列级授权很可能不满足 `FOR UPDATE`；若实测失败，其将对 `tasks` 改授表级 UPDATE。**结论须回帖 coordination** | — |
| C-7 `language` | ✅ 闭合。**固定 `'zh'`**（产出内容语种）。附 xiaobao 内部两处订正：其 PM 所述「原文语种归 `raw_items.language`」该列不存在；其占位行原写原文语种，将统一为 `'zh'` | v1.2 |
| C-8 重试真源 | ✅ 闭合。以 `tasks.attempt` 对比 **`tasks.max_attempts` 列**判定，**禁止硬编码 3**（该列按其 `AI_MAX_RETRIES` 写入）；`l1_attempt` 为镜像值 | v1.3 |
| C-9 索引 | ✅ 闭合，**前提被推翻**：`tasks.metadata` 列**根本不存在**（契约起草错误），`raw_item_id` 是一级 uuid 列 + 外键，关联走主键，`ix_tasks_queue` 已足够，**无需新增索引** | v1.3 |
| Q-1 `needs_context` | ⏳ **待 xiaobao PM 表态**（不阻塞）。其 Architect 已倾向补列（成本极低，认可 ai 的论证）。若补列则 ai 一并写入 | — |
| Q-2 `context` 恒空 | ✅ 闭合。xiaobao PM **知悉并接受**（前端条件渲染、零 UI 副作用），承诺不作为质量问题回流，且不要求 ai 改过滤策略 | — |
| Q-3 `analysis` 空值 | ✅ 闭合。**同意写 NULL**（其列 nullable、前端真值判断，NULL 语义更准） | — |
| Q-4 rss 原文链接 | ✅ 闭合，**ai 侧判断被推翻**：链接**一直存在**于 `raw_items.source_item_url` 列（非 `content` jsonb 键），只是此前未 GRANT。且该列对 `x_twitter` 同样有值 → **ai 不必从 `tweet_id` 构造 URL，三类源统一读列** | v1.3 + GRANT |
| Q-6 rss/jin10 接入排期 | ✅ 闭合。**近期不接入 AI 处理链路**；同意 ai 的验收分层。将来接入时由 xiaobao 提报新联调诉求 | — |

**本轮 ai 侧被推翻的 3 处判断（记录，供后续复盘）：**

1. **C-3 写入方式**：ai 从触发器语义推断「ai INSERT」，逻辑正确但缺少一条 ai 无从得知的产品硬约束（占位行用于 L0 通过后立即可见）。**教训：跨服务推断不能只从技术语义出发，涉及对方产品行为的必须问。**
2. **C-4 退避根因**：ai 判断「契约 claim SQL 缺时间条件」，实际是对方 GRANT 缺列。现象推演完全正确、根因归错。
3. **Q-4 rss 无链接**：ai 依据对方 R-5 字段表得出结论，但该表只覆盖 `content` jsonb、未覆盖一级列。**教训：对方交付的结构说明可能有覆盖盲区，涉及「确实没有」的否定结论要回问一次**——这正是 Owner「没法确认的不能留成遗留」原则的价值所在（该条原本已被 ai 记为「已知限制」结案）。

## 7. 风险和开放问题

**界面要点**：本迭代为纯后端服务迭代，无用户可见界面变更（ai 不含前端，调用方为 xiaobao；DB 模式不对终端用户暴露接口）。故不展开界面要点。

**已闭合的开放问题（R1 → R2）：**

| # | 结论 |
|---|------|
| O-1 `score_total` 归属 | ✅ **已定案方案 A**（2026-07-25，xiaobao 认定为契约起草笔误，已订正 v1.1）。PRD R1 按既有边界所写范围**被证实正确**，无需修改 |
| O-5 `l1_status` 枚举重复 | ✅ 已订正（合并单行） |
| O-4 双模式开关粒度 | ✅ 定为**进程级 + DB 模式仍监听 `/health`**（Architect 定），可行性由 AC-9.3 验证 |
| O-7 凭据注入 | ✅ 方案已定（DevOps）：口令不入仓、按字段拆分变量、`chmod 600` 仓外文件注入、轮换需重启（无热加载） |
| O-3 worker 运行参数 | ✅ 已有约束：单批 **N ≤ 8**、轮询 **10~30s**、灰度期**单实例**（DevOps 提出、Architect 确认为正确性约束，已写入 AC-3.3） |

**仍开放（设计阶段落定）：**

- **O-2（P1）数据源协议的抽象边界**：协议切在 `tasks.run_task` 之上（Architect 倾向、Developer 确认成本近零）；但按 §0 准则裁定 2，需定义为**显式协议**而非两个映射函数——协议应包含哪几个操作、`L1Input`/`L1Output` 是否直接作为跨层契约、协议实现的注册与选择方式，由 Architect 设计阶段落定。
- **O-6（P1）事务边界与连接管理**：三段式（claim 短事务 / 处理无事务 / 写回短事务）已写入 AC-4.2；连接池规模、与 `SECURITY DEFINER` 触发器的交互、async 驱动下的连接生命周期，由 Architect 落定。
- **O-8（P0，新增）async 改造的切分与回归策略**：改造涉及 4 个 IO 点 + 调用链 + 40 例单测框架迁移。切分方式（一次性改 vs 分层改）、回归基线如何固定（如先录制 v0.1 的输出快照再改造）、LangGraph async 节点的编排调整，由 Architect + Developer 在设计阶段共同落定。**这是本迭代最大的技术风险项**——风险在回归，不在重写。
- **O-9（P1，新增）DB 驱动选型**：须与 async 地基一致（AC-9.2）。候选 asyncpg / psycopg3(async)，由 Architect 定；选型需同时满足 AC-3.4 的行锁权限实证结论。
- **O-10（P2，新增）`locked_by` worker 标识规则**：是否含实例名/进程启动时间，影响 AC-5.4 的自愈范围判定；需与 xiaobao 确认自愈行为不与其回收逻辑冲突。

## 8. 工程成本与实现切片

> R2 新增（R1 完全缺失成本信息，Architect 设计切分与 PM 排期均缺依据）。数据来自 Developer R1 基于 v0.1 代码基线的核算。

**最大红利：处理核心的解耦几乎是现成的。** `run_task(task_type, run_id, inp, client, tools)`（`tasks.py:55`）已是与传输层无关的签名，`main.py:34` 只调用它一行，HTTP 关注点全在 `main.py` 内。因此 §1 约束 2 的实现风险低于其表述的谨慎程度。

**工作量分五块：**

| # | 块 | 说明 | 风险 |
|---|---|---|------|
| 1 | **async 地基改造** | 4 个出网调用点改 `AsyncClient` + 调用链改造 + LangGraph async 节点 + 40 例单测迁移 `pytest-asyncio` | **最高（回归风险，非重写风险）** |
| 2 | 入向映射 | 三类 source type × 字段搬运 + URL 构造 + 缺失兜底 | 中（C-1 未闭合时 `domain_tags` 恒空） |
| 3 | 出向映射 + 写回事务 | 字段映射 + 三段式事务 + 幂等 | 中（依赖 C-3 定案） |
| 4 | claim SQL + worker 循环 | 退避判定 + 批量上限 + 优雅停机 + 启动自愈 | 中（依赖 C-2/C-5/C-6） |
| 5 | 结构化 logging | 横切改造，须注入式（AC-6.4） | 低（但与 AC-2 有张力，设计阶段先定形态成本最低） |

**测试面**：现有 40 例全在 HTTP / graph / client 层。DB 模式需新增四类——claim 并发、字段映射、写回事务回滚、优雅停机。其中**并发 claim 与事务回滚必须对真实 PG 测**（mock 测不出 `SKIP LOCKED` 与事务语义），依赖 `news_test` 与造数队列（见 §5 届时前置 2）。

---

## Review 记录

### R1 — DevOps Review（2026-07-25）

- 结论：**未通过**（需 PM 修改后进入 R2）
- Review 范围：运行形态变更（HTTP 常驻服务 → 常驻 worker）、环境变量与凭据注入、健康检查与探活、发布风险与回滚条件、灰度期运维可行性
- 实读依据：`v0.2-prd.md`、`v0.2.md`、coordination `contracts/news-l1-db.md` v1 与 `communications/REQ-003-db-boundary-async.md`、本项目 `.env.example` / `.gitignore` / `requirements.txt` / `src/agent_hub/{config,main}.py`；运行实查 `lsof 8100/8001/5432`（**三者当前均无监听**）、`ls .env`（**不存在**）
- 前提说明：O-1（P0）未有结论前 PRD 本就不能定稿，本轮意见与 O-1 并列，不因 O-1 而搁置。

**运维侧现状事实（本轮实查，影响下述判断）**：v0.1 的测试环境已不在——`.env` 已不存在、`127.0.0.1:8100` 无监听，共享库 `5432` 本机也无监听。即 v0.2 的 DB 模式不是「在既有环境上加个 worker」，而是**环境重建 + 新增外部依赖（共享数据库）**，部署面比 v0.1 大。

问题：

1. **高｜DB 模式下探活面缺失，AC-1~AC-8 无一条覆盖「怎么判断 worker 活着」。** O-4 倾向进程级开关，则 DB 模式进程很可能不起 HTTP 服务，`/health`（`main.py:20`）随之消失。v0.1 的全部运维手段（`curl /health`、`lsof` 看端口）在 DB 模式下同时失效，而 worker 静默死亡的表现与「队列为空空转」完全一样——运维无法区分。这不是实现细节，是验收缺项：没有探活面就无法定义部署就绪检查的通过条件，也无法接任何托管层的存活探针。建议 AC-1 增补：DB 模式下仍须提供可外部观测的存活面，最低限二选一——① DB 模式进程仍监听 `/health`（只做探活、不接受 run 请求）；② 每轮轮询输出心跳日志并暴露 `last_poll_at`，由 DevOps 按日志时间戳判活。选哪个由 Architect 在 O-4 定，但「必须有」应写进 PRD。

2. **高｜停机与回滚时的残留 `processing` 锁无任何验收覆盖，回滚不是「改配置重启」那么简单。** AC-1 把回滚定义为「改配置并重启」，US-4 同调。但 DB 模式的回滚风险不在开关，在**回滚瞬间的库状态**：worker 被停时，已 claim 未处理完的条目停留在 `l1_status='processing'` + `locked_by` 已写，切回 HTTP 模式后这批条目在 ai 侧无人处理，只能等 xiaobao 侧 30 分钟卡死回收（契约 §卡死回收）。按单条 74~79s、批量 N 条计，一次不优雅的停机会让 N 条新闻延迟至少 30 分钟。AC-5 只覆盖了「worker 被杀死」这一被动路径（依赖 xiaobao 回收，正确），但没覆盖**主动停机/回滚**这一必然发生的路径。建议 AC-1 或新增 AC 增补：收到停机信号（SIGTERM）时 worker 应优雅停机——停止新 claim、处理完当前条目后释放锁，超出宽限期才强杀并退回卡死回收路径；并明确宽限期上限须小于 xiaobao 的 1800s 阈值。这条同时是 v0.3 托管化的前置（无优雅停机，systemd/launchd 的 restart 会持续制造残留锁）。

3. **高｜AC-6 的脱敏要求覆盖不到最主要的口令泄漏路径。** AC-6 写「日志不得包含 API key、数据库口令、完整 `raw_content`」，方向对，但数据库口令最常见的泄漏路径不是主动打印，而是**连接失败时驱动抛出的异常信息里带 DSN**（psycopg / asyncpg 的连接错误、SQLAlchemy 的 `repr(engine.url)`、以及未捕获堆栈）。`main.py:35-43` 现有兜底只回 `type(exc).__name__`，是安全的；但 worker 侧一旦按常规写法 `logger.exception(...)`，DSN 会整串进日志。建议 AC-6 增补一条可验证的断言：数据库连接/查询异常必须在记录前重写为不含连接串与口令的消息（以「构造一个错误口令的连接、断言日志中不出现口令子串」作为验证方式）。

4. **高｜DB 模式的冒烟与联调造数依赖未列入 §5 前置依赖，会直接卡住部署就绪检查。** v0.1 的发布冒烟是 `curl /health` + 一次真实 `POST`，两个动作 ai 侧都能独立完成。DB 模式下冒烟必须是「库里出现一条 `process_type='ai'` 且 `l1_status='queued'` 的条目 → worker claim → 写回 `completed`」，而 ai 对 `raw_items` **只有 SELECT 权限**（契约 §权限矩阵），**ai 侧无法自行造数**。也就是说本迭代的部署就绪检查存在一条硬性跨项目依赖：测试库造数由谁、以什么方式提供。PRD §5 已列 R-1（角色 GRANT）、R-2（schema 迁移）、连接信息，唯独漏了造数。建议 §5 增补第 4 条待确认项，并在 coordination `REQ-003` 待跟进表追加对应行（PM 侧动作）。

5. **中｜AC-6 未规定日志 sink 与轮转，托管化顺延后这是实打实的运维缺口。** worker 模式下日志是唯一观测面（US-6 自己也这么说），但 AC-6 只规定了日志「内容」，没规定「去哪」。托管化已顺延 v0.3，则 v0.2 灰度期大概率沿用 v0.1 的 `nohup` 方式——日志进 `nohup.out`、无轮转、无切割，一个 7×24 轮询进程的日志量远大于按请求触发的 HTTP 服务。建议 AC-6 补一句：日志统一输出到 stdout（由托管层收集），v0.2 灰度期由 DevOps 指定落盘路径并人工轮转，不在应用内实现轮转。

6. **中｜「托管化顺延 v0.3」的运维前提未在范围边界写明。** 顺延本身我认可（形态未定先写托管配置确实返工）。但顺延的代价必须显式写出来：v0.2 的 worker 是**无托管、崩溃不自动拉起**的进程，而它与 v0.1 的 HTTP 服务有本质差别——HTTP 服务挂了调用方立刻收到连接错误，worker 挂了没有任何一方会立即察觉，队列静默积压。因此 v0.2 只能在**人工看护下灰度**，不得进入无人值守运行。建议 §4「本迭代不做」的托管化条目补上这句约束，否则部署就绪检查阶段无法判定「无托管」是已知取舍还是遗漏。

7. **低｜§5 前置依赖未提新增数据库驱动依赖与 `.env.example` 更新。** `requirements.txt` 当前无任何 DB 驱动，DB 模式必然新增（psycopg / asyncpg / SQLAlchemy，选型属 Architect）。两者均在受保护路径（`project-context.md` §受保护路径：`.env.example`、`requirements.txt` 属部署配置）。不阻塞，但建议 §5 提一句，便于设计阶段一并定。

**O-7（凭据注入）— DevOps 结论（PRD 已指派本角色，此处给出，供 PM 收编进 PRD 或留待设计阶段落地）**：

- **口令不入任何仓库文件**：`.env` 已被 `.gitignore:5` 覆盖（已核实），口令只进 `.env`；`.env.example` 只写变量名 + 用途，与 v0.1 现行做法一致。
- **变量名建议按字段拆分，不用整串 DSN**：`AI_DB_HOST` / `AI_DB_PORT` / `AI_DB_NAME` / `AI_DB_USER` / `AI_DB_PASSWORD`。拆分的理由正是问题 3——整串 DSN 极易被日志、`ps` 输出、异常信息整体带出；拆分后拼装点唯一、可集中脱敏。若 Architect 因驱动 API 倾向单一 DSN，则必须配套「DSN 变量禁止直接进日志/异常」的实现约束。
- **运行模式变量**：建议 `RUN_MODE=http|db`（对应 AC-1），缺失或非法时按 AC-1 走 HTTP 模式 + 记警告；这条 AC-1 已写，认可。
- **轮换**：当前无配置热加载（`config.py:16` 的 `load_dotenv()` 仅进程启动时读一次），口令轮换必须重启 worker。轮换窗口内需 xiaobao 侧允许新旧口令并存，或约定轮换在停机窗口执行——此项待 R-3 回应后与 xiaobao DevOps 约定，不阻塞本迭代。
- **交付渠道**：`ai_worker` 口令请 Owner 通过带外渠道交付（不经 git、不经 coordination 仓、不贴进任何 `docs/`），由本角色写入部署机 `.env`。

**O-3（worker 运行参数）— DevOps 侧输入（与 Architect 共同归属，供设计阶段取用）**：

- **单批 claim 上限存在契约推导出的硬上限**：claim 时整批同时置 `processing` 并写 `locked_at`（AC-3），若批内串行处理，批内最后一条的持锁时长 ≈ N × 单条耗时。以 v0.1 实测 74~79s、xiaobao 卡死阈值 1800s 计，`N × 79s < 1800s` → **N ≤ 22 是绝对上限，超过必然触发误回收与重复处理**。留出 LLM 超时与重试余量后，**建议 v0.2 取 N ≤ 8**。若设计阶段选择批内并发处理，该上限可放宽，但必须以「批内最长完成时间」重算，不能按平均值。
- **轮询间隔**：建议 10~30s。空转成本仅一次 SELECT，间隔过短无收益；相对 74~79s 的单条耗时，30s 内的领取延迟可忽略。
- **worker 实例数**：v0.2 灰度期建议**单实例**。AC-3 已要求并发 claim 不重复处理（该要求应保留，是正确性底线），但多实例会把验证面从「闭环是否跑通」扩大到「锁竞争是否正确」，与人工看护灰度的定位不符。多实例留到 v0.3 与托管化一并验证。

建议修改（按优先级）：

- 必改（高）：AC-1 补 DB 模式探活面（问题 1）；补优雅停机与回滚残留锁的验收（问题 2）；AC-6 补数据库连接异常脱敏的可验证断言（问题 3）；§5 补测试库造数依赖并同步 coordination 待跟进表（问题 4）。
- 宜改（中）：AC-6 补日志 sink 约定（问题 5）；§4 托管化顺延条目补「人工看护灰度、不得无人值守」（问题 6）。
- 可选（低）：§5 提一句新增 DB 驱动与 `.env.example` 更新（问题 7）。
- O-7 视为已有 DevOps 结论，可从开放问题降级为「已定、待设计阶段落地」；O-3 的运维侧约束（N ≤ 8、间隔 10~30s、单实例）请 Architect 在设计阶段确认或反驳。

未构成问题、明确认可的部分：

- 把 DevOps 纳入本轮 Review 的判断正确——运行形态变更 + 新增数据库凭据，确属部署领域，前一版本「PRD 阶段不涉及部署变更」的理由在本版本确实不再成立。
- AC-1 的「配置缺失/非法时按 HTTP 模式启动并记警告、不得静默进入 DB 模式」是正确的失败安全默认（fail-safe），与 `config.py` 现有 provider 配置校验风格一致。
- AC-5 把「worker 被杀死后不自行回收他人锁」交给 xiaobao 侧回收，符合契约 §卡死回收的职责划分，ai 侧不越权，正确。
- AC-4 要求写回单事务、不留「结果已写但状态未推进」的不一致，是回滚与重试安全的基础，认可。
- 把结构化 logging 并入本迭代（而非跟着托管化一起顺延）是对的——worker 模式下它是唯一观测面，若一并顺延，v0.2 将处于「既无探活、又无日志」的不可运维状态。

### R1 — Architect Review（2026-07-25）

- 结论：**未通过**（需 PM 修改后进入 R2）
- Review 范围：适配层抽象边界、worker 与既有 LangGraph 编排的接缝、双模式开关设计、O-1 的架构影响、DB 契约与既有 HTTP 契约的一致性可达性
- 实读依据：`v0.2-prd.md`、`v0.2.md`、coordination `contracts/news-l1-db.md` v1、`contracts/news-l1.md` v1、`communications/REQ-003-db-boundary-async.md`、`decisions/0002`；本项目 `src/agent_hub/{main,tasks,schemas}.py`、`graphs/news_l1.py`、`docs/baseline/project-context.md`
- 与 DevOps R1 的关系：DevOps 已覆盖运行形态、探活、停机、脱敏、造数、日志 sink。本轮不重复其 7 条，只在 A-5 与末节对其 O-3 数学与 O-4 相关结论做确认或补充。本轮问题集中在**输入侧映射与状态机真源**——这是 DevOps 未覆盖、且比 O-1 影响面更大的一片缺口。

**总体判断**：worker 闭环的**输出侧**（写回 `processed_news` + 推进状态）在契约里被描述得较完整，但**输入侧**（如何从库里的 `raw_items` 构造出与 HTTP 模式等价的处理输入）几乎没有真源支撑。AC-8 要求「两种模式产出的业务字段等价」，而按现有契约授权的可读列，DB 模式**构造不出等价的输入**，因此 AC-8 当前不可达。这不是实现难度问题，是契约缺项，须回到 xiaobao 侧补齐后 PRD 才能定稿。

问题：

1. **阻塞｜DB 模式的输入构造缺口，AC-8「两模式业务字段等价」当前不可达。** 三处具体缺口：

   - **`domain_tags` 无契约授权来源**。HTTP 契约 `L1Input.domain_tags`「来自 L0 分类」，是 prompt 与工具路由的输入之一（`graphs/news_l1.py:311` `_build_query` / `:320` `_build_kb_query` 均消费）。但 `news-l1-db` v1 §raw_items 的 ai_worker 可读列只有 `id / source_id / content / published_at / l0_status / l1_status / l1_attempt / process_type`——只有 L0 的**状态**，没有 L0 的**分类结果**。DB 模式下 `domain_tags` 只能恒为 `[]`，与 HTTP 模式输入不等价。
   - **原文 URL 不在可读列**。HTTP 契约 §raw_content 约定明写「若 `raw_items.source_item_url` 存在而 `raw_content.url`/`canonical_url` 不存在，xiaobao 必须补入 `raw_content.url`」——也就是说 URL 在 xiaobao 侧是**独立列 `source_item_url`**，靠调用方在构造请求时补进 `raw_content`。DB 模式没有这个构造方，而 `source_item_url` 不在 ai 可读列清单里。后果是 `_extract_url`（`graphs/news_l1.py:407`）取不到 URL → `link_read` 工具在 DB 模式下**整条路径失效**（`_should_link_read`，`:267`），处理质量与 HTTP 模式产生系统性差异。
   - **预取上下文在 DB 模式无提供方，且引出一个 PRD 未讨论的架构事实**。HTTP 模式下 `kb_results` / `link_content` / `search_summary` 由 xiaobao 预取传入；DB 模式无人预取，KB 检索只能由 ai **主动**发起，而 KB 检索的唯一通道是回调 xiaobao 的 `POST /v1/kb-search`（`tools/kb.py`，契约 `kb-search.md`）。**结论：DB 模式并未消除 ai → xiaobao 的 HTTP 依赖，只是把方向反转了。** PRD §1「集成模式从 HTTP 同步扩展为数据库边界」的表述会让人误读为 DB 模式下两侧无 HTTP 耦合，实际仍需 xiaobao 侧 `kb-search` 接口与 `x-admin-token` 可用；xiaobao 侧 HTTP 不可用时，DB 模式退化为无 KB 上下文。这同时改变 `tool_summary` 的统计口径（DB 模式下 kb_search 计数必然从 0 变为常态 ≥1），而 AC-8 说「业务字段等价」——工具调用统计不属业务字段，但该差异应显式写明，否则联调时会被当成 bug。

   建议：§5 前置依赖追加「DB 模式输入构造所需的可读列补充」，并在 coordination 向 xiaobao 提出契约订正请求——最少需要把 L0 分类结果字段与 `source_item_url` 纳入 ai_worker 可读列（或明确它们已在 `content` jsonb 内、给出 key 名）。AC-8 应把等价口径**限定**为「在输入等价的前提下业务字段等价」，并单列一条说明 DB 模式的输入差异项与 KB 主动检索带来的 `tool_summary` 口径变化。

2. **阻塞｜`tasks.status` 的取值集合没有任何真源，AC-4 / AC-5「同步更新 tasks 状态」不可实现、不可验证。** `news-l1-db` v1 给了 `l1_status` 的完整枚举（§l1_status 枚举），但 `tasks.status` 只在字段表里写「任务状态 / 可更新」，取值集合与状态转移全缺。AC-4 要求「同步更新 `tasks` 状态」、AC-5 要求「置 `retryable_failed` / `final_failed`」——后者是 `l1_status` 的值，`tasks.status` 该写什么无从得知，也无法写出可验证的断言。ai 侧不能自行猜一套枚举写进库（`tasks` 是 xiaobao 业务真源，猜错会污染其状态机与卡死回收逻辑）。建议：作为 P0 契约缺项提报 xiaobao，要求补 `tasks.status` 枚举与转移规则（含「谁设置」列），与 `l1_status` 的对应关系写清；在此之前 AC-4/AC-5 中涉及 `tasks.status` 的部分不能定稿。

3. **阻塞｜`processed_news` 是 INSERT 还是 UPDATE 未定，且与 `news_positions` 触发器时序耦合，AC-4 存在两种互斥实现。** 契约 §职责边界表写 xiaobao 侧「`processed_news` 写入（AI 类）| ❌ 占位创建」，字面意思是 xiaobao **已插入占位行**、ai 做 UPDATE；但 §权限矩阵给 ai_worker 的是 `SELECT, INSERT, UPDATE` 三权，PRD AC-4 也只写「向 `processed_news` 写入」，未区分。两种语义导致的差异是实质性的：

   - 若 xiaobao 占位 INSERT：`news_positions` 触发器（§触发器，`INSERT` 后触发、`SECURITY DEFINER`）在**占位时**就已执行，此时 `score_total` / `score_dimensions` 均为空——若排序位依赖分值，会先按空值排一次；ai 后续 UPDATE 不再触发关联，排序位如何更新无契约说明。
   - 若 ai INSERT：触发器在结果齐备后触发，语义自然，但 ai 需生成 `id`（uuid PK，`raw_item_id` 是否有唯一约束未说明），且重试场景必须定义幂等键（同一 `raw_item_id` 重试时是 upsert 还是产生第二行）。

   AC-4 的「单事务、不留结果已写但状态未推进的不一致」在两种语义下事务范围也不同。建议：PRD 明确采用哪一种（架构上我倾向 **ai INSERT + 以 `raw_item_id` 为幂等键做 upsert**，让触发器在结果齐备后触发，语义最干净），并请 xiaobao 在契约中把写入方式、`raw_item_id` 唯一约束、重试幂等语义、触发器与 UPDATE 的关系一并写明。这条与 O-1 强相关：若 `score_total` 归 xiaobao（方案 A），还须明确 xiaobao 何时补算——ai 写完 `completed` 之后由谁触发，以及在补算完成前 `news_positions` 的排序位是否可见。

4. **高｜契约定义的退避间隔没有进入 claim 条件，失败条目会被立即重复领取，AC-5 的重试语义与契约自相矛盾。** 契约 §task type 定义 `l1_ai_process` 退避间隔 `[60s, 300s, 900s]`，但 §AI 待处理队列 claim 规则的 SQL 只有 `process_type='ai' AND l1_status IN ('queued','retryable_failed') ORDER BY published_at ASC LIMIT N`——**没有任何时间条件**。按此实现，一条刚被置为 `retryable_failed` 的条目会在下一个轮询周期（DevOps 建议 10~30s）立刻被重新 claim，退避完全失效：失败条目将以轮询间隔的节奏连续消耗 3 次尝试，几十秒内耗尽重试预算进入 `final_failed`，而退避本意是给下游故障（LLM 限流、provider 5xx）恢复窗口。AC-5 只写了「递增 attempt / 置 retryable_failed」，没写退避判定，因此这个矛盾在验收层不会被发现。建议：AC-5 增补退避判定条件（可用 `tasks.attempt` + `tasks.updated_at` 推导：`updated_at + backoff(attempt) <= now()` 才可被 claim，`updated_at` 在 ai 可更新列内，实现上可行），并请 xiaobao 把该条件补进契约的 claim 规则 SQL，避免两侧各写一套。

5. **高｜确认 DevOps 的批量上限约束成立，并补充其边界条件。** DevOps 问题 O-3 给出的 `N × 79s < 1800s → N ≤ 22`、建议 N ≤ 8，架构上我确认成立，推导前提正确（`locked_at` 在 claim 时一次性写入且此后不更新，批内串行则最后一条的持锁时长≈N×单条耗时）。补两点：① 该不变式是**正确性约束而非调优建议**——违反它会直接产生「xiaobao 误回收 + ai 仍在处理」的双写竞态（ai 处理完写 `completed`，xiaobao 已把它回收为 `retryable_failed`，最终状态取决于写入顺序），因此应写进 AC-3 作为可验证约束，而不是只留在 O-3 开放问题里；② 若设计阶段改为批内并发，上限按「批内最长完成时间」重算的同时，并发度还受 LLM provider 速率限制约束（`ADR-0002` 的 fallback 链在被限流时会串行重试多个 provider，单条耗时的尾部会显著拉长），故并发化顺延 v0.3 的前提下，v0.2 取 N ≤ 8 是稳妥的。
6. **高｜claim 的原子性在现有列级 GRANT 下能否实现，未经验证就被写成了验收标准。** AC-3 要求「同一条目已被其他 worker 领取时不得重复领取」，标准实现是 `SELECT ... FOR UPDATE SKIP LOCKED` 或带条件的原子 `UPDATE ... RETURNING`。但契约 §权限矩阵下有两处约束需要先验证：① ai_worker 对 `tasks` **只有 SELECT 与部分列 UPDATE，没有 INSERT**——claim 只能 UPDATE xiaobao 预建的 task 行，可行，但要求「xiaobao 入库时必然为每条 `process_type='ai'` 的 `raw_items` 建好 `l1_ai_process` task」，契约未明确承诺这一点（若 task 缺失，条目在 `raw_items` 里是 `queued` 但无 task 可 claim，会成为永久漏处理的黑洞）；② PostgreSQL 对 `SELECT ... FOR UPDATE` 的权限判定按**表级** UPDATE 权限进行，而契约给的是**列级** UPDATE——列级授权是否满足行锁的权限检查，须在真实库上验证，不能假定。建议：把这两项作为 R-1 就绪确认的子项明确列出（「以 `ai_worker` 身份实际执行一次 claim SQL 并断言可执行 + 并发不重复」），并请 xiaobao 补充「AI 类条目入库时必建 task」的承诺；AC-3 的并发要求保留（是正确性底线），但其可实现性不能悬空。

7. **中｜`processed_news.language` 与 `id` 在 AC-4 被要求写入，但两者都没有 AI 输出来源。** 契约 §processed_news 中 `language` 的「对应 AI 输出」列是 `—`，`id` 也是 `—`；而 `L1Output`（`schemas.py:49`）确实没有 language 字段。AC-4 把 `language` 列进「契约指定字段」要求写入，取值从哪来、缺省写什么都没定义（是原文语种检测结果？固定 `zh`？沿用 `translation` 的 key？）。`id` 则取决于问题 3 的 INSERT/UPDATE 结论。建议：AC-4 逐字段标注来源，无来源的字段要么从写入清单移出（由 xiaobao 侧填），要么明确取值规则；若决定由 ai 产出语种标识，属 `L1Output` 扩展，需先改 `news-l1` HTTP 契约（变更纪律第 1 条），不能只在 DB 模式偷偷多产一个字段。

8. **中｜`raw_items.l1_attempt` 与 `tasks.attempt` 双计数器，重试上限的判定真源未定。** AC-4 要求递增 `l1_attempt`，AC-5 要求递增 `attempt`，两者都在 ai 可写范围内，但「尝试次数未达上限（3）」按哪个判定没写。两个计数器在 xiaobao 侧卡死回收（回收也会推进状态）介入后必然出现漂移。建议：明确以 `tasks.attempt` 为重试判定真源（它与 §task type 的最大尝试次数 3 和退避表同源），`raw_items.l1_attempt` 视为对外展示的镜像值，并在 AC 中写明「两者在同一事务内一起推进」。

9. **中｜「不回收他人锁」的口径需要区分「他人」与「自己上次进程」。** AC-5 写 ai 侧不自行回收他人锁（正确，与契约 §卡死回收一致）。但 worker 重启后，库里会留下一批 `locked_by` = 本 worker 标识的 `processing` 条目——这类锁按契约字面属于「ai 自己写的锁」，却又不是当前进程持有的。若一律等 xiaobao 的 1800s 回收，则每次重启都会让队首若干条延迟至少 30 分钟（与 DevOps 问题 2 的优雅停机是同一问题的两面：优雅停机能覆盖计划内重启，覆盖不了崩溃）。建议：PRD 明确口径——ai 仅可在启动时回收「`locked_by` 等于本 worker 标识」的残留锁（属自愈，不越权），其余一律交 xiaobao；`locked_by` 的标识规则（是否含实例名 / 进程启动时间）需在设计阶段定，并请 xiaobao 确认该自愈行为不与其回收逻辑冲突。

10. **低｜claim 的 join 路径可能缺索引支撑。** AC-3 要求按 `published_at` 升序取，而 `published_at` 在 `raw_items`、claim 目标在 `tasks`，两表关联只能走 `tasks.metadata->>'raw_item_id'`（`metadata` 对 ai 只读、jsonb）。jsonb 表达式 join 若无对应索引，队列增长后 claim 会成为周期性全表扫。不阻塞 PRD，但建议在 R-2（schema 迁移确认）中附带确认是否存在支撑该访问路径的索引，必要时由 xiaobao 侧补。

**O-1（`score_total` 归属）— Architect 结论**：支持 **方案 A**（`score_total` 归 xiaobao 写入，ai 只产 `score_dimensions`）。理由三条，按权重排序：

1. **加权是消费侧策略，不是处理侧事实**。四维原始评分是对新闻内容的判断（ai 的职责），加权公式是「这个产品当前认为什么更重要」的排序策略——它会随 xiaobao 的产品目标频繁调整，而 L1 处理逻辑不该因为排序偏好变化而改动、重测、重新部署。
2. **与 `decisions/0002` 的多调用方定位直接冲突**。ai 是生态内部通用 AI 中枢，第二个调用方的加权口味必然不同；把加权放进 ai 会迫使 ai 为每个调用方持有一份权重配置，等于把调用方的业务策略吸进服务方。
3. **会形成双向耦合与真源分裂**。ai 若要算加权，就得知道权重定义在哪、谁能改、改了如何生效——目前契约与沟通文档均未提供（REQ-003 沟通文档已自行指出这是方案 B 的额外前提）。而权重真源一旦跨仓同步，`news-l1` HTTP 契约明写「不由 ai 计算」也必须一并推翻。

补充一条方案 A 落地时必须同时定的事项（与问题 3 相关）：**xiaobao 侧补算 `score_total` 的时机**。ai 写回 `completed` 后由谁触发补算（触发器 / 轮询 / 应用层），以及补算完成前 `news_positions` 的排序位是否已对外可见。若不定这一条，方案 A 会把冲突从「谁算」推迟成「什么时候算」。若 Owner 最终选方案 B，则按 PRD 已写的路径回 PRD 阶段增补，且需 xiaobao 先交付权重真源、可配置性与变更通知机制——在此之前不建议进设计阶段。

**O-2（适配层抽象边界）— Architect 倾向（设计阶段落定）**：切在 **`tasks.run_task` 之上**，`L1Input` / `L1Output` 直接作为跨层契约，处理核心零改动。依据是 v0.1 的入口已经是解耦的——`main.py:32` 仅做 `run_task("news-l1", run_id, inp, client=..., tools=...)`，HTTP 关注点（请求解析、响应包装、耗时统计）全在 `main.py` 内，`tasks.py` / `graphs/news_l1.py` 不含任何传输层概念。因此适配层的职责收敛为：`raw_items`+`sources` 行 → `L1Input` 的入向映射、`L1Output` → `processed_news` 列的出向映射，两个映射器 + 一个 worker 循环，处理核心不动。AC-2 提出的验证方式（同一条用例在两种数据源各跑通一次）正确且成本低，认可保留。映射失败的归类建议：入向映射失败（必填字段缺失/类型不符）归 `client_error` 类、不重试直接 `final_failed`（重试不会让脏数据变干净），出向映射失败归 `server_error` 类、可重试——此项请在 AC-5 的 `error_kind` 分类里补上，DevOps 的 AC-6 `error_kind` 枚举当前也未含映射错误。

**O-4（双模式开关粒度）— Architect 结论**：采纳 **进程级**（一个进程只跑一种模式），与 PM 倾向一致；**但 DB 模式进程仍监听 `/health`**，即选 DevOps 问题 1 的方案①。两者不矛盾：`/health` 只做存活探针、不注册 `POST /v1/runs/news-l1` 路由，因此「一个进程只处理一种数据来源」的灰度与回滚推理不受影响，同时保住了 v0.1 已有的全部运维手段（`curl /health` + `lsof`）与未来托管层的存活探针。相比方案②（心跳日志 + `last_poll_at`），复用现成 `/health` 的实现成本几乎为零，且不需要 DevOps 靠解析日志时间戳判活。建议在 `/health` 响应中附 `mode` 与 `last_poll_at`，让探活同时能区分「活着且在轮询」与「活着但轮询卡住」。

**O-6（事务边界）— Architect 倾向（设计阶段落定）**：`processed_news` 写入 + `raw_items.l1_*` 推进 + `tasks` 状态与锁释放三者放**同一事务**，与 AC-4 的「不留结果已写但状态未推进」一致；`news_positions` 由触发器在同一事务内完成（`SECURITY DEFINER`，ai 无需权限）。需在设计阶段一并处理的是：该事务与 LLM 调用必须**分离**——LLM 调用（74~79s）绝不能持有数据库事务，否则连接被长时间占用且极易撞上服务端 idle-in-transaction 超时。即 claim 一个短事务、处理无事务、写回一个短事务，共三段。连接池规模按「worker 实例数 × 批内并发度 + 1」即可，v0.2 单实例串行下 2~3 个连接足够。

建议修改（按优先级）：

- **必改（阻塞）**：AC-8 的等价口径按问题 1 收敛并补 DB 模式输入差异说明；§5 追加「DB 模式输入构造所需可读列」前置依赖；AC-4/AC-5 中依赖 `tasks.status` 的部分待契约补枚举后再写（问题 2）；AC-4 明确 `processed_news` 的 INSERT/UPDATE 语义与重试幂等键（问题 3）。
- **必改（高）**：AC-5 增补退避判定条件（问题 4）；把批量上限不变式写进 AC-3（问题 5）；AC-3 的 claim 原子性补可实现性验证前置（问题 6）。
- **宜改（中）**：AC-4 逐字段标注来源、处理 `language`/`id`（问题 7）；明确 `tasks.attempt` 为重试判定真源（问题 8）；明确残留锁自愈口径（问题 9）。
- **可选（低）**：R-2 附带确认 claim 访问路径索引（问题 10）。
- O-1 已给架构结论（方案 A）+ 落地前提；O-2 / O-4 / O-6 已给结论或倾向，其中 **O-4 可从开放问题降级为「已定」**（进程级 + DB 模式仍监听 `/health`），O-2 / O-6 留设计阶段按上述倾向落定。

**需 PM 转达 xiaobao 的契约缺项（本轮新增，建议合并进 coordination `REQ-003` 待跟进表）**：

| # | 契约缺项 | 严重度 | 对应本轮问题 |
|---|----------|--------|--------------|
| C-1 | ai_worker 可读列缺 L0 分类结果与 `source_item_url`，DB 模式无法构造等价输入 | P0 | 问题 1 |
| C-2 | `tasks.status` 取值枚举与状态转移缺失 | P0 | 问题 2 |
| C-3 | `processed_news` 由谁 INSERT、`raw_item_id` 是否唯一、重试幂等语义、触发器与 UPDATE 的关系 | P0 | 问题 3 |
| C-4 | claim 规则 SQL 未含退避时间条件，与 §task type 退避间隔矛盾 | P1 | 问题 4 |
| C-5 | 未承诺「AI 类条目入库时必建 `l1_ai_process` task」；列级 UPDATE 能否支撑行锁待实库验证 | P1 | 问题 6 |
| C-6 | `processed_news.language` 无 AI 输出来源，写入方未定 | P2 | 问题 7 |
| C-7 | 重试上限按 `tasks.attempt` 还是 `raw_items.l1_attempt` 判定未定 | P2 | 问题 8 |
| C-8 | ai 启动时自愈「自己上次进程残留锁」是否与 xiaobao 回收逻辑冲突 | P2 | 问题 9 |
| C-9 | claim 的 `tasks.metadata->>'raw_item_id'` join 路径是否有索引支撑 | P2 | 问题 10 |

未构成问题、明确认可的部分：

- **适配层作为硬约束（§1 约束 2）而不是「顺便解耦」，判断正确**，且 `decisions/0002` 作为依据引用得当。v0.1 的入口形状恰好已经支持这一点，工程风险低于 PRD 的谨慎表述。
- **AC-2 的验收方式（两种数据源各跑通同一条用例）是正确的适配层验收姿势**——它验的是依赖方向而非代码形态，比「检查有没有抽象类」这类形式化标准有效得多。
- **§4 把「主链路算法逻辑变更」「HTTP 对外契约变更」「换非 reasoning 模型」明确排除在外，正确**。本迭代是集成模式变更，同时动处理逻辑会让「两模式结果是否等价」失去比对基准。
- **顺延四项的理由（形态依赖新架构，先做外围会返工）成立**，尤其工具并发化：并发单元在 worker 模式下确实多出「批内多条」这一维，两维一起设计是对的。
- **把 O-1 列为 P0 阻塞而不是「先按现有边界做、以后再说」，处理得当**——业务边界冲突若在 PRD 阶段被绕过，会在实现阶段以「少写一个字段」的形式暴露，代价更高。
- **`news-l1` HTTP 契约保持不变、DB 模式作为并行新增（§1 约束 1）**，为灰度与回滚保住了退路，与 DevOps 关注的回滚路径一致。

### R1 — Developer Review（2026-07-26）

- 结论：**未通过**（需 PM 修改后进入 R2）
- Review 范围：工程成本与实现切片、claim / 写回的实现路径、验收标准是否可验证、v0.1 代码复用面
- 实读依据：`v0.2-prd.md`、`v0.2.md`、coordination `contracts/news-l1-db.md` **v1.1**、`contracts/news-l1.md` v1、`communications/REQ-003-db-boundary-async.md`（读至 2026-07-26 最新一帖）；本项目 `src/agent_hub/{main,tasks,schemas,config}.py`、`graphs/news_l1.py`、`llm/client.py`、`tools/{base,kb}.py`、`tests/`、`requirements.txt`、`.env.example`；实查 `grep -rn "async def\|await " src/`（**0 命中**）、`grep -rn "stub" src/`（**0 命中**）
- 与另两方的关系：DevOps 已覆盖运行形态 / 探活 / 停机 / 脱敏 / 造数 / 日志 sink，Architect 已覆盖输入侧映射 / 状态机真源 / 契约缺项 C-1~C-9。本轮不重复其 17 条，只补 Developer 边界内、且**必须核对代码或契约逐字段才能发现**的问题；其中 D-2 / D-5 是对两方结论的实现层校验（一条证伪其组合、一条精化其范围）。

**前提：本 PRD 的事实层已被 coordination 07-25~07-26 三帖推翻，R2 须先刷新再改意见（详见末节）。** 简言之：O-1 已定案方案 A、契约已订正 v1.1、DB 前置已全部解除、DevOps 问题 4（造数）已解决。这不改变本轮问题的成立性，但会改变 PRD 多处措辞与定稿条件。

**契约版本核对（本轮附带产出）**：另两方 Review 的实读依据均标注 `news-l1-db` **v1**，而契约 07-25 已订正为 **v1.1**。我对 v1.1 逐条复核 Architect 的 C-1~C-9：**除 C-1 的 `source_item_url` 分支应撤回（见 D-5）外，其余全部仍成立**（v1.1 只动了 `score_total` 归属与 `l1_status` 枚举合并，未补任何可读列、未补 `tasks.status` 枚举、claim SQL 仍无退避条件）。O-1 / O-5 相关部分则已闭环。建议后续 Review 在实读依据中标注契约版本号。

问题：

1. **阻塞｜`tags_v2` 第五类标签两份契约冲突，AC-4 的写回内容无法确定。** `news-l1-db` v1.1 §processed_news 写 `tags_v2`「五类标签：`{domain, entity, event, content_type, sentiment}`」，而 `news-l1` HTTP 契约 §Tags 五类是 `{domain, entity, event, content_type, processing}`（`news-l1.md:133`），ai 的 `Tags`（`schemas.py:41-46`）同样是 `processing`——**ai 从不产 `sentiment`**（`normalize_output_node` 只填四类 + `processing`）。AC-4 把 `tags_v2` 列进「契约指定字段」要求写入，但两种写法都不成立：按 ai 现状写 → 多一个 DB 契约没有的 `processing`、少一个 `sentiment`；按 DB 契约写 → `sentiment` 无来源，且 `processing` 丢失。**`processing` 不能丢**：它承载 `engine:agent_hub` / `llm:{provider}` / `degraded:{reason}`（`news_l1.py:370-372`），是 worker 模式下判断"这条结果是否经过降级、走的哪个 provider"的**唯一结构化线索**——US-6 说日志是唯一观测面，但降级事实目前只落在 `tags.processing` 里，日志方案（AC-6）也没说要把它冗余出来。这与 O-1 是同一类型的起草笔误（`news-l1-db` 未对齐 HTTP 契约），变更纪律第 5 条「输出语义以 HTTP 契约为准」同样适用。建议：作为 **C-10** 提报 xiaobao 订正 `tags_v2` 为 `processing`；订正前 AC-4 中 `tags_v2` 的写入内容不能定稿。

2. **阻塞｜同步代码基线 + `/health` 探活 + 74~79s 阻塞处理，三者在同一进程内不可同时成立——Architect 的 O-4 结论与 DevOps 问题 1 的方案①组合起来会产出比"没有探活"更糟的形态。** 事实前提：v0.1 代码**全同步**，`grep -rn "async def\|await " src/` **0 命中**；LLM 调用是同步 `httpx.post`（`client.py:176`），KB / link / web 工具同样同步阻塞。Architect O-4 定「DB 模式进程仍监听 `/health`」，DevOps 进一步要求 `/health` 附 `last_poll_at` 以区分"活着但轮询卡住"。但若 worker 循环与 uvicorn 同进程、且 worker 跑在 event loop 内，单条处理的 74~79s 会**整段阻塞 event loop**，`/health` 在此期间不响应 → 探针按超时判死 → 重启 → 正好制造 DevOps 问题 2 所说的残留 `processing` 锁 → 每条新闻延迟至少 30 分钟等 xiaobao 回收。也就是说：一个假死误判触发的重启循环，比没有探活面更有破坏性。可行解两条，成本差别不大——① worker 循环跑在独立线程，uvicorn 主线程只服务 `/health`，处理核心零改造（推荐）；② worker 与 HTTP 探活拆两个进程。建议 PRD 增补一条可验证 AC：**DB 模式下单条处理进行中（≥60s）时，`/health` 仍须在 2s 内返回，响应体含 `mode` 与 `last_poll_at`**。这条能一次性把 O-4 与 DevOps 问题 1 的组合验死或验活，成本只是一个集成测试。
   连带一条对设计阶段的约束：该要求**反向限制 DB 驱动选型**——选 asyncpg 会把适配层拖进 async，而处理核心是同步的，AC-2 要求"处理核心无需修改"，混合后必然破防（要么 `run_task` 包 `run_in_executor`，要么核心改 async）。建议 §5 把「本迭代不引入 async 改造」写成显式前置约束，驱动倾向 psycopg3 同步模式；具体选型仍归 Architect 设计阶段定。

3. **高｜AC-7 一半是空条款、另一半定位错且覆盖不全——按现状实现完，同一 bug 在另外两个工具上原样保留。** 三点，逐条实查：
   - **(a) 空条款**：AC-7 末句「`tags.processing` 不应包含 `stub`」**当前已满足**——v0.1 已真实化，`src/` 中 `stub` 0 命中，且 `tests/test_news_l1.py:186` 已有断言 `assert "stub" not in processing`。这条不产生任何工作量，留在验收标准里只会在验收时制造"又完成一项"的错觉。建议删除，或改写为「保持既有断言不回归」。
   - **(b) 缺陷位置不在 `tools/kb.py`**：`kb.py:72` 空结果时返回的是 `ToolResult(ok=True, items=[])`——**已经是"成功但空"的正确语义**。把它变成故障的是 graph 层 `news_l1.py:213` 的 `if result.ok and result.items:`，空 items 落进 else 分支，记 `StepError(kind="kb_error")` 并追加 `degradations += ["kb_search_failed"]`。PRD 按字面「KB 返回 200 且 `results: []` 时标记空结果」去改，会改错文件。
   - **(c) 同一 bug 有三处实例**：`news_l1.py:150`（link_read）、`:179`（web_search）、`:213`（kb_search）三处写法完全相同。Tavily 搜不到结果、链接抓取成功但正文为空，同样被记成 `web_search_failed` / `link_read_failed`。AC-7 只写 KB，只修一处会留下"三个工具对同一情形两种语义"的不一致；而 DB 模式下预取上下文消失、工具路由触发率显著上升（Architect 问题 1 已指出），这个不一致会被放大。建议 AC-7 扩为「三个工具统一区分『成功但无结果』与『调用故障』」，并写明期望产出：空结果**不进** `degradations`（因而不产生 `degraded:` 标签）、只在日志与 `tool_summary` 中体现，`tool_summary` 维持"发起即计数"口径不变。这样才可验证。

4. **高｜AC-2 与 AC-8 的通过判据互斥，同一次验证会同时得出"通过"和"不通过"。** AC-2 的验证方式是「同一条处理用例在两种数据源各跑通一次」，AC-8 要求「同一条输入在两种模式下产出的业务字段等价」。但按 Architect 问题 1（我复核成立），DB 模式构造的 `L1Input` 与 HTTP 模式**本就不等价**——`domain_tags` 恒为 `[]`，而它同时进 prompt 与 KB 查询（`news_l1.py:206` `domain_tags=inp.domain_tags or None`），输出必然漂移。于是同一次验证里 AC-2 判通过、AC-8 判不通过，实现阶段会卡死在"到底算不算过"。建议把判据分开写死：
   - **AC-2 判"依赖方向"**：处理核心（`tasks.py` / `graphs/` / `llm/`）代码 diff 为空，且两种数据源均产出结构合法的 `L1Output`——不比对内容。
   - **AC-8 判"语义一致"**：限定为「在输入等价的前提下字段语义一致」，并显式列出 DB 模式的已知输入差异项（`domain_tags` 恒空、预取上下文缺失、KB 由预取转主动检索）。
   
   方向与 Architect 的口径收敛一致，但实现阶段需要的是可执行判据，不只是口径。

5. **高｜C-1 的 URL 分支应撤回并改写为实现要求：URL 可构造，但适配层必须显式回填 `raw_content["url"]`，否则 link_read 在 DB 模式静默失效。** Architect 问题 1 提「`source_item_url` 不在可读列 → link_read 整条路径失效」。对照 xiaobao 07-25 交付的 R-5 结构说明逐类核对后，这条需要**精化而非推翻**：
   - **`x_twitter`**：`content` 内有 `tweet_id` / `author_username` / `entities{urls[]}`，且 R-5 明确给了兜底规则「`author_username` 空串时源 URL 用 `x.com/i/status/` 形式」。→ **URL 可由适配层构造**（`x.com/{author_username}/status/{tweet_id}`，空则 `x.com/i/status/{tweet_id}`），**不需要契约新增可读列**。
   - **`rss`**：R-5 字段表为 `{title, summary, author, categories, source_name}`，**确无任何原文链接字段**。→ rss 类 link_read 无 URL 可用；但按 ai PM 07-26 在 coordination 定下的验收分层，rss 本迭代仅单测覆盖、无真实数据，故**不阻塞 v0.2**，应作为已知限制写进 PRD，而非当契约缺项挂着。
   - **实现要点（必须进 PRD 或设计文档）**：`_extract_url`（`news_l1.py:407`）只认 `raw_content["url"]` / `["canonical_url"]` 两个键，而 x_twitter 的 `content` **两个键都没有**。若适配层把 `raw_items.content` 原样塞给 `L1Input.raw_content`，`_should_link_read`（`:267`）恒为 False——**link_read 不报错、不降级、静默失效**，联调时表现为"工具从不触发"，极难归因。这属 AC-2 入向映射职责，建议点名写出。
   
   综上建议把 C-1 拆两条上报：`domain_tags`（L0 分类结果）**仍缺、仍需契约订正**；`source_item_url` **撤回**（R-5 已给构造规则）。

6. **中｜AC-4 字段来源逐条核对，三处语义待定（补充 Architect 问题 7，不重复 `language` / `id`）。**
   - **`analysis`**：`schemas.py:54` 为 `Optional[str] = None`，DB 列 `analysis text`。None 时写 NULL 还是 `''` 未定，直接影响 xiaobao 侧判空展示。
   - **`context`**：`normalize_output_node:362-367` 会过滤掉 URL 不在证据集内的条目（防 LLM 编造，v0.1 有意设计）。DB 模式证据更少，`context` 大概率恒为 `[]`。不是 bug，但写回后 xiaobao 看到空 `context` 会误读为"ai 没产出背景"，建议并入 AC-8 的已知差异说明。
   - **`needs_context` 在 DB 模式无处可写**：`L1Output` 有该字段、`processed_news` **无对应列**。HTTP 模式下它是给调用方的质量信号（"上下文不足、结果存疑"），DB 模式丢掉后 xiaobao 无法区分"证据充分的高分"与"证据不足的高分"——这与 v0.1 遗留 D-2（上下文充分性阈值）是同一条线。建议 PRD 明确取舍：接受丢弃（写进已知差异）或提报契约新增列。我倾向前者（v0.2 不扩契约），但必须显式写，不能默默丢。

**工程成本与实现切片（Developer 职责，PRD 完全未涉及）**

PRD 没有任何工作量估算或切片说明。基于 v0.1 代码基线给出，供 PM 排期与 Architect 设计阶段切分：

- **最大复用面：处理核心零改动**。`run_task(task_type, run_id, inp, client, tools)`（`tasks.py:55`）已经是与传输层无关的签名，`main.py:34` 也只是调用它一行。**Architect 的 O-2 倾向（切在 `run_task` 之上、`L1Input`/`L1Output` 作跨层契约）我确认成立，且工程成本几乎为零**——这是 v0.2 最大的一块红利，PRD 应该把它写出来（当前 §1 约束 2 的表述让适配层听起来像重活）。
- **新增工作量集中在四块**：① 入向映射（三类 source type × 字段搬运 + URL 构造 + 缺失兜底）② 出向映射 + 写回事务 ③ claim SQL + worker 循环 + 优雅停机 ④ 结构化 logging。
- **④ 是横切改造，与"处理核心零改动"存在张力**：现状**零 logging**（`src/` 无 `logging` 使用，全靠 FastAPI 默认输出），AC-6 要求每个处理步骤记录 `step` / `duration_ms` / `status`，必然要碰 `graphs/` 的每个 node。建议设计阶段明确 logging 以**注入方式**（随 state 或依赖注入传入）而非在 `graphs/` 里直接 import 全局 logger，否则 AC-2 的"处理核心零改动"验不过——这是两条 AC 之间的隐藏冲突，现在定成本最低。
- **测试面**：现有 40 例全部在 HTTP / graph / client 层，DB 模式需新增四类——claim 并发、字段映射、写回事务回滚、优雅停机。其中**并发 claim 与事务回滚必须对真实 PG 测**（mock 测不出 `SKIP LOCKED` 与事务语义），`news_test` 已就绪、5 条 `queued` 已预置。建议 PRD 或设计阶段写明这部分测试依赖真实库，并计入 R-4 造数队列消耗——xiaobao 已提示队列耗尽需补跑 `seed_ai_queue_test.sql`，自测阶段就会开始消耗，不要等联调才发现队列空了。

**事实层刷新清单（R2 必做；非 Review 意见，但不刷新则定稿会带着过期前提）**

| # | PRD 现写 | 实际（coordination 07-25~07-26） |
|---|----------|----------------------------------|
| 1 | O-1「P0，阻塞定稿」；Review 计划定稿条件「O-1 必须先有结论」 | **已定案方案 A**（`a0cc00c`，契约订正 v1.1 + CHANGELOG 记行）。PRD 现有范围（ai 只产 `score_dimensions`、不算 `score_total`）**无需修改，是正确的**；O-1 应降为「已定案」，定稿条件随之解除 |
| 2 | O-5「契约小瑕疵，建议订正」 | **已订正**（`l1_status` 枚举 `completed` 已合并单行）→ 可关闭 |
| 3 | §5「`ai_worker` 角色与 GRANT ... **尚未确认已就绪**」 | **已就绪**：角色 + 列级 GRANT 逐列对照契约一致、越权拦截实测通过（`SELECT alerts` / `INSERT raw_items` / `UPDATE process_type` 均 permission denied）、触发器已建 |
| 4 | §5「schema 迁移是否已在测试库落地待确认」 | **已确认**：ai 可写四列 + 只读列在 `news_test` 全部存在 |
| 5 | DevOps 问题 4：造数依赖未列入 §5，会卡住部署就绪检查 | **已解决**：`seed_ai_queue_test.sql` 已交付并已跑，`news_test` 现有 5 条 `process_type='ai'` + `l1_status='queued'` |
| 6 | （PRD 未涉及） | **新增硬事实：系统当前只有 `x_twitter` 真实数据**（生产 757 / test 154，rss 与 jin10_flash 无 `raw_items`）。PM 已在 coordination 定下三类源验收分层（x_twitter 真实端到端 / rss / jin10_flash 仅单测 + 标注待补）——**这条必须写进 §3 验收标准**，否则 PRD 与对外承诺不一致 |
| 7 | （PRD 未涉及） | **两项新登记前置**：① 生产库 `news` 的 GRANT 未执行（上生产前置，不进 v0.2 部署就绪检查）② 造数队列会耗尽，需 xiaobao 补跑脚本 |
| 8 | §5 连接信息「Owner / DevOps 提供」 | 四要素已交付（`127.0.0.1:5432` / `news_test` / `ai_worker`，同机 localhost），**仅口令待 Owner 带外交付**——这是 ai 侧目前唯一剩余外部依赖 |

建议修改（按优先级）：

- **必改（阻塞）**：`tags_v2` 第五类冲突提报 C-10、订正前 AC-4 该字段不定稿（问题 1）；增补「处理进行中 `/health` 仍须 2s 内响应」AC + §5 写明本迭代不引入 async 改造（问题 2）。
- **必改（高）**：AC-7 删空条款、扩为三工具统一语义并写明期望产出（问题 3）；AC-2 / AC-8 判据分开写死（问题 4）；C-1 拆分——撤回 `source_item_url`、保留 `domain_tags`，并把"适配层须显式回填 `raw_content['url']`"写进 AC-2（问题 5）。
- **宜改（中）**：AC-4 补 `analysis` 空值规则、`context` 与 `needs_context` 的已知差异说明（问题 6）。
- **必做（非意见）**：按上表刷新事实层 8 项，其中第 6 项（三类源验收分层）属验收标准变更，必须进 §3。
- 建议 §4 或新增小节补工程切片与成本判断（当前 PRD 无任何成本信息，Architect 设计阶段的切分与 PM 的排期都缺依据）。

未构成问题、明确认可的部分：

- **适配层的工程成本被高估了，这是好消息**：v0.1 的 `run_task` 签名与 `main.py` 的薄入口，使"处理核心不感知数据来源"几乎是现成的。PRD §1 把它列为硬约束（而非"顺便解耦"）判断正确，但实现风险远低于文中的谨慎表述。
- **AC-2 的验收方式（两种数据源各跑通同一条用例）是对的**——验依赖方向而非代码形态，比"检查有没有抽象基类"有效得多；只是判据需按问题 4 写死。
- **把结构化 logging 并入本迭代而非跟着托管化顺延，从代码现状看是必须的**：现状零 logging，worker 模式下若不做，出问题时没有任何可查线索——这不是"观测性不足"，是"完全不可诊断"。
- **KB 空结果语义（D-3）纳入本迭代成本极低**：改动集中在 `news_l1.py` 三处同构判断，顺手一起修比单修 KB 更省，且能消除三工具语义不一致。
- **不做主链路算法变更、不改 HTTP 契约、不换模型**——从实现角度强烈认可：v0.2 要验的是"两种模式结果是否等价"，任何算法改动都会让比对失去基准，联调时无法归因。
- **O-1 按既有边界写范围的判断，事后被 xiaobao 侧证实为正确**（定案方案 A，契约笔误订正）。PM 当时选择"暂按既有边界 + 标 P0 阻塞"而不是"按 DB 契约先实现"，避免了一次返工。

### R3 — Developer 复审（2026-07-27）

- 结论：**未通过**（3 高 2 中 2 低，需 PM 修改后进入 R4）
- Review 范围：R1 六条意见的收敛复核（含 3 处被推翻判断）、async 改造后的工程成本重估、claim 与写回实现路径、验收可验证性
- 实读依据：`v0.2-prd.md` R3 全文、`v0.2.md`；实查 `grep -rn "httpx\.(get|post|Client|AsyncClient)" src/`（**全仓 4 处，与 AC-9.1 清单逐行一致**）、`grep -rn "\.invoke(" src/`（`tasks.py:64` 一处）、`tests/` 的 fake 实现与用例分布
- **前置声明**：本轮**不重提**任何以"成本最优"为由的方案。§0 准则（基础夯实优先于便捷）是 Owner 决策，R2 据此推翻我 R1 的三处推荐，我认可该方向——下文所有意见均在"async 地基进范围"这一既定前提内提出，属**补充而非推翻**。

**第一部分：R1 六条收敛复核（本轮指派任务）— 全部到位**

| R1 问题 | R3 收敛情况 | 判定 |
|---|---|---|
| ① `tags_v2` 第五类冲突 | C-10 闭合，`sentiment` 认定为起草笔误、第五类回归 `processing`，与 HTTP 契约一致；AC-4.6 已写死 | ✅ 到位 |
| ② 同步基线 vs 探活 vs 74~79s 阻塞 | 推翻我的线程方案，改 async 地基（US-8/AC-9）；**我提的可验证判据「处理中 ≥60s 时 `/health` 须 2s 内返回」被完整保留为 AC-9.3** | ✅ 到位，且比我的方案更彻底 |
| ③ AC-7 三点 | 全采纳：空条款已删（.4）、缺陷位置写明在 graph 三处而非 `kb.py`（.3）、三工具统一语义（.1/.2） | ✅ 完全到位 |
| ④ AC-2 / AC-8 判据互斥 | AC-2 改判"依赖方向"（含核心 diff 为空 + 结构合法性），AC-8 改判"输入等价前提下语义一致"并列出 6 项已知差异 | ✅ 到位 |
| ⑤ C-1 拆分 / URL 回填 | AC-2.4 保留"必须回填 `raw_content` URL 键"及静默失效的说明，并给了验证方式；URL 来源改为直读 `source_item_url` | ✅ 到位（我的构造方案被推翻，见下） |
| ⑥ AC-4 字段来源 | AC-4.6 逐字段表，`analysis`→NULL、`language`→`'zh'`、`id` 不写、新增 `published_at` | ✅ 到位 |

**对三处推翻我的判断，我的确认**：

1. **C-3「ai UPDATE 占位行」推翻我方推断——我接受，且认为这条比技术推断更重要**。占位行是 xiaobao 的产品硬约束（L0 通过后须立即前台可见），这是 ai 从数据库语义里**无论如何也推不出来**的信息。`INSERT ... ON CONFLICT (raw_item_id) DO UPDATE` 的写法我逐条核过：`raw_item_id` 唯一约束已有、`id` 走 DB 默认不显式写、`score_total` 不出现在 SET 子句即可守住语义边界、占位行缺失时退化为 INSERT——**实现上无坑，可以照写**。
2. **Q-4「rss 无原文链接」被推翻——这条是我的错，且错法值得记**。我依据对方 R-5 字段表得出"确实没有"的否定结论，而该表只覆盖 `content` jsonb 的键、未覆盖 `raw_items` 一级列，`source_item_url` 一直存在。我当时还把它写成"已知限制"结案。**教训我认领：对"对方确实没有某样东西"的否定结论，不能靠对方交付物的沉默来证实，必须回问一次。** §6 的复盘记录准确。
3. **C-4 退避根因（我方现象推演正确、根因归错）**：AC-5.2 改为读 `tasks.run_after`、禁止 ai 自行用 `updated_at + backoff` 计算，我完全同意——两套退避真源在卡死回收介入后必然漂移，这个风险比我 R1 描述的更大。

**第二部分：本轮新问题（全部落在 R2/R3 新增的 async 范围内）**

1. **高｜HTTP 模式的并发退化没有任何验收覆盖，而 PRD 自己在 US-8 里点出了这个风险。** US-8 背景写得很准确：「FastAPI 本身是 async 框架，同步端点靠线程池兜底，HTTP 模式下侥幸无碍」。但推论只写了一半——线程池兜底**不只是在 worker 模式下消失**：一旦 `main.py` 的端点由 `def` 改为 `async def`（AC-9.1 要求"沿调用链改造至 `main.py` 端点"），FastAPI 就**不再把它派发到线程池**，而是直接在 event loop 上执行。此时若 async 改造有任何一处遗漏（某个工具仍是同步 `httpx`、某个 LangGraph 节点仍是同步且做 IO），HTTP 模式会从"v0.1 的线程池并发"退化成"event loop 串行"——**并发请求排队，比 v0.1 更糟**。
   AC-1 要求 HTTP 模式「行为与 v0.1 等价」，但没说清"等价"的维度：单请求输入输出等价是一回事，并发行为等价是另一回事，而后者恰恰是 async 改造最可能破坏的。AC-9.3 只给 DB 模式写了探活验收，HTTP 模式这一侧是空的。
   建议增补一条可验证 AC（与 AC-9.3 对称）：**HTTP 模式下并发发起 N 个请求（N≥3，LLM 走 mock 固定延时），总耗时不应显著超过单条耗时**（判据可取 < 1.5×单条），以此证明未串行化。这条同时是 AC-9.1「改造彻底性」的唯一客观检验——只靠 code review 看不出漏了哪个同步点。

2. **高｜AC-9.4 的「行为不变」按现写法不可验证，是循环论证。** AC-9.4 要求「v0.1 已验证的处理逻辑在 async 改造后行为不变，现有 40 例单测全部通过（框架层允许改造为 `pytest-asyncio`，断言语义不得放宽）」。问题在于：**这些单测本身必须被改写才能跑**。实查：`tests/test_news_l1.py:44` 的 `FakeClient.complete_json`、`:64-73` 的 `NullTools` 四个方法、`tests/test_news_l1_tools.py:16` 的 `FakeTools` 四个方法，全部是同步签名；协议改 async 后这些 fake 必须一起改 async，`TestClient` 也要换成 `ASGITransport` + `AsyncClient`。于是"用改写后的测试证明改写后的代码行为不变"——**改错了断言、或改 fake 时无意放宽了行为，测试同样会绿**。"断言语义不得放宽"是正确的意图，但不是可执行的判据（谁来判定没放宽？）。
   建议把回归基线**外部化**，增补为 AC-9.4 的执行方式：**async 改造动手前，先在 v0.1 基线上录制黄金样本**——固定若干组 `L1Input` + 固定的 mock LLM 响应，跑出 `L1Output` 的 JSON 快照落盘；改造后用同一组输入与同一组 mock 重跑，断言输出**逐字段一致**。这样回归判据独立于测试代码的改写，才真正验得住"行为不变"。O-8 已经写了「回归基线如何固定（如先录制 v0.1 的输出快照再改造）」——那句话是对的，但它待在开放问题里、归设计阶段，而 AC-9.4 是验收标准；**验收标准不能依赖一个尚未决定的方法**。建议把黄金样本从 O-8 提升进 AC-9.4，作为该条的验证方式写死。
   补一个量级事实供判断：涉及 fake 改写的用例约 21 例（`test_news_l1` 10 + `test_news_l1_tools` 10 + `test_health` 1，静态计数），`test_llm_client` 10 例与 `test_config_providers` 5 例大部分不碰 IO 协议。即**过半用例要动**，不是"框架层小改"。

3. **高｜AC-3.6 的 C-6 行锁实证要求「实现前」完成，但口令注入排期在部署阶段，两者时序冲突，会卡住实现阶段。** AC-3.6 写「**实现前**须以 `ai_worker` 身份在 `news_test` 实际执行一次 claim SQL」；而 §5 把口令注入归为「待注入（归 DevOps 在**部署阶段**执行）」，并明确「到位前无法连库，**阻塞联调冒烟与 C-6 行锁实证**」。两处对同一件事的时点要求相反。
   这不是文字问题，是实打实的返工风险：xiaobao Architect 已预判「列级授权很可能不满足 `FOR UPDATE`」。若实证失败，claim 的实现方式要变（改表级 GRANT，或改用带条件的原子 `UPDATE ... RETURNING`），**而 claim 是 worker 循环的地基**——等到部署阶段才发现，前面按 `FOR UPDATE SKIP LOCKED` 写的 claim、事务边界、并发测试都要重做。
   好消息是解除成本极低：§5 已把口令交付简化为「ai 侧 DevOps 在同机直接读 `/root/.secrets/...` 写入 `.env`」，不需要 Owner 人肉转交。建议明确：**把口令注入前移为「实现阶段开工前置」而非部署阶段**（一次性动作，Owner 只需授权 DevOps 访问该文件），并在 AC-3.6 保留 fallback 表述——若实证失败，按 xiaobao 承诺改表级 GRANT，ai 侧 claim 实现不必预先兼容两套。

4. **中｜§8 成本表把我 R1 的核算原样搬进 R3，但那份核算的前提（线程方案、处理核心零改动）已经被 §0 准则推翻了，现在会误导排期。** §8 开头「最大红利：处理核心的解耦几乎是现成的」——这句话我 R1 说过，在**线程方案下**完全成立。但在 async 方案下它只对了一半，需要拆开说：
   - **仍然成立的**：解耦的*依赖方向*是现成的。`run_task` 签名与传输层无关、`main.py` 只调一行，协议切点不用挪——这是 AC-2 的红利，不受 async 影响。
   - **已经不成立的**：处理核心的*代码*不再零改动。async 改造要碰的是——4 个出网调用点、`AIClient` 协议（`client.py:35` `complete_json`）、`NewsTools` 协议的 4 个方法（`base.py:37-40`）及其 `DefaultNewsTools` 实现、6 个 LangGraph 节点中做 IO 的 4 个、`tasks.py:64` 的 `graph.invoke → ainvoke`、`run_task` 自身、`main.py` 端点，外加过半单测。这是**全仓 IO 相关代码的一次性翻新**，不是"改 4 个点"。AC-2.2 已经很清楚地把这部分定性为"地基变更、不计入 AC-2"，但 §8 的叙述没跟上。
   
   建议 §8 按 async 前提重写这段，并把表格第 1 行的"风险最高"补上量级说明（涉及文件数与用例数），否则 Architect 做设计切分、PM 排期都会低估。**这不是要求缩范围**——按 §0 准则该做就做，但成本要报准。

5. **中｜`l0_label` 的空值兜底未写，适配层容易产出 `[None]` 或 `['']`。** AC-8.2 说 `domain_tags` 按单值处理（`[l0_label]`）。但 `l0_label` 是 varchar，NULL 与空串都可能出现（L0 未分类 / 分类失败）。若适配层直接包成单元素数组，`domain_tags` 会变成 `[None]` / `['']`，而它会流进 `_build_kb_query` 的 `domain_tags=inp.domain_tags or None`（`news_l1.py:206`）与 prompt——`['']` 是**真值**，不会被 `or None` 拦掉，会把一个空标签传给 xiaobao 的 KB 检索接口。建议 AC-8.2 或 AC-2 补一句：`l0_label` 为 NULL/空串时映射为 `[]`。

6. **低｜O-9 驱动选型的 Developer 输入：建议 psycopg3(async) 而非 asyncpg。** 两者都满足 AC-9.2。倾向 psycopg3 的理由与 §0 准则同向（不是成本理由）：① asyncpg 不走标准 DBAPI 且占位符是 `$1`，psycopg3 用 `%s`——若 C-6 实证失败需要改写 claim SQL（换 `UPDATE ... RETURNING` 等），psycopg3 下 SQL 可直接沿用惯例、迁移面小；② psycopg3 的同步与 async API 同构，将来若某处需要同步工具脚本（如造数校验、运维排查）不必引第二个驱动；③ 服务端游标与 `COPY` 等能力对后续 RunRecord 持久化（v0.3）更友好。asyncpg 的性能优势在本场景无意义——瓶颈是 74~79s 的 LLM 调用，不是数据库往返。
7. **低｜LangGraph 只有做 IO 的节点需要 async，纯计算节点不必改。** `ingest_context` 与 `normalize_output` 是纯内存处理（无 IO），`kb_search`/`link_read`/`web_search`/`llm_process` 才有出网调用。LangGraph 的 `ainvoke` 支持混合节点。**这不是"图便捷"**——把纯计算节点改成 `async def` 不带来任何并发收益，反而多一层协程调度；建议设计阶段按"有 IO 才 async"划线，避免把"全面 async"理解成"每个函数都加 async"。

**第三部分：工程成本重估（本轮指派任务）**

按 async 进范围后重估，替代我 R1 的核算（该核算的线程方案前提已失效）：

| 块 | 改动面（实查） | 相对 R1 变化 |
|---|---|---|
| async 地基 | 4 个出网点 + 2 个协议（`AIClient.complete_json`、`NewsTools` 4 方法）+ `DefaultNewsTools` + 4 个 IO 节点 + `tasks.py:64` `invoke→ainvoke` + `run_task` + `main.py` 端点 + 约 21 例单测的 fake 与客户端改写 | **R1 不存在此块**（新增，且为最大块） |
| 数据源协议 + DB 实现 | 协议定义 + DB/HTTP 两实现 + 三类 source type 映射 | 略增（R1 是两个映射函数，现为显式协议） |
| 出向映射 + 写回事务 | `ON CONFLICT DO UPDATE` + 三段式事务 | 持平（C-3 定案后无歧义） |
| claim + worker 循环 | claim SQL + 退避（读 `run_after`）+ 批量上限 + 优雅停机 + 启动自愈 | 持平 |
| 结构化 logging | 注入式，横切 graph 各节点 | 持平（AC-6.4 已定形态，风险下降） |

**关键判断：本迭代的风险重心已从"契约缺项"整体转移到"async 回归"。** R1 时的阻塞全部是外部契约缺项（现已闭合 9/10）；R3 之后 ai 侧自身的最大不确定性是 async 改造的回归——O-8 标 P0 且自评"风险在回归不在重写"，这个判断我确认准确。正因如此，上述高②（黄金样本回归基线）不是锦上添花：**它是这个 P0 风险唯一的客观兜底**。

建议修改（按优先级）：

- **必改（高）**：补 HTTP 模式并发退化的验收（问题 1）；把黄金样本回归基线从 O-8 提升进 AC-9.4 作为验证方式写死（问题 2）；把口令注入前移为实现阶段开工前置、解除 AC-3.6 与 §5 的时序冲突（问题 3）。
- **宜改（中）**：§8 按 async 前提重写"处理核心零改动"的表述并补量级（问题 4）；补 `l0_label` 空值兜底（问题 5）。
- **可选（低）**：O-9 记入 Developer 的驱动选型输入（问题 6）；async 划线按"有 IO 才 async"（问题 7）。

未构成问题、明确认可的部分：

- **AC-9.1 的 IO 点清单经实查完全准确**：`grep -rn "httpx\.(get|post|Client|AsyncClient)" src/` 返回且仅返回 `llm/client.py:176`、`tools/kb.py:50`、`tools/web_search.py:24`、`tools/link_reader.py:38` 四处，行号逐一对得上。PRD 写到这个精度，实现阶段不会漏点。
- **§0 准则的三处裁定，我事后认为都是对的**，尤其裁定 1：我 R1 推荐线程方案时把"处理核心零改动"当成了主要权重，但 v0.3 的工具并发与批内并发确实会把同步基线再翻一次，届时 worker 要推倒重来。**线程方案是把债推到形态更复杂的时候还**，这个判断准则比我的成本判断更长远。
- **AC-3.1 只 claim `tasks` 不扫 `raw_items`、且明确 ai 不做孤儿探测**——正确。孤儿窗口是 xiaobao 的事务缺陷，ai 侧加探测逻辑是替对方的 bug 长期买单，且会与其修复后的行为重叠。
- **AC-5.2 禁止 ai 自行计算退避**——强烈认可。两套退避真源在卡死回收介入后必然漂移，而这类漂移在联调时表现为"偶发重复处理"，是最难查的一类。
- **AC-8.2 把 `score_total` 保持 NULL 显式写成"不是 ai 的缺陷"**——这条对联调极有价值。排序沉底 + 徽章显示 0 是很显眼的现象，不预先写明，联调时第一反应必然是查 ai 写回。
- **AC-10.3「不得声称三类均已真实验收通过」**——这条守住了完成度的诚实性，赞同保留原样。
- **§6 的复盘记录（3 处被推翻判断）连"逻辑正确但缺产品约束""现象对根因错"都分开写了**，没有把"错了"混成一团。这种留痕对后续跨服务协作的价值高于结论本身。

**订正（同日追加）——我的问题 3 只说到表层，按 DevOps R3 问题 1 合并处理：**

DevOps 同日并行复审指出：即便口令今天到位，C-6 实证仍然做不了，因为 **ai 在那台服务器上根本没有运行环境**。其三条实查事实我已独立复现，全部成立——`uname -s` = **Darwin**（本机 macOS）、`ls -d /root` **不存在**（PRD §5 的 `/root/.secrets/...` 是 Linux 服务器路径）、本机 `5432` 与 `8100` 均**无监听**，且 `scripts/` / `deploy/` / `.env` 均不存在。

因此**我的问题 3 建议应作废并入其问题 1**：需要前移的不是"口令注入"这一个动作，而是**整个服务器部署环境准备**（代码上服务器、Python 环境、`.env` 落点、日志落点、启动方式）。我原建议若被按字面采纳（只把口令注入前移到实现阶段），AC-3.6 的 C-6 实证**仍然无处执行**，时序冲突只解了一半。

我的问题 3 中仍然成立的部分：**AC-3.6「实现前」与 §5「部署阶段」的时序冲突本身确实存在**，且后果判断不变——C-6 若拖到部署阶段才失败，claim / 事务边界 / 并发测试要返工，因为 claim 是 worker 循环的地基。只是解法要按 DevOps 的口径放大到环境准备层面。

### R3 — DevOps 复审（2026-07-27）

- 结论：**未通过**（3 高 2 中，需 PM 修改后 R4；三条高均为**增补**而非推翻，修改量小）
- Review 范围（PRD §Review 状态指派）：运行形态 / 探活与优雅停机 / 凭据注入 / async 后的部署形态；并接手 C-6 行锁实证的执行安排
- 实读依据：`v0.2-prd.md` R3 全文、`v0.2.md`、`INDEX.md`；本项目 `.env.example` / `.gitignore` / `requirements.txt` / `src/agent_hub/{config,main}.py`；实查 `uname -s`（**Darwin**）、`ls -d /root`（**不存在**）、`ls .env`（**不存在**）、`lsof 8100/8001/5432`（**三者均无监听**）、`ls scripts/ deploy/`（**均不存在**）、v0.1 部署形态记录（`v0.1.md:46`）
- **与 Developer R3 的关系（本轮同日并行）**：其问题 3 指出 AC-3.6「实现前须完成 C-6 实证」与 §5「口令注入归部署阶段」的时序冲突，结论是把口令注入前移为实现阶段开工前置。**方向我确认成立，但那只是这件事的表层**——我的问题 1 是同一处的下一层：即便口令今天到位，实证仍然做不了，因为 **ai 在那台服务器上根本没有运行环境**。两条应合并处理：前移的不只是口令注入，而是整个「服务器部署环境准备」。本轮其余问题与 Developer / Architect 无重叠。

**R1 我方 7 条的收敛核对——全部收敛，且多数强于当初的建议：**

| R1 问题 | R3 落点 | 判定 |
|---|---|---|
| 1 探活面缺失 | AC-9.3：处理进行中（≥60s）`/health` 须 2s 内返回，含 `mode` + `last_poll_at` | ✅ **强于原建议**（原只要求"必须有"，现成了可验证的抗阻塞断言） |
| 2 优雅停机与残留锁 | AC-5.7 优雅停机 + AC-5.6 启动自愈 | ✅ 收敛（但宽限期取值有缺口，见问题 2） |
| 3 DB 连接异常带 DSN 脱敏 | AC-6.2 + 可验证断言（错误口令连接、断言日志无口令子串） | ✅ 逐字采纳 |
| 4 造数依赖 | §5 已就绪：`seed_ai_queue_test.sql` 已跑，5 条 `queued` | ✅ 闭合 |
| 5 日志 sink | AC-6.3 stdout + 灰度期人工轮转 | ✅ 逐字采纳 |
| 6 托管化顺延代价 | §4：**只能人工看护灰度、不得无人值守** | ✅ 逐字采纳 |
| 7 DB 驱动与 `.env.example` | §5「新增技术依赖」+ 变量按字段拆分 | ✅ 采纳（AC-9.2 还追加了"驱动须与 async 一致" |

问题：

1. **高｜§5 称「唯一剩余外部依赖是口令」不成立——ai 的运行位置要从本机迁到服务器，整个部署环境缺失，且 C-6 实证无处执行。**

   三条实查硬事实对不上：① 本机是 **macOS**（`uname -s` = Darwin），**`/root` 不存在**——PRD §5 写的 `/root/.secrets/ai_worker_news_test.pw` 是 **Linux 服务器**路径；② v0.1 的全部部署形态是**本机** `127.0.0.1:8100` uvicorn nohup（`v0.1.md:46`），xiaobao 也在本机 `8001`；③ 本机 `5432` **无监听**，即 `news_test` 不在本机。

   把三条并起来：PRD 说的「与 xiaobao **同机**」「`host=127.0.0.1`」指的是**那台 Linux 服务器**，而 ai 至今只在开发机上跑过。也就是说 v0.2 隐含一个 PRD 从未写出的形态变更——**ai 的运行位置从开发机迁移到服务器**。这不是部署阶段的执行细节，理由有三：

   - §5 明写「**唯一**剩余外部依赖 = 口令」，这句话是**事实性错误**。还差：服务器上的代码 checkout、Python 运行环境、`.env` 落点、进程启动方式、日志落盘路径（AC-6.3 说"由 DevOps 指定"，指定在哪台机器上？）。项目内 `scripts/` 与 `deploy/` **均不存在**，v0.1 也没留下任何可复用的部署脚本。
   - PRD 把 C-6 行锁实证写成 **AC-3.6「实现前须完成」**，而实证要以 `ai_worker` 身份连 `127.0.0.1:5432`——只能在服务器上做。前置环境不存在，这条 AC 当前**不可执行**。
   - 它改变部署就绪检查的通过条件（v0.1 是"本机 curl /health + 一次真实 POST"，v0.2 变成"服务器上 worker 连库跑通闭环"），而 v0.2 至今没有任何部署就绪检查的定义。

   建议：§5 把「部署位置与运行环境」列为**与口令并列的第二项待办**（归 DevOps），至少写清：目标服务器、ai 代码上服务器的方式、`.env` 与日志落点、启动命令。同时把「唯一剩余外部依赖」的措辞改掉。**这条不阻塞设计阶段开工**（Architect 定协议与驱动选型不依赖部署位置），但阻塞 C-6 实证与任何联调冒烟。

2. **高｜AC-5.7 的优雅停机宽限期只约束了上限、没约束下限，也没覆盖 ASGI 与托管层——按字面配置一个"合规"的值，优雅停机会完全失效。**

   AC-5.7 写「宽限期上限须小于 1800s」。但真正决定成败的是**下限**：单条 74~79s、单批 N ≤ 8 且批内串行（§4 明写 v0.2 批内串行），则「处理完当前条目后退出」的最坏等待 ≈ 632s。若把宽限期配成 60s 或 90s——**完全符合 AC-5.7 的字面要求**——则每次停机都在处理中途被强杀，AC-5.7 的验收（"断言锁被释放"）在真实批量下必然失败，残留锁照样产生。

   更麻烦的是宽限期在真实部署里由**三层**共同决定，PRD 只约束了最里面一层：
   - 应用层：worker 自己的宽限期（AC-5.7 已写）
   - **ASGI 层**：DB 模式进程是 uvicorn 托着 `/health` + worker 循环（AC-9.3 要求同进程），uvicorn 的 graceful shutdown 超时会先于 worker 结束进程
   - **托管层（v0.3）**：systemd 的 `DefaultTimeoutStopSec` 通常为 **90s**，远小于 632s——托管化落地当天，优雅停机就会被 systemd 按 90s 强杀掉

   建议：AC-5.7 补下限与跨层一致性——「宽限期须 ≥ 单批最长处理时间（N × 单条耗时上限），且 < 1800s；应用层、ASGI 层、托管层三处须配一致值」；§4 托管化顺延条目点名 `TimeoutStopSec` 为 v0.3 的具体前置。验证方式也应据此加严：**在 N > 1 的批处理进行中**发 SIGTERM（而非单条），断言整批锁均被释放。

3. **高｜回滚被 US-4 / AC-1 描述成 ai 单方"改配置重启"，实际是 ai 与 xiaobao 的双侧协同动作，且有顺序要求——故障时按 PRD 字面执行会把问题放大。**

   AC-8.2 自己给出了证据：xiaobao 侧存在「database 模式」与「HTTP/内建 L1 路径」两条路（其 `calcScoreTotal` 只挂后者，`l1-processor.ts:171`）。也就是说 DB 模式下 **xiaobao 不再发 HTTP 请求**，它改为入库 + 建 task。那么 ai 单方"切回 HTTP 模式"的实际后果是：ai 不再 claim，而 xiaobao 仍在往 `tasks` 里堆 `queued`——**队列无人处理、静默积压**，比不回滚更糟（DB 模式至少还在处理）。

   正确的回滚是双侧、有序的：**先** xiaobao 切回 HTTP 集成模式（停止建 task、恢复主动调用），**后** ai 切 `RUN_MODE=http` 重启；反序会产生一个"两侧都不处理"的窗口。此外回滚瞬间已 claim 的在途条目仍需走优雅停机（问题 2）或等 1800s 回收。

   建议：US-4 与 AC-1 补明回滚是**双侧协同**、写出顺序与前置沟通对象；并在 §5 或 §7 登记一条——回滚预案需与 xiaobao DevOps 事先对齐触发条件与联系方式，不能等故障时现商量。**这条对灰度期的实际风险最大**：v0.2 是人工看护灰度（§4），出问题时第一反应就是回滚，而当前 PRD 给的回滚动作是错的。

4. **中｜`last_poll_at` 没有陈旧阈值定义，运维拿到这个字段仍判不出死活。**

   AC-9.3 让 `/health` 暴露 `last_poll_at`，方向对（是我 R1 提的）。但没定义"多旧算死"。而 DB 模式下这个判断并不平凡：worker claim 一批 8 条后进入处理，若轮询与处理是同一循环，`last_poll_at` 在整批处理期间（最坏 632s）都不更新——**正常工作的 worker 看起来和卡死的 worker 一样**。运维若按"超过 60s 未轮询即判死"处理，会重启一个健康进程并制造残留锁（正是 Developer R1 问题 2 担心的那类反噬）。

   建议：AC-9.3 补一条——`/health` 除 `last_poll_at` 外还须暴露**在途处理进度**（`in_flight` 条数与 `current_item_started_at`，或等价字段），使"活着且在处理"与"活着但卡住"可区分；并写明 `last_poll_at` 的陈旧容忍上限 = 单批最长处理时间 + 轮询间隔。这是部署就绪检查与 v0.3 探针配置的判读依据，现在定成本最低。

5. **中｜「同机直读」消除了传输风险，但引入了口令副本漂移，且 root 授权机制未定。**

   新方案（Owner 2026-07-27）比 R1 的带外交付更好，我认可——口令不经对话、不经人肉转交，泄漏面确实更小。但两点未定：

   - **授权机制**：源文件是 `chmod 600` + **root only**，ai DevOps 要读它必须有 root/sudo，或由 Owner 改属主/加 ACL。§5 只写「Owner 只需授权访问该文件」，没说授权形式。这直接决定注入动作能否执行，也决定 ai DevOps 在该服务器上的权限面有多大（涉及最小权限原则，不该顺手给全量 sudo）。
   - **副本漂移**：注入后口令存在**两份**（`/root/.secrets/*.pw` 与 ai 的 `.env`），无任何同步机制。xiaobao DevOps 轮换源文件后，ai 的 `.env` 不会跟随，worker 会在下次重启时连库失败——而失败点距离变更点很远，很难归因。R1 的带外交付方案里每次轮换都是显式人工动作、必然重走一遍；新方案"直接读文件"看起来自动化，实际仍是**一次性快照**，反而更容易被误认为实时读取。

   建议：§5 补两句——① 授权形式明确为「按需 sudo 读取该文件」或「Owner 代为复制到 ai 可读路径」，二选一，不给全量 sudo；② 明写 `.env` 是**快照**，口令轮换必须重新执行注入 + 重启 worker（`config.py:16` 的 `load_dotenv()` 仅启动时读一次，无热加载），并请 xiaobao DevOps 在轮换前通知。

**C-6 行锁实证 — DevOps 执行安排（PRD §Review 状态指派本角色接手）**

前置：口令注入完成 + 服务器上可执行 `psql`（问题 1 的环境到位后即可做，**不必等 async 改造、不必等任何代码**）。

执行步骤（全程在显式事务内，最后 `ROLLBACK`）：

| # | 动作 | 判定 |
|---|------|------|
| 1 | 以 `ai_worker` 连 `news_test`，`BEGIN;` | 连接成功 |
| 2 | `SELECT id FROM tasks WHERE type='l1_ai_process' AND status='queued' AND run_after <= now() ORDER BY created_at LIMIT 1 FOR UPDATE SKIP LOCKED;` | **核心判定**：不报 `permission denied` 即列级 GRANT 支撑行锁；报错则 C-6 结论为"需表级 UPDATE" |
| 3 | 另开一个会话重复步骤 2 | 应拿到**不同行**或空结果（`SKIP LOCKED` 生效、并发不重复） |
| 4 | `UPDATE tasks SET status='running', attempt=attempt+1, locked_by='probe', locked_at=now(), run_after=run_after WHERE id=<上一步 id>;` | 验证 AC-3.2 的全部可写列（含新 GRANT 的 `run_after`）确实可写 |
| 5 | 越权对照：`UPDATE tasks SET type='x' WHERE id=...;` | 应 `permission denied`（确认权限边界仍在） |
| 6 | **`ROLLBACK;`** 两个会话均回滚 | 队列不被消耗 |

**纪律（重要）**：`news_test` 只有 **5 条**预置 `queued`（§5 届时前置 2 明确会耗尽），实证必须全程事务内回滚，**不得**把任何一条推进到 `running`——否则自测阶段还没开始，冒烟样本就少一条。

**结论回帖分工**：AC-3.6 要求"实证结论须回帖 coordination"。我出结论与证据（SQL 输出原文，**不含口令**），**回帖动作归 PM**（跨项目 `communications/` 写入不在 DevOps 权限内，见 `role-devops.md` §跨项目协作）。若步骤 2 失败，需 xiaobao 对 `tasks` 改授表级 UPDATE——那是新的跨项目请求，同样走 PM 转达。

未构成问题、明确认可的部分：

- **R1 我方 7 条全部收敛，其中 3 条被写得比原建议更强**（AC-9.3 的抗阻塞探活断言、AC-6.2 的错误口令验证法、AC-5.7 的优雅停机验证法）。R3 把"建议"变成了"可验证条款"，这是 PRD 质量的实质提升。
- **async 地基进本迭代（§0 裁定 1 / US-8 / AC-9）从部署视角强烈认可。** Developer R1 问题 2 指出的"同步阻塞 + 同进程探活"死结，线程方案只是把它藏起来；async 是真解。AC-9.3「处理进行中 `/health` 仍须 2s 内返回」尤其关键——它把"探活是否真的可用"变成了一次性验死或验活的集成测试，而不是等上线后才发现探针在说谎。
- **AC-9.2「DB 驱动须与 async 地基一致、不得同步驱动 + async 核心」**，避免了最典型的一类混合形态事故，认可。
- **§4 把托管化顺延的代价显式写成「只能人工看护灰度、不得无人值守」**——这正是我 R1 要的效果：让"无托管"成为**已知取舍**而非遗漏，部署就绪检查时可据此判定，不必再争论。
- **AC-3.5 把批量上限不变式定性为"正确性约束，非调优建议"并写进 AC**（我 R1 提出、Architect 确认），处理得当。N ≤ 8 在 §4「批内串行」的前提下仍成立。
- **口令纪律写得干净**：不进 git / coordination / `docs/` / 会话明文，四条都点到了。`.gitignore:5` 覆盖 `.env` 已复核属实。
- **§5「届时前置」两条（生产 GRANT 不假定就绪、造数队列会耗尽）** 是很好的运维前瞻——尤其第 1 条明确"v0.2 部署就绪检查不假定生产已就绪"，避免了把测试环境的通过误当成生产就绪。

建议修改（按优先级）：

- **必改（高）**：§5 增列「部署位置与服务器运行环境」为第二项待办并订正"唯一剩余外部依赖"措辞（问题 1）；AC-5.7 补宽限期下限与三层一致性、验证方式改用 N > 1 批处理（问题 2）；US-4 / AC-1 补明回滚为双侧协同动作及顺序（问题 3）。
- **宜改（中）**：AC-9.3 补在途处理进度字段与 `last_poll_at` 陈旧阈值（问题 4）；§5 补口令授权形式与"`.env` 是快照、轮换须重注入 + 重启"（问题 5）。
- C-6 实证安排已在上节给出，可在环境到位后立即执行，**不阻塞设计阶段**。
