# v0.2 迭代记录

## 概览
- 目标：承接 coordination REQ-003 —— news-l1 集成模式从 HTTP 同步扩展为数据库契约边界异步解耦（轮询 worker + 数据源适配层 + 双模式灰度），并入结构化 logging 与 KB 空结果语义修正（v0.1 遗留 D-3）
- 开始时间：2026-07-04（2026-07-25 Owner 拍板范围重排，主线改为 REQ-003）
- 关闭时间：待定
- 最终状态：进行中
- 类型：标准迭代
- 当前阶段：PRD 阶段（**R4 待三方复审**；R1/R3 三方均未通过，R2 未经 Review 即被外部事实推进）
- 收尾状态：进行中（迭代未关闭）

### 范围重排记录（2026-07-25）
- 原范围：稳定性六项（结构化 logging / 服务托管化 / KB 空结果语义 / 工具并发 / RunRecord 持久化 / 多 provider 生产验证），PRD 起草于 2026-07-04。
- 重排原因：v0.2 PRD 起草比 coordination REQ-003 提报（2026-07-05）早一天，未包含 REQ-003；REQ-003 是运行形态级变更（HTTP 服务 → 轮询 worker），托管化 / 工具并发 / RunRecord 三项的形态均依赖新架构，先做外围会返工。xiaobao v0.6.1 侧前置产出已全部就绪并等待 ai 承接（已等 20 天）。
- 重排决策：Owner 2026-07-25 拍板「v0.2 重排，REQ-003 为主线」。
- 顺延项（转 v0.3 或部署阶段）：服务托管化、工具调用并发化、RunRecord 持久化、多 provider 生产验证；已登记 `INDEX.md` 跨任务待办。
- 并入项：结构化 logging（worker 模式唯一观测面，本迭代必做）、KB 空结果语义 D-3（低成本、xiaobao 已等）。
- 重排前的 PRD 版本未经任何 Review（R1 待 Review 状态挂 21 天），无 Review 意见需继承，故按 R1 重写而非推进轮次。

## 阶段门禁

> Bootstrap 初始化、迭代关闭确认和收尾归档不是迭代内的阶段，而是独立的非角色机制。触发时机和执行规则见 `docs/baseline/mechanisms.md`。

### PRD 阶段
| 轮次 | 产出方动作 | 指定 Review 方 | Review 结果 | 阶段状态 |
|------|------------|------------------|-------------|----------|
| R1 | PM 初版（`v0.2-prd.md`，稳定性六项，2026-07-04） | Architect、Developer | 未 Review（范围重排作废） | 已作废 |
| R1 | PM 重写（`v0.2-prd.md`，主线 REQ-003，2026-07-25 范围重排） | Architect、Developer、DevOps | DevOps 未通过（2026-07-25，4 高 2 中 1 低）；Architect 未通过（2026-07-25，3 阻塞 3 高 3 中 1 低）；Developer 未通过（2026-07-26，2 阻塞 3 高 1 中 + 事实层刷新 8 项） | 三方齐·均未通过 |
| R2 | PM 按三方 R1 意见 + **Owner 核心原则**修改（2026-07-26） | Architect、Developer、DevOps | 未 Review（被外部事实推进，直接出 R3） | 已作废 |
| R3 | PM 按 xiaobao 三方答复收敛（2026-07-27，契约升 v1.3 + 3 列 GRANT，4 条阻塞全闭） | Architect、Developer、DevOps（三方复审，不减方） | Developer 未通过（2026-07-27，3 高 2 中 2 低；R1 六条已全部收敛）；DevOps 未通过（2026-07-27，3 高 2 中；R1 七条已全部收敛）；Architect 未通过（2026-07-27，4 高 2 中 2 低；R1 十条已全部收敛，C-3 反转已独立核证） | 三方齐·均未通过 |
| R4 | PM 按三方 R3 复审收敛（2026-07-27，10 高 + 6 中低全部处置，新增 C-11~C-13） | Architect、Developer、DevOps（三方复审，不减方） | **Developer 通过**（2026-07-27，2 中 2 低均不阻塞）；**Architect 通过（附条件）**（2026-07-27，1 高 3 中 3 低；附条件 = 黄金样本四类路径覆盖须于设计阶段开工前经 Change Note 补进 AC-9.4）；DevOps 待 Review | Review中 |

### 设计阶段
| 轮次 | 产出方动作 | 指定 Review 方 | Review 结果 | 阶段状态 |
|------|------------|------------------|-------------|----------|

### 实现阶段
| 轮次 | 产出方动作 | base_commit | head_commit | 指定 Review 方 | Review 结果 | 验证 | 阶段状态 |
|------|------------|-------------|-------------|------------------|-------------|------|----------|

## Review 记录

| 日期 | 阶段/轮次 | Review 方 | 结论 | 详情 |
|------|-----------|-----------|------|------|
| 2026-07-25 | PRD R1 | DevOps | 未通过（4 高 2 中 1 低，需 PM 修改后 R2） | [v0.2-prd.md](v0.2-prd.md) §Review 记录 · R1 — DevOps Review。高：① DB 模式探活面缺失 ② 优雅停机/回滚残留 `processing` 锁无验收 ③ AC-6 未覆盖 DB 连接异常带 DSN 的口令泄漏路径 ④ 测试库造数依赖未列前置（ai 对 `raw_items` 只读，无法自行造数，卡部署就绪检查）。另给出 O-7 凭据注入结论与 O-3 运维侧约束（单批 N ≤ 8、轮询 10~30s、灰度期单实例）。 |
| 2026-07-25 | PRD R1 | Architect | 未通过（3 阻塞 3 高 3 中 1 低，需 PM 修改后 R2） | [v0.2-prd.md](v0.2-prd.md) §Review 记录 · R1 — Architect Review。阻塞：① DB 模式输入构造缺口（`domain_tags` / `source_item_url` 不在 ai 可读列、预取上下文无提供方）→ AC-8「两模式等价」不可达 ② `tasks.status` 枚举无真源 → AC-4/AC-5 不可实现 ③ `processed_news` INSERT/UPDATE 语义与 `news_positions` 触发器时序未定。高：④ 契约退避间隔未进 claim 条件（失败条目会被立即重领）⑤ 批量上限不变式属正确性约束、须写进 AC-3 ⑥ claim 原子性在列级 GRANT 下可实现性未验证。结论：O-1 支持方案 A（`score_total` 归 xiaobao）；O-4 定为进程级 + DB 模式仍监听 `/health`；O-2/O-6 给设计阶段倾向。新增 9 条契约缺项（C-1~C-9）需 PM 转达 xiaobao。 |

| 2026-07-26 | PRD R1 | Developer | 未通过（2 阻塞 3 高 1 中 + 事实层刷新 8 项，需 PM 修改后 R2） | [v0.2-prd.md](v0.2-prd.md) §Review 记录 · R1 — Developer Review。阻塞：① `tags_v2` 第五类两份契约冲突（DB 契约要 `sentiment`，HTTP 契约与 ai 实现均为 `processing`），AC-4 写回内容无法确定，且 `processing` 承载的降级/provider 标识是 worker 模式唯一结构化线索 → 提 C-10 ② 代码零 async（实查 0 命中）下，Architect O-4「DB 模式仍监听 `/health`」+ DevOps 探活要求 + 74~79s 同步阻塞三者同进程不可同时成立，会因假死误判触发重启循环、反制造残留锁 → 需增补「处理中 `/health` 2s 内响应」AC + 禁 async 改造前置。高：③ AC-7 末句已满足（`stub` 0 命中且已有测试断言）属空条款，缺陷实际在 `news_l1.py:213` 非 `kb.py`，且同一 bug 在 link_read/web_search 三处同构 ④ AC-2 与 AC-8 判据互斥、同次验证会同时判通过与不通过 ⑤ C-1 应拆分：`source_item_url` 按 R-5 可由适配层构造（撤回），`domain_tags` 仍缺；并须写明适配层要显式回填 `raw_content["url"]`，否则 link_read 静默失效。中：⑥ AC-4 的 `analysis` 空值规则、`context` 过滤行为、`needs_context` 在 DB 模式无列可写。另附：工程成本与切片（处理核心零改动确认成立，logging 横切与 AC-2 存张力）、契约 v1.1 复核（C-1~C-9 除 C-1 的 URL 分支外全部仍成立）、事实层刷新 8 项（O-1 已定案 / 前置已解除 / 只有 x_twitter 真实数据须进 §3）。 |

| 2026-07-27 | PRD R3 | Developer | 未通过（3 高 2 中 2 低，需 PM 修改后 R4） | [v0.2-prd.md](v0.2-prd.md) §Review 记录 · R3 — Developer 复审。**R1 六条已全部收敛到位**（含 3 处被推翻判断，Developer 逐条确认接受，Q-4 的错法已认领）。本轮新问题全部落在 async 新范围：高①**HTTP 模式并发退化无验收覆盖**——端点由 `def` 改 `async def` 后 FastAPI 不再派发线程池，改造若有遗漏则 HTTP 模式从"线程池并发"退化为"event loop 串行"，比 v0.1 更糟，而 AC-9.3 只覆盖 DB 模式；需补对称的并发验收。高②**AC-9.4「行为不变」是循环论证**——实查 `test_news_l1.py:44` `FakeClient`、`:64-73` `NullTools`、`test_news_l1_tools.py:16` `FakeTools` 全为同步签名，async 后必须改写（约 21/36 例受影响），用改写后的测试证明改写后代码行为不变验不住；需把 O-8 的"黄金样本快照"提升进 AC-9.4 作为验证方式写死。高③**AC-3.6 与 §5 时序冲突**——C-6 行锁实证要求"实现前"完成，但口令注入归部署阶段；xiaobao 已预判列级授权很可能不满足 `FOR UPDATE`，若实证推迟到部署阶段才失败，claim/事务/并发测试全部返工；建议口令注入前移为实现阶段开工前置（同机直读，成本极低）。中④§8 成本表沿用 R1 线程方案前提的"处理核心零改动"表述，async 下已不成立（解耦方向仍是红利，但代码要碰 4 出网点 + 2 协议 + 4 IO 节点 + `invoke→ainvoke` + 端点 + 过半单测），会低估排期。中⑤`l0_label` 空值兜底未写（`['']` 是真值、会穿过 `or None` 传给 KB 检索）。低⑥⑦ 驱动选型建议 psycopg3(async)、async 划线按"有 IO 才 async"。**附**：AC-9.1 的四个 IO 点经 `grep` 实查行号逐一准确；风险重心已从"契约缺项"转移到"async 回归"。 |
| 2026-07-27 | PRD R3 | DevOps | 未通过（3 高 2 中，需 PM 修改后 R4） | [v0.2-prd.md](v0.2-prd.md) §Review 记录 · R3 — DevOps 复审。**R1 七条已全部收敛**，其中 3 条被写得强于原建议（AC-9.3 抗阻塞探活断言 / AC-6.2 错误口令验证法 / AC-5.7 优雅停机验证法）。本轮新问题：高①**§5「唯一剩余外部依赖是口令」不成立**——实查本机为 macOS（`/root` 不存在）、`5432` 无监听、项目内无 `scripts/`·`deploy/`、v0.1 全程跑本机 `127.0.0.1`，故 PRD 说的「与 xiaobao 同机 + `host=127.0.0.1`」指向一台 **Linux 服务器**，即 ai 运行位置要从开发机迁到服务器，而该部署环境（代码上机方式 / Python 环境 / `.env` 与日志落点 / 启动命令）整块缺失，AC-3.6 的 C-6 实证当前**不可执行**；与 Developer R3 高③是同一处的下一层（前移的不只是口令，是整个环境准备）。高②**AC-5.7 优雅停机宽限期只有上限没有下限，且未覆盖 ASGI 与托管层**——单批 N≤8 串行最坏 632s，配 90s 完全"符合"AC 字面却必然中途强杀；systemd `DefaultTimeoutStopSec` 通常 90s，v0.3 托管化落地当天优雅停机即失效；需补「≥单批最长处理时间且 <1800s，三层配一致」并改用 N>1 批处理验证。高③**回滚被 US-4/AC-1 写成 ai 单方"改配置重启"，实际是双侧协同且有顺序**——DB 模式下 xiaobao 不再发 HTTP（其 `calcScoreTotal` 只挂 HTTP 路径，见 AC-8.2），ai 单方切回 HTTP 会导致队列静默积压，比不回滚更糟；须先 xiaobao 切回集成模式、后 ai 切 `RUN_MODE`，且回滚预案要事先与 xiaobao DevOps 对齐。中④`last_poll_at` 无陈旧阈值定义，整批处理期间（最坏 632s）不更新，健康 worker 与卡死 worker 表现相同，需补在途处理进度字段。中⑤「同机直读」消除传输风险但引入**口令副本漂移**（`/root/.secrets` 与 `.env` 两份、无同步、无热加载），且 root 授权形式未定。**附**：已给出 C-6 行锁实证的完整执行安排（6 步、全程事务内 `ROLLBACK` 不消耗 5 条预置队列、结论回帖归 PM）。 |
| 2026-07-27 | PRD R3 | Architect | 未通过（4 高 2 中 2 低，需 PM 修改后 R4） | [v0.2-prd.md](v0.2-prd.md) §Review 记录 · R3 — Architect 复审。**C-3 反转已独立核证成立**（不依赖对方答复，实读 xiaobao `v0.6.1-design.md:357` 占位方案①、`schema.ts:202-205` `raw_item_id` `.unique()`、`:201` `id defaultRandom()` 三处），结论：对设计几乎无影响，但**写回幂等性从此依赖对方 schema 的唯一约束**，须登记为跨服务依赖。**R1 十条已全部收敛**，3 条被写得强于原建议。本轮新问题（两方均未触及，全在协议形状与运行时结构）：高①**AC-2.1 的四操作协议对 HTTP 模式是退化的**——「推」（端点被动接收）与「拉」（worker 主动取批）是相反的控制流，HTTP 数据源会有 2~3 个空方法，而 AC-2.2「切换时 diff 为空」**能被一组空实现糊弄过去**；应按职责分层（映射协议两侧都真实实现 / 拉取协议仅 pull 型实现），判据改为「同一核心接两条真实控制流」。高②**claim 排序在 R1→R3 演进中整条丢失**——R1 的「按 `published_at` 升序」随「改为只查 tasks」失去载体后未补替代，现 AC-3 无任何取件顺序：FIFO 保证消失 + `tasks.priority`（`schema.ts:262`，其队列索引 `ix_tasks_queue(status, run_after, priority)` 明示预期访问模式）被无视；需补 `ORDER BY priority DESC, run_after ASC`。高③**退避数组长度 3 < `tasks.max_attempts` schema 默认 5**（`schema.ts:265`）——第 4/5 次重试 `backoff(attempt)` 越界，静默退化即回到 C-4 刚修好的「立刻重领」；且契约 §task type 写「最大 3」与 schema 默认 5 仍不一致。高④**AC-9.3 的 `/health` 200 可能是假信号**——async 下 worker 循环若为 `asyncio.create_task`，其未捕获异常被静默吞掉（仅 gc 时打一行 `Task exception was never retrieved`），worker 已死而进程/HTTP/`mode` 全部正常；需补顶层异常捕获 + `worker_alive` 字段 + worker 已死时 `/health` 返回非 200（与 DevOps 中④互补：其字段答「卡没卡住」，本条答「还在不在」）。中⑤**AC-2.4 的函数引用指错**（我 R1 引入、PM 照抄，已认领）——决定 `link_read` 触发的是 `tools/link_reader.py:23`（**额外要求 `http(s)://` 前缀**）而非 `news_l1.py:407`（不检查前缀），两者判定不一致；`source_item_url` 若无协议前缀会导致 link_read 静默不触发而 context 仍填该 url，且 AC-2.4 给的验证方式会因此不稳定。中⑥`tasks.raw_item_id` **nullable**（`schema.ts:258`），claim SQL 需补 `AND raw_item_id IS NOT NULL`。低⑦契约实读已是 **v1.4**（§5 仍写「待升 v1.4」已过期，且缺「ai 已跟进版本」落点）。低⑧`published_at` 双写需定归属。**降风险附项**：给出 C-6 失败时不依赖 `FOR UPDATE` 的条件式原子 `UPDATE ... RETURNING` fallback（并发退化为阻塞而非跳过，只需列级 UPDATE 权限）→ C-6 从「worker 地基可能返工」降为「两种写法二选一」。新增 C-11（`priority` 方向语义）/ C-12（退避表长度）/ C-13（`source_item_url` 格式 + 幂等依赖登记）待 PM 转达。 |
| 2026-07-27 | PRD R4 | Architect | **通过（附条件）**（1 高 3 中 3 低，无阻塞级） | [v0.2-prd.md](v0.2-prd.md) §Review 记录 · R4 — Architect 复审。**R3 八条已全部收敛，其中 3 条强于原建议**（越界行为写成测试断言 / 三重探活分工表述更清 / 堵空实现措辞更严）。本轮：高①**AC-9.4 黄金样本未定义覆盖路径**——方法论正确但「若干组」不约束覆盖面，而 §6/§8 均认定它是 O-8 这个 P0 风险的**唯一客观兜底**；同步转 async 的回归极少出在正常路径（改错单测立刻红），而集中在异常路径：`asyncio.wait_for` 抛 `asyncio.TimeoutError` 与 `httpx.TimeoutException` **不是同一类型**（`llm/client.py` 的 error kind 按类型分派会漏接 → 降级语义与 `error_kind` 一起失真）、provider fallback 链顺序、工具全失败降级、非法 JSON 容错——只录 happy path 则兜底只兜住一半且无人知道；须补四类路径覆盖要求。中②**AC-3.7 fallback 行为描述不准（我 R3 引入、PM 照抄）**：并发下后到者**本轮拿空批**（子查询无 `SKIP LOCKED`、两 worker 选出同一批 id），非「阻塞后拿到其他行」→ fallback 是 v0.2 单实例专用，**v0.3 多实例前必须先解决 C-6**，须写进 §4 顺延项。中③**AC-5.7 优雅停机与 AC-9.3 探活在「停机中」互相打架**：worker task 正常收尾结束时 `worker_alive`=false → `/health` 非 200 → v0.3 托管层判死重启；`worker_alive` 二态无法表达「正在正常退出」，须改三态 `running/stopping/dead`，仅 `dead` 返非 200。中④AC-2.1「不得有空方法」需澄清恒等映射属真实实现（HTTP 侧 `to_l1_input` 天然是 identity）。低⑤§6「已闭合 12 项」计数错（实为 14 项 /13 行，`C-1/Q-5` 合并计），§8「12/13」同。低⑥PRD §Review 状态表未推进 R4。低⑦流程提醒：R4 若再未通过，按 baseline §9-10 应升级「阻塞」交 Owner，不得直接出 R5。**同日订正**：中②与 Developer R4 中①是同一处，其补充更深——fallback 正确性**只在 `READ COMMITTED` 下成立**（依赖 EvalPlanQual 重新求值；RR/SERIALIZABLE 下直接抛 `could not serialize access`，claim 报错而非拿空批），该条记在 Developer 名下；另接受其中②对 AC-2.2 的改进（「核心代码完全相同」缺可证伪性，应拆为 grep 静态判据 + 动态跑通两条）。**判通过的理由**：本轮问题无一阻塞，高①属补验证细节（轻量变更走 Change Note），中②责任在我且不影响 v0.2 正确性；契约侧阻塞已全清、三方 R3 的 10 条高已全部处置、设计阶段方向明确——剩余条目在设计阶段承接的成本低于再走一轮 PRD 轮次。 |

| 2026-07-27 | PRD R4 | Developer | **通过**（2 中 2 低，均不阻塞定稿） | [v0.2-prd.md](v0.2-prd.md) §Review 记录 · R4 — Developer 复审。**R1 六条 + R3 七条已全部收敛**，其中 5 条逐字采纳（AC-9.5 HTTP 并发不退化、AC-9.4 黄金样本外部化、AC-8.2 `l0_label` 空值兜底、O-9 psycopg3(async)、§5 部署环境与口令双待办前移）。**认领一处订正**：AC-2.4 的 URL 判定落点我 R1/R3 两轮引错——决定 `link_read` 触发的是 `tools/link_reader.py:23`（要求 `http(s)://` 前缀），而非我引用的 `news_l1.py:407`（只被 `ingest_context_node` 用、不检查前缀）；结论方向成立但落点指错，且未发现两处判定不一致，Architect 的发现更深一层，R4 的规范化处置充分。**O-8 还订正了我的 async 划线说法**：判据应为「无 IO **且耗时毫秒级**」而非单看有无 IO（同步节点在协程中被直接执行、耗时占用 event loop），我接受。本轮问题：中①**AC-3.7 的 fallback 隐式依赖 READ COMMITTED**——「UPDATE 阻塞→重新求值→`status='queued'` 不成立故排除」只在 READ COMMITTED 下成立，REPEATABLE READ 及以上会抛 `could not serialize access` 而非安全跳过；且其并发语义与 `SKIP LOCKED` 不等价（并发 worker 子查询选出同一批 id，第二个拿 0 行而非另一批），v0.3 多实例时吞吐受限，建议补隔离级别约束 + 登记已知限制（可并入设计阶段 O-6/O-9）。中②**AC-2.2 判据前半句「核心代码完全相同」是同义反复**（只要不复制成两份必然成立），建议拆为静态 grep（核心目录不命中数据源概念词）+ 动态两条路径各跑通。低③ Review 状态表未推进到 R4。低④ 单测分母口径不一（AC-9.4 写 40、§8 与实查为 36）。**成本表复核**：§8 重写准确可直接用于切分；块 4 风险由「中（依赖 C-2/C-5/C-6）」降为「中（C-6 已有 fallback）」确认成立——fallback 只需列级 UPDATE 权限，C-6 不再决定 claim 地基形态。 |

### R2 修改记录（2026-07-26，PM）

**Owner 核心原则（2026-07-26 定，写入 PRD §0，优先级高于成本考量）**：以基础夯实、可扩展性强的方式开发，而不是图便捷；宁可现在多干活，也要让系统更健全、后期接入更友好。依据：ai 是持续迭代产品，基础越牢后续可扩展的功能越多。

按此准则推翻 R1 三处「成本最优解」：

| # | R1 成本最优解 | R2 裁定 |
|---|---|---|
| 1 | worker 跑独立线程绕开同步阻塞、处理核心零改造（Developer 问题 2 推荐①） | **改为 async 地基改造**（新增 US-8 / AC-9）——线程方案是绕过而非解决；同步阻塞是既有架构债，v0.3 工具并发必然要还，届时 worker 推倒重来 |
| 2 | 适配层 = 两个映射函数（Architect O-2 倾向） | **改为定义数据源协议**（AC-2.1）——映射函数方案下第二调用方要回来改我们代码 |
| 3 | `tags_v2` 第五类二选一 | **主张 `sentiment` 与 `processing` 两者都保留**（C-10）——二者非同类，丢任一削弱可诊断性 |

**其余修改**：AC-2/AC-8 判据分开写死（解 Developer 问题 4 的互斥）；AC-3 增补批量上限不变式（N ≤ 8，正确性约束）+ claim 原子性实证前置；AC-4 字段来源逐条标注 + 三段式事务 + 写入方式挂 C-3；AC-5 增补退避判定、重试真源、残留锁自愈、优雅停机；AC-6 增补 DSN 脱敏可验证断言、注入式 logging、降级事实冗余进日志；AC-7 删空条款（`stub` 已 0 命中）+ 缺陷定位订正至 `news_l1.py:150/179/213` + 扩为三工具统一语义；新增 AC-9（async 地基 + 处理中 `/health` 2s 响应）、AC-10（三类源实现 + 验收分层，不得虚报）；新增 §6 契约缺项清单 C-1~C-10、§8 工程成本与切片；§5 前置依赖按已就绪/待交付/届时前置三段重写；事实层刷新 8 项全部落实。

**新增开放问题**：O-8 async 改造的切分与回归策略（P0，本迭代最大技术风险，风险在回归不在重写）、O-9 DB 驱动选型须与 async 一致、O-10 `locked_by` 标识规则。

### R3 修改记录（2026-07-27，PM）

**触发**：xiaobao 三方于 2026-07-27 全部答复 ai 转达的契约缺项——PM 答 4 项（C-10/C-7/Q-2/Q-6）、Architect 答 C-2/C-3/C-5 + 非阻塞 6 项 + 主动上报己方 3 项缺口、DevOps 执行 3 列 GRANT（test + prod 双库对称）。契约由 v1.1 连升 **v1.2 → v1.3**，待升 v1.4（补权限矩阵）。

**结果：原 4 条阻塞定稿项（C-2/C-3/C-5/C-10）全部闭合**，PRD 中所有 `[待 C-xx]` 标记解除；10 条契约缺项闭合 9 条（C-6 待 ai 实证），6 条 Q 项闭合 5 条（Q-1 待 xiaobao PM 表态，不阻塞）。

**ai 侧 3 处判断被推翻，R3 逐条收敛**：

| # | ai 原判断（R2） | 实际 | R3 处置 |
|---|---|---|---|
| 1 | C-3 推断为 **ai INSERT**（依据触发器 INSERT 后触发的语义） | **ai UPDATE 占位行**——占位行是 xiaobao 产品硬约束（L0 通过后新闻须立即可见），且 `news_positions` 不存排序键故 ai 的时序担心不成立 | AC-4.1 改为 UPDATE 占位行，实现用 `INSERT ... ON CONFLICT (raw_item_id) DO UPDATE`；`id` 不写由 DB 生成 |
| 2 | C-4 判断根因是**契约 claim SQL 缺时间条件**，并计划 ai 自行用 `updated_at + backoff` 计算 | 根因是 **xiaobao GRANT 少给 `run_after` 列**；其退避机制本就存在。ai 自算会形成两套真源 | AC-5.2 改为写 `tasks.run_after`，**明令禁止自行计算** |
| 3 | Q-4 判断 **rss 无原文链接**（依据其 R-5 字段表），已记为「已知限制」结案 | 链接**一直存在**于 `raw_items.source_item_url` 列（R-5 只覆盖 `content` jsonb、未覆盖一级列），且对 `x_twitter` 同样有值 | AC-2.4 改为**三类源统一读 `source_item_url` 列**，废弃从 `tweet_id` 构造的方案 |

> 第 3 条印证了 Owner「没法确认的不能留成遗留」原则的价值——该条原本已被 ai 记为「已知限制」结案，正是因为按该原则提出确认才被纠正。

**其余 R3 变更**：AC-3 claim 改为以 `tasks` 为准 + 补 `run_after <= now()` 条件 + 明确「只 claim tasks 不扫 raw_items 是安全的、不需孤儿探测」；AC-4 字段表全部确定（`tags_v2` 第五类回归 `processing`、`language` 固定 `'zh'`、**新增写入 `published_at`**、`analysis` 空值写 NULL）；AC-5 重试上限改读 `tasks.max_attempts`（**禁止硬编码 3**）+ `tasks.status` 四值写死；AC-8 已知差异更新（`domain_tags` 改为 `l0_label` 单值来源不再恒空但仍非等价、**新增 `score_total` 在 DB 模式保持 NULL 的现象说明**）；AC-10 验收分层经 xiaobao PM 确认；§5 新增 3 列 GRANT 就绪 + xiaobao 侧 3 项缺口登记 + C-5 窗口修复承诺；§6 重写为闭合状态表 + 3 处被推翻判断的复盘。

**联调判读须知（写入 §5 与 AC-8.2）**：`score_total` 在 database 模式**没有触发点**（xiaobao 的 `calcScoreTotal` 只挂在 HTTP 路径），ai 写回后该列保持 NULL → 新闻按分排序沉底、前端评分徽章显示 0。**这不是 ai 的缺陷**，xiaobao 已上报其 PM 决策补触发点。

### R4 修改记录（2026-07-27，PM）

**触发**：ai 侧三方 R3 复审全部完成，均未通过但**无一条阻塞级**——Developer 3 高 2 中 2 低、DevOps 3 高 2 中、Architect 4 高 2 中 2 低，10 条高严重度全为增补型。三方一致确认 R1 意见已全部收敛（含 3 处被推翻判断），且各自认可 §0 准则的三处裁定。

**三片新问题与处置**：

| 提出方 | 问题 | R4 处置 |
|---|---|---|
| Developer 高① | HTTP 模式并发退化无验收覆盖——端点改 `async def` 后 FastAPI 不再派线程池，改造若有遗漏则 HTTP 模式从「线程池并发」退化为「event loop 串行」，**比 v0.1 更糟** | **新增 AC-9.5**：并发 N≥3 请求总耗时 < 1.5× 单条。这是 AC-9.1 改造彻底性的唯一客观检验 |
| Developer 高② | AC-9.4「行为不变」是**循环论证**——测试桩全同步、async 后必须改写约 21/36 例，用改写后的测试证明改写后的代码行为不变 | **黄金样本从 O-8 提升进 AC-9.4 写死**：改造前在 v0.1 基线录制 `L1Output` JSON 快照，改造后同输入同 mock 重跑、逐字段比对。回归判据独立于测试代码改写 |
| Developer 高③ + DevOps 高①（**已合并**） | AC-3.6 要求「实现前」完成 C-6 实证，§5 却把口令注入排在部署阶段；且**即便口令到位也做不了**——实查本机 macOS、无 `/root`、5432 无监听、无 `scripts/`·`deploy/`，PRD 说的「同机」指一台 Linux 服务器，**ai 至今只在开发机跑过** | **§5 待办改为两项并前移为「实现阶段开工前置」**：待办 1 = 服务器部署环境（目标机 / 上机方式 / Python 环境 / `.env` 与日志落点 / 启动命令）；待办 2 = 口令注入。订正「唯一剩余外部依赖」这一**事实性错误** |
| DevOps 高② | AC-5.7 优雅停机宽限期**只有上限没有下限**，且未覆盖 ASGI 与托管层——N=8 串行最坏 632s，配 90s 完全符合字面却必然中途强杀；systemd 默认 90s，v0.3 托管化落地当天即失效 | AC-5.7 补「**≥ 单批最长处理时间且 < 1800s，应用/ASGI/托管三层配一致**」；验证方式改用 **N>1 批处理**中发 SIGTERM；§4 托管化条目点名 `TimeoutStopSec` |
| DevOps 高③ | 回滚被写成 ai 单方「改配置重启」，**实为双侧协同且有顺序**——DB 模式下 xiaobao 不再发 HTTP，ai 单方切回会导致队列静默积压，**比不回滚更糟** | US-4 与 AC-1.5 写明顺序：**先 xiaobao 切回集成模式 → 后 ai 切 `RUN_MODE`**；反序会有「两侧都不处理」窗口；回滚预案须灰度前与 xiaobao DevOps 事先对齐 |
| Architect 高① | AC-2.1 四操作协议对 HTTP 模式**退化**——推与拉控制流相反，HTTP 侧会有 2~3 个空方法，而 AC-2.2「diff 为空」**能被一组空实现糊弄过去** | AC-2 重写为**按职责分层**：映射协议（两模式真实实现、不得有空方法）+ 拉取型协议（仅 pull 实现）；判据改为「**同一核心接两条真实控制流**」 |
| Architect 高② | **claim 排序在 R1→R3 演进中整条丢失**——R1 的「按 `published_at` 升序」随「改为只查 tasks」失去载体后未补，现 AC-3 无任何取件顺序：FIFO 消失 + `tasks.priority` 被无视 | AC-3.2 补 `ORDER BY priority DESC, run_after ASC`（走 `ix_tasks_queue`）。**这是回归修复，错在 PM R3 改写时漏补** |
| Architect 高③ | 退避数组长 3 < `tasks.max_attempts` **schema 默认 5**——第 4/5 次 `backoff(attempt)` 越界，静默退化即回到 C-4 刚修好的「立刻重领」 | AC-5.2 规定**越界取末值 900s**，禁止取 0 或崩溃；新增验证用例；提 C-12 请对方确认 |
| Architect 高④ | AC-9.3 的 `/health` 200 **可能是假信号**——async 下 worker 若为 `asyncio.create_task`，未捕获异常被静默吞掉，worker 已死而进程/HTTP/`mode` 全部正常 | AC-9.3 补**三重探活**：抗阻塞（原有）+ `worker_alive`（worker 死时 `/health` 返回**非 200**）+ 在途进度（`in_flight`/`current_item_started_at`，DevOps 中④）。三者分别答「探活可不可信」「还在不在」「有没有卡住」 |

**中低项处置**：AC-2.4 函数引用订正为 `tools/link_reader.py:23`（原引 `news_l1.py:407` 有误，**该错由 Architect R1 引入、已认领**）并补 URL 协议前缀规范化 + 三例验证；claim 补 `AND raw_item_id IS NOT NULL`（该列 nullable，否则 claim 提交后才失败、且 ai 会把不属于自己的任务标 `failed`）；`l0_label` 空值兜底为 `[]`（`['']` 是真值会穿过 `or None` 传给 KB 检索）；§5 补口令授权形式（按需 sudo 或 Owner 代拷，**不给全量 sudo**）与「`.env` 是快照、轮换须重注入 + 重启」；契约版本统一 **v1.4** 并补「ai 已跟进版本」落点；`published_at` 补归属声明（xiaobao 改语义须先知会、届时 ai 停写）；§8 按 async 前提重写成本表。

**降风险（Architect 附项）**：AC-3.7 补 C-6 的 fallback——不依赖行锁的条件式原子 `UPDATE ... RETURNING`（并发退化为阻塞而非跳过，只需列级 UPDATE 权限）→ **C-6 从「worker 地基可能返工」降级为「两种写法二选一」**，实现阶段不必等待 xiaobao 改授权。

**开放问题更新**：O-2 定为「按职责分层，不按模式对称」；O-6 补「async 下连接随事务获取释放、不得跨 `await` 长持」；O-8 划线判据订正为「**无 IO 且毫秒级**才保持同步」，回归基线已提升进 AC-9.4；O-9 倾向 psycopg3(async)（两方一致，理由非成本）。

**新增待转达 xiaobao 3 条**（均非阻塞，ai 侧已按明确假设实现）：C-11 `tasks.priority` 方向语义；C-12 退避表长度与「最大 3 vs 默认 5」不一致；C-13 `source_item_url` 是否保证带协议前缀 + 登记「`raw_item_id` 唯一约束是 ai 写回幂等前提」。

## Change Notes

| Change Note | 状态 | 变更级别 | 受影响角色 | 执行状态 | 归档状态 |
|-------------|------|----------|------------|----------|----------|

## Git 关键节点

| 节点 | commit | 说明 |
|------|--------|------|

## 迭代关闭归档
- Owner 验收（真源记录，未验收 / 打回则不得关闭）：—
- Developer 自测结论（证据齐备）：—
- 实现 R1 Review 结论：—
- 部署就绪检查：—
- 关闭结论：—
- 未关闭原因或延期项：—
- 已生成 summary：—
- 已更新 `docs/progress/INDEX.md`：—
- 已更新当前角色日志：—
- 其他角色待补充/确认：—
- 已更新知识库：—
- 遗留问题下一步：—
- coordination 同步：—