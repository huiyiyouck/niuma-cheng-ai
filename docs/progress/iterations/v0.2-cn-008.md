# v0.2 Change Note · CN-008 — claim 事务边界确认，`DB_OP_BOUND` 改用事务级超时

## 基本信息
- 日期：2026-07-30
- 提出角色：**Architect**（应 Owner 要求确认 claim 事务边界；DevOps 的 `ALTER ROLE` 与 Developer 的连接池实现均等待本确认）
- 关联文档：`v0.2-design.md`（§2.6 / §4.10 / §8 测试 25）、`v0.2-cn-005.md`（其变更 3 的取值被本 CN 订正）、`v0.2-prd.md`（AC-4.7）
- 关联阶段：设计阶段（已定稿）→ 实现阶段（开工前）
- 变更级别：**轻量变更**
- 当前状态：**三方已确认**（2026-08-01，DevOps 末票附条件 2 中 2 低）

## 背景

DevOps 在确认 CN-003~CN-007 时查出一处**会卡住启动的算术错误**（commit `a7e90fd`）：设计 §2.6 的公式 `DB_OP_BOUND = (RETRY+1) × (statement_timeout + delay)` 代入默认值得 **27s**，而校验表写的是 **18s**，且该条是启动门禁——按当时默认值 `27s ≤ 20s` 为假，**worker 会直接拒绝启动**。

其认领了算术源头（`RETRY=2` 意为「失败后再试 2 次」、总尝试 3 次，其 R2 给的 `2 × (8+1)` 漏算首次尝试），并给出订正方案 A：`statement_timeout 8000→5000`、`lock_timeout 5000→3000`，使 `3 × 6000 = 18s` 与文档已写的数字吻合。

**Owner 要求 Architect 先确认 claim 事务边界，确认后 DevOps 才执行 `ALTER ROLE`、Developer 才落连接池。** 本 CN 即该确认的结论。

## 问题描述

### A. 先认账：那段文字在我的文档里自相矛盾，我确认 CN-005 时照抄了错值

设计 §2.6 原文在**同一个代码块里**写了两个互相矛盾的算式：

```text
DB_OP_BOUND = (L1_WRITEBACK_RETRY + 1) × (STATEMENT_TIMEOUT + RETRY_DELAY)
            = (2 + 1) × (8000 + 1000)          ← 27s
当前取值下按 DevOps 给出的自洽算式：2 × (8s + 1s) = 18s ≤ 20s ✔   ← 18s
```

DevOps 说「三方同错」是准确的，但**这处自相矛盾在我的文档里**：我把公式和 DevOps 的代入值都抄了进去，没做一次算术核对。这属于我在 CN-005 确认时该拦住的。

### B. 更要紧的一处：`statement_timeout` 是「每条语句」的，不是「每个事务」的

DevOps 的方案 A 在算术上自洽，但它建立在一个**不成立的隐含假设**上——「一次写回 = 一条语句」。

**本次确认的事务边界**（据设计 §3.4 / §4.6 / §4.7 逐条点算）：

| 事务 | 语句数 | 构成 |
|---|---|---|
| **claim 事务**（写法 A） | **4** | `SELECT … FOR UPDATE SKIP LOCKED` → `UPDATE tasks`（置 running）→ `SELECT raw_items ⋈ sources` → `UPDATE raw_items`（置 processing + `l1_attempt+1`） |
| **写回事务** | **3** | `INSERT … ON CONFLICT DO UPDATE` → `UPDATE raw_items` → `UPDATE tasks` |
| 失败事务 | 2 | `UPDATE tasks` → `UPDATE raw_items` |

按 `statement_timeout = 5s` 代入真实语句数：

| 量 | 方案 A 的隐含算法 | **按实际语句数** |
|---|---|---|
| 一次写回最坏 | 5s | **15s**（3 条 × 5s） |
| 写回含重试（3 次尝试 + 2 次间隔） | 18s | **47s** |
| claim 最坏 | 未计入 | **20s**（4 条 × 5s） |
| 单条链路 DB 合计 | 18s | **67s** |

**67s ≫ 20s 收尾余量** —— 即方案 A 改完两个默认值后，启动门禁会通过，但**保证是假的**：真到停机时卡在写回，仍会超出宽限期被 SIGKILL，留下残留锁。这正是 AC-4.7 当初要防的事。

**根因**：PostgreSQL **没有内建的事务执行超时**。`statement_timeout` 逐语句生效，`idle_in_transaction_session_timeout` 管的是空闲而非执行。因此**光靠数据库侧配置给不出事务级上界**。

## 变更判断

- 是否改变产品范围：**否**
- 是否改变主流程：**否**（claim → 处理 → 写回三段式不变，事务边界本身**未改**，本 CN 是把它**确认并写明**）
- 是否改变架构方案：**否**（新增的是一个应用层包装函数与一个配置项，不动分层与依赖方向）
- 是否改变接口/数据契约：**否**
- 是否改变验收/回归：**是** —— AC-4.7 的取值与判据订正、测试 25 扩为三项。**均为收紧**（把一个假的保证换成真的），不放宽任何一条
- 是否改变部署方式：**否**（`ALTER ROLE` 仍可做，见变更 3）
- **结论：轻量变更**

## 影响范围

- 架构：无方向变化；§2.6 公式与取值订正、§4.10 新增事务级超时包装
- 实现：连接池新增 `run_tx` 统一入口；新增一个配置项；两个既有配置项默认值调整
- 自测/验收：测试 25 由「持锁冲突 + 不等式」扩为三项，新增**专验「语句级超时挡不住事务超时」**的一条
- 部署：`ALTER ROLE` 的取值约束明确化（见变更 3）

## 处理方式

### 变更 1：`DB_OP_BOUND` 改用事务级超时，拆为两个量

```text
DB_WRITEBACK_BOUND = (L1_WRITEBACK_RETRY + 1) × (AI_DB_TX_TIMEOUT_MS + L1_WRITEBACK_RETRY_DELAY_MS)
                   = (2 + 1) × (5000 + 1000) = 18s      ← 用于收尾余量（停机时只可能卡在写回）
DB_OP_BOUND        = AI_DB_TX_TIMEOUT_MS + DB_WRITEBACK_BOUND
                   = 5s + 18s = 23s                      ← 用于 AC-3.6（单条完整链路含 claim）
```

拆开的理由：两个量的用途不同。收尾余量只需覆盖「停机瞬间正在进行的写回」；AC-3.6 的不变式要覆盖「单条从 claim 到写回的完整链路」，**原式漏了 claim 事务**。

**三条不等式，全部进启动门禁**：

| # | 不等式 | 当前取值 |
|---|--------|---------|
| 1 | `DB_WRITEBACK_BOUND ≤ L1_SHUTDOWN_GRACE_MS − N × L1_ITEM_BUDGET_MS` | `18s ≤ 20s` ✔ |
| 2 | `N × (L1_ITEM_BUDGET_MS + DB_OP_BOUND) < 1800s × 0.6` | `1 × 263s < 1080s` ✔ |
| 3 | `AI_DB_LOCK_TIMEOUT_MS < AI_DB_STATEMENT_TIMEOUT_MS < AI_DB_TX_TIMEOUT_MS` | `3s < 4s < 5s` ✔ |

**已定的 260 / 280 不用改**，`DB_WRITEBACK_BOUND` 仍是 18s——与 DevOps 方案 A 的结论数值一致，**只是语义从「每条语句 5s」纠正为「整个事务 5s」**，这才是 18s 真正成立的前提。不等式 2 由 258s 微调为 263s（含 claim），余量依旧约 4 倍。

### 变更 2：配置项调整（Developer 落地）

| 配置 | 原 | 新 | 说明 |
|---|---|---|---|
| **`AI_DB_TX_TIMEOUT_MS`** | — | **`5000`（新增）** | **事务级**上限，应用层 `asyncio.wait_for` 包整个事务 |
| `AI_DB_STATEMENT_TIMEOUT_MS` | `8000` | **`4000`** | 单语句兜底，须 < 事务级 |
| `AI_DB_LOCK_TIMEOUT_MS` | `5000` | **`3000`** | 等锁应比查询慢更早触发，便于日志区分 |
| `AI_DB_CONNECT_TIMEOUT_MS` | `5000` | 不变 | — |

**连接池统一事务入口**（设计 §4.10 已落）：

```python
async def run_tx(pool, fn):                      # claim / 写回 / 自愈 三类事务统一走这里
    async def _inner():
        async with pool.connection() as conn:    # 连接随事务获取释放，不跨 await 长持
            async with conn.transaction():       # 出作用域自动 COMMIT / 异常自动 ROLLBACK
                return await fn(conn)
    return await asyncio.wait_for(_inner(), timeout=cfg.tx_timeout_ms / 1000)
```

- 超时抛 `asyncio.TimeoutError` → 回滚 → 计入写回重试；`error_kind=db_error`。
- **不得逐事务另设超时**：三类事务统一用同一个值，否则 `DB_OP_BOUND` 要按最大值重算，多一个易漂移的量。

### 变更 3：`ALTER ROLE` 的边界（DevOps 落地）

`ALTER ROLE ai_worker SET statement_timeout = …` / `SET lock_timeout = …` **可以做且推荐**，作为「应用某条路径忘了 `SET`」的兜底。两条约束：

1. **取值须与应用层一致或更宽松**。角色默认值会被连接后的 `_configure`（`SET`）覆盖，故它约束不了应用；反过来若角色默认值**更严**（如 2s），在 `_configure` 执行前的那几条语句上会先生效，造成难以归因的偶发失败。
2. **它不能替代事务级超时**。`ALTER ROLE` 只能设语句级参数，PG 无事务级等价物——**`DB_OP_BOUND` 的保证只来自应用层的 `asyncio.wait_for`**，不能靠数据库侧配置绕开。

### 变更 4：测试 25 扩为三项

① 持锁冲突在 `lock_timeout`（3s）内失败；② **事务级**——mock 一个含多条语句、总耗时超 `tx_timeout` 但**每条都不超 `statement_timeout`** 的事务，断言在 5s 处被取消并回滚（**专验「语句级超时挡不住事务超时」**）；③ 配置校验断言三条不等式。

### 连带：CN-005 变更 3 与 PRD AC-4.7

CN-005 变更 3 给的取值（`statement_timeout=8s` / `lock_timeout=5s`，最坏 `2×(8+1)=18s`）与 PRD AC-4.7 的对应表述**由本 CN 订正**。**其提出该项的判断完全成立**——「claim 与写回两个事务在所有预算之外」这个洞是真的，本 CN 只是把封堵方式从「语句级」换成「事务级」。

## 需确认角色和理由

| 角色 | 指定理由 | 是否确认 | 确认意见 |
|------|----------|----------|----------|
| DevOps | 算术错误的发现者与 `ALTER ROLE` 的执行方；变更 3 的两条约束需其确认可落地；三个超时取值影响其部署校验 | **已确认（附条件，2 中 2 低）**（2026-08-01） | **同意四条变更。我的方案 A 被推翻这件事成立，我认账。** ① **认账**：我的订正建立在「一次写回 = 一条语句」上，而 `statement_timeout` 逐语句生效。按实际语句数代入——写回 3 条 × 5s = 15s/次、含重试 47s、加 claim 4 条 20s = **单条链路 67s ≫ 20s 收尾余量**。即照我的方案改完两个默认值，启动门禁会变绿、而**保证是假的**：真到停机仍会卡在写回被 SIGKILL 留残留锁，正是 AC-4.7 要防的那件事。我在 CN-005 里只核了算术自洽、没核算式的物理含义，这比原先那个漏算首次尝试的错更隐蔽。② **算术复核全部成立**：`DB_WRITEBACK_BOUND = 3 × (5000+1000) = 18s ≤ 260 − 1×240 = 20s` ✔；`DB_OP_BOUND = 5 + 18 = 23s` → `1 × (240+23) = 263s < 1080s` ✔；`3s < 4s < 5s` ✔。事务边界点算（claim 4 / 写回 3 / 失败 2）与 §3.4 / §4.6 / §4.7 逐条对得上。拆两个量对——收尾余量只需覆盖停机瞬间的写回，AC-3.6 要覆盖含 claim 的完整链路。③ **变更 3 已实机验证可落地**（本次新增证据，目标机 `zijie`，全程事务内 `ROLLBACK` 未留改动）：`ai_worker` **非超级用户但 `ALTER ROLE` 自改可执行**（PG 允许角色修改自身 USERSET 参数）→ **无需 xiaobao DBA 配合，不是跨项目阻塞**；当前 `rolconfig` 为**空**；**裸连接下 `statement_timeout=0` / `lock_timeout=0`** —— 这条实测正好**确认了变更 3 的兜底价值**：`_configure` 执行前的那几条语句确实处于完全无超时状态，不是理论担忧。 **附条件与问题：** **中①（附条件）`AI_DB_CONNECT_TIMEOUT_MS` 是本次唯一被排除在门禁外、仍可自由漂移的超时量。** `run_tx` 的 `asyncio.wait_for` 包住的是 `_inner()`，而 `pool.connection()` **在 `_inner()` 内** → **建连耗时计入 tx 预算**；当前 `connect = 5000 = tx = 5000`，异常路径下光建连就能吃光整个事务预算。这不是假想场景：按 Developer 注意点 (a)，事务超时会导致连接被丢弃，**紧接着的那次重试正好需要新建连接**，于是三次尝试可能全耗在建连上、一条语句都没执行。后果不是破坏 18s 上界（上界仍成立，停机安全性不受影响），而是**让 §4.6 的有限重试在最需要它的场景下失效**——而 §4.6 的立论正是「这条已花掉 240s 预算和一次 LLM 费用」。更要紧的是**它是同型问题的第四次**（CN-004 的 `N ≤ 8`、CN-005 的三层配一致、本 CN 的语句级 vs 事务级，都是「不变式的输入不被任何机制保证」）：不等式 3 只管 `lock < statement < tx`，有人把 connect 调到 30s，三条不等式照样全绿。**建议 `AI_DB_CONNECT_TIMEOUT_MS 5000→1000` 并入不等式 3 → `connect(1s) < lock(3s) < statement(4s) < tx(5s)`。** 实测支撑：同机内网建连+查询 **35/37/35/35/36 ms**（5 次，含 psql 进程启动开销，实际建连更快），1000ms 已是 25 倍余量。**中②变更 3 约束 1 没有强制点**，与设计 §698「三层一致性的强制点只能在部署层」完全同构。实测 `rolconfig` 现在是空的，但没有任何机制阻止将来有人 `ALTER ROLE` 设成 2s（严于应用层 4s），那会造成只在建连瞬间出现、极难归因的偶发失败——正是变更 3 约束 1 警告的事，却无人守。**建议 `deploy.sh` 增一条校验：以 `ai_worker` 裸连接 `SHOW statement_timeout` / `SHOW lock_timeout`，断言为 `0` 或 ≥ 应用层取值。这条我落地**，本次实测该查询已跑通。**低③跨项目边界**：`ALTER ROLE` 虽不需对方配合，但它是对 xiaobao 数据库的**持久化**配置写入（不是会话级）。建议执行时经 coordination 知会一句——不需审批（只影响 `ai_worker` 自身会话），但对方 DBA 日后排查时应能查到来源。**低④公式多算一个重试间隔**：`(RETRY+1) × (TX + DELAY)` 隐含 3 个间隔，而 3 次尝试之间实际只有 2 个 → 18s 比真实上界 17s 多算 1s。**保守方向，不用改**；但建议在 §2.6 注明「多算一个间隔是有意的保守」，否则将来很可能被人当笔误"精确化"掉这 1s——本迭代已经因为动算式翻过两次车。 **部署侧影响结论：`deploy.sh` 现有三层停机时限校验无需改动**——本 CN 未动 260 / 280，`DB_WRITEBACK_BOUND` 仍是 18s ≤ 20s 余量，该脚本校验的 `TimeoutStopSec > L1_SHUTDOWN_GRACE_MS` 关系不受影响；新增工作只有中②那条 DB 侧校验。 |
| Developer | 变更 2 的配置项与 `run_tx` 统一入口是其实现依据；测试 25 的三项由其落地 | **已确认（附两条实现注意点）**（2026-07-30） | **同意四条变更，事务边界点算与三条不等式我逐条复核成立。** ① **点算核对**：claim 事务 4 条（`SELECT … FOR UPDATE SKIP LOCKED` → `UPDATE tasks` → `SELECT raw_items ⋈ sources` → `UPDATE raw_items`）、写回 3 条、失败 2 条，与设计 §3.4 / §4.6 / §4.7 逐条对得上。② **不等式复算**：`DB_WRITEBACK_BOUND = 3 × (5000+1000) = 18s ≤ 260 − 1×240 = 20s` ✔；`DB_OP_BOUND = 5 + 18 = 23s` → `1 × (240+23) = 263s < 1080s` ✔；`3s < 4s < 5s` ✔。**拆成两个量是对的**——收尾余量只需覆盖「停机瞬间正在进行的写回」，而 AC-3.6 要覆盖 claim→写回的完整链路，原式确实漏了 claim 事务。另核：失败路径（claim 5s + 处理 240s + `mark_failed` 5s = 250s）小于写回成功路径的 263s，故取 263s 覆盖最坏成立。③ **「PG 没有内建事务执行超时」这个根因判断正确**——`statement_timeout` 逐语句、`idle_in_transaction_session_timeout` 管空闲不管执行，所以事务级上界**只能来自应用层**，变更 3 第 2 条把这点写死很有必要（否则很容易有人以为 `ALTER ROLE` 就够了）。④ **我在 CN-005 提的是「假设不成立」，本 CN 给的是「换一种封堵方式」，方向我完全认同**：DevOps 那条「claim 与写回在所有预算之外」的洞是真的，数值 18s 也没变，变的是它成立的前提——从「每条语句 5s」纠正为「整个事务 5s」。 **两条实现注意点（不影响确认，实现时我会处理并在自测覆盖）**：**(a) `wait_for` 取消后连接可能不能安全归还池。** `asyncio.wait_for` 超时是靠向内部协程抛 `CancelledError` 实现的；若此时 psycopg3 正在等服务端响应，`conn.transaction()` 的 `__aexit__` 要发 ROLLBACK 而该 ROLLBACK 本身也需与服务端通信。psycopg3 对被取消的连接状态不做保证，`psycopg_pool` 归还时会检测并丢弃重建——**功能上安全，但每次事务超时会消耗一个连接**；池是 `min=1/max=3`，若超时频发会出现池抖动。这也正是 `statement_timeout=4s < tx_timeout=5s` 的价值所在：单条语句卡死时服务端先自己中断，`wait_for` 只在「多条语句累计超时」这种少数场景才真正开火。我会在测试 25② 里顺带断言**超时后连接池仍可正常取连接**。**(b) `CancelledError` 与 `TimeoutError` 的区分是自洽的，但必须写对。** `wait_for` 在 Python 3.8+ 会区分两种情形：超时抛 `TimeoutError`（继承 `Exception`，会被 `except Exception` 捕获 → 按变更 2 计入写回重试、记 `error_kind=db_error`）；**外部取消**（优雅停机 cancel worker task）则**重新抛出 `CancelledError`**（继承 `BaseException`，不被 `except Exception` 捕获 → 正常向上传播）。这与设计 O-8 的「`CancelledError` 不得被 `except BaseException` 或裸 `except` 吞掉」正好呼应：**一旦有人在 `run_tx` 外层写了 `except BaseException`，超时重试和优雅停机会同时失效**。实现时我按 `except Exception` 写并加注释说明为何不能放宽。 ⑤ **变更 3 的两条约束我从实现侧确认**：`ALTER ROLE` 的值须「与应用层一致或更宽松」是对的——`_configure` 的 `SET` 会覆盖角色默认值，但覆盖发生**在连接建立之后**，若角色默认更严，在 `_configure` 执行前的那几条语句上会先生效，产生只在建连瞬间出现的偶发失败，这类问题极难归因。 ⑥ **变更 4 的测试 25② 是三项里最有价值的一条**：mock 一个「每条都不超 `statement_timeout`、但总耗时超 `tx_timeout`」的事务，正是专验「语句级挡不住事务级」——**这条测试如果不写，这次订正的全部意义在回归中就没有守护**。测试 25 三项我负责落地。 |
| PM | 变更 1 订正了 CN-005 变更 3 的取值与 PRD AC-4.7 的表述，需其同步 PRD | **已确认，PRD 已同步**（2026-07-30） | **同意四条变更。PRD AC-4.7 已按本 CN 重写**（事务级上限 + 四项配置 + 两个派生量 + 三条不等式 + 「语句级挡不住事务级」必测例），AC-3.6 的不变式同步为 `N × (单条预算 + DB_OP_BOUND) < 1800s × 0.6`。**三点判断：** ① **根因订正比数值订正重要得多，这条必须在 PRD 里写死。** DevOps 的方案 A 在算术上自洽、启动门禁也会通过——**但保证是假的**：按实际语句数（claim 4 / 写回 3）代入，单条链路 DB 合计 67s ≫ 20s 收尾余量，真到停机仍会被 SIGKILL 留下残留锁，**而这正是 AC-4.7 当初要防的那件事**。「校验通过但保证不成立」是本迭代反复出现的一类问题（CN-004 的 `N ≤ 8`、CN-005 的三层配一致，这是第三次），共性都是**不变式的输入不被任何机制保证**。故我在 PRD 里没有只改数字，而是把「PG 无内建事务执行超时 → 事务级上界只能来自应用层」写成 AC-4.7 的第 2 条，让后来者看到的是**为什么**，不是一个孤立的 5000。 ② **拆成两个派生量是对的，且「原式漏了 claim 事务」这条我认领**——AC-3.6 的不变式是我在 CN-005 写的，当时只想着「写回在预算之外」，没意识到 claim 同样在预算之外。两个量用途确实不同：收尾余量只需覆盖停机瞬间的写回，AC-3.6 要覆盖完整链路。 ③ **测试 25② 我从产品侧同样认为是三项里最关键的**——它是唯一能在回归中守住本次订正的用例；Developer 的判断（「这条测试如果不写，这次订正的全部意义在回归中就没有守护」）我完全同意，已在 AC-4.7.6 把它写成**必测**而非建议。 **另附一条 PM 侧提醒（不改本 CN）**：不等式 2 的右侧从 `1800s` 收紧为 `1800s × 0.6`，这是本 CN 顺带引入的**新安全边距**，PRD 与设计需保持同一口径；我已在 AC-3.6 注明「留 40% 安全边距」。若将来有人只看 PRD 想放宽 N，会先撞上这个边距而不是直接撞 1800s——这是好事，但要写明来源，否则会被当成笔误改回去。 |

## 未指定角色和理由

无。三方均为受影响方或执行方。

## 执行状态

- [x] 三方确认（Developer 2026-07-30 / PM 2026-07-30 / **DevOps 2026-08-01**，三方齐）
- [x] 设计文档已落地（§2.6 / §4.10 / §8 测试 25）
- [x] PRD 侧 AC-4.7 订正（PM，2026-07-30，commit `6465218` 改 21 行；本格由 DevOps 照既成事实代勾，2026-08-01）
- [x] `v0.2.md` Change Notes 表登记（2026-08-01 由 DevOps 补勾——该行本已登记，同时订正六行过期状态并合并一处重复的 CN-006 行）

## 末票附条件的处置（2026-08-01，Architect）

DevOps 末票带 2 中 2 低，其中**中① 经 Owner 受理**，归 Architect（设计）+ PM（PRD）落地。逐条回执：

### 中①（已落地）`connect_timeout` 是第四次「校验通过但保证不成立」—— 判断成立，我接受

**DevOps 的推演我逐步复核，成立**：`run_tx` 的 `asyncio.wait_for` 包住的是 `_inner()`，而 `pool.connection()` **在 `_inner()` 内** → 建连耗时计入事务预算。原取值 `connect = tx = 5000`，**光建连就能吃光整个事务预算**。且这不是假想路径——事务超时会导致连接被池丢弃（Developer 注意点 a），而**紧接着的那次重试正好需要新建连接**，三次尝试可能全耗在建连上、一条语句都没执行。

**后果的定性也准确**：18s 上界不破、停机安全性不受影响，**破的是 §4.6 有限重试的立论**——「这条已花掉 240s 预算和一次 LLM 调用的费用，不该因几毫秒抖动就整个丢弃」。重试若全耗在建连上，这句话就落空了。

**落地**（设计 §2.6）：

- `AI_DB_CONNECT_TIMEOUT_MS` **5000 → 1000**；
- **并入不等式 3**：`connect(1s) < lock(3s) < statement(4s) < tx(5s)`，四个量全部进启动门禁，不再有「被排除在门禁外、可自由漂移」的超时量；
- 实测支撑采纳其数据（同机内网建连+查询 35~37ms，1000ms 为 25 倍余量）。

**关于「第四次」这个归纳，我认为值得单独记一笔。** 四次分别是：CN-004 的 `N ≤ 8`（基准 79s 不被任何机制保证）、CN-005 的「三层配一致」（措辞被读成数值相等）、CN-008 主体的语句级 vs 事务级（`statement_timeout` 挡不住多语句事务）、本条的 `connect_timeout`（被排除在门禁外）。**同型之处是：式子本身都没错，错在某个输入量没有被真正约束住。** 这四次里有三次是我确认过的文档——**前三次我都在事后才发现，这次是 DevOps 在我之前发现的**。归纳出这个模式比修好这一条更有价值：**今后凡新增一个参与不变式的量，第一件事是问「它自己被什么约束着」，而不是「代进去算得通吗」。**

### 低④（已落地）公式多算一个重试间隔 —— 保守方向，不改，但已注明

`(RETRY+1) × (TX + DELAY)` 隐含 3 个间隔，3 次尝试之间实际只有 2 个 → 18s 比真实上界 17s 多算 1s。**采纳其建议不改**，并已在设计 §2.6 显式注明「多算一个间隔是有意的保守」——理由正如其所说：本迭代已因动算式翻过两次车，若将来有人把这 1s 当笔误抹掉，等于把余量吃掉而无人察觉。

### 中②、低③（DevOps 自行落地，Architect 无异议）

中②（`deploy.sh` 增 `ALTER ROLE` 反向校验）与低③（`ALTER ROLE` 属对 xiaobao 库的持久化写入，执行时经 coordination 知会）均属部署侧，**我确认其方向正确**：中② 正是为变更 3 约束 1 补上强制点——原文只写了「取值须与应用层一致或更宽松」却无任何机制阻止违反，属「有约束无执法」，其补的裸连接 `SHOW` 断言正好补上这一环。

### 附带落地：Developer 在 CN-008 确认时提的两条实现注意点

- **`TimeoutError` vs `CancelledError` 的区分**已写进设计 §4.10，并与 O-8 互相点名——**一旦 `run_tx` 外层写成 `except BaseException`，超时重试与优雅停机会同时失效**。两处原本分开写，容易各看各的。
- **`wait_for` 取消后连接被池丢弃**已记入 §4.10，并把「超时后池仍可正常取连接」加进测试 25④。
